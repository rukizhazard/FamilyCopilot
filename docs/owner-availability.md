# Bounded owner-only two-person availability

## Kimi coexistence activation, 17 September 2026

The later user-coordinated restart enabled the separate native Kimi path in the
existing Windows-auth task. Parent native execution, exact two-person bounds,
snapshot location/semantics and cleanup gates are unchanged. Locked parent and
child metadata preflight, exact process identity/mode, live/disk safe idle,
date/saved/October/child markers and current `tpbl-teams-v2` assets passed.
Full offline suite **786/786** passed. No parent refresh, child list/import or
private snapshot read occurred in this activation; prior parent true-refresh
approval remains consumed. Kimi's one-workflow deployment approval was separately
used once, superseding the historical unused statements below.
[Current child activation and user-only test](kimi-calendar-import.md#shared-local-service-activated-17-september-2026).

## Windows-native parent Update repair, 17 September 2026

The owner explicitly requested a true Mike/Debby Update to test existing access,
then repair of the failing path. This later authorization supersedes the earlier
October migration's no-agent-query restriction only for the parent repair test.
The original WSL request returned HTTP 503/unavailable, cleanup not requested and
local idle. It was an actual failed refresh, not evidence of revoked Outlook
access. The precise tenant policy behind historical changes is not established.

Windows-native metadata preflight subsequently verified account, exact caller,
ARM credential, existing connected Outlook, exact Disabled availability v5,
Disabled owner-list v2, SAS Disabled and invitation/delivery preservation.
The worker uses installed VS Code's native Node runtime and existing Windows
Azure CLI credentials. Only explicit application modules are temporarily staged
on Windows, not auth files or the private snapshot. A disposable empty extension
directory isolates optional CLI extensions and is removed after execution.
No token/caller/ARM body/callback crosses the native protocol; only validated
busy-only data, binding and fixed status frames return. The WSL operation lock
covers startup, execution, native process exit and independent cleanup.
No PowerShell execution policy, Conditional Access or broker setting was changed.

### Actual activation and refresh evidence

- Initial activation attempts did not finish the task handoff; one explicitly
  reported `task_start_timeout`. Other generic failures do not prove their cause.
  Fresh inspection found no 8002/18002 listener. No force kill was used.
- The successful activation repeated metadata preflight under the existing lock,
  confirmed 8002 already stopped, started the existing **Windows auth** VS Code
  task, then received `verify`. Exact process identity and native execution
  environment were checked. Local live/disk idle status and date/saved/October
  markers passed; current activity assets matched disk and `tpbl-teams-v2`.
  The delegated local activator printed `cloudRequests: 0`, which describes only
  its local checks, not native Azure preflight; the combined report is now fixed
  to state `cloudMetadataVerified: true` rather than claim zero cloud requests.
- One explicit refresh through the running **8002** API returned HTTP **200**,
  **cached false**, **Mike checked / Debby checked**, **336 slots each**, and
  **workflow_disabled**. No automatic calendar retry or cache fallback occurred.
- Independent native readback after the response verified Disabled availability
  v5/owner-list v2, SAS Disabled, connector connected, invitation disabled,
  delivery off, preserved-resource checks and extension-directory removal.
  Local status returned idle. This verifies actual read access, not only login.
- The server's normal successful refresh wrote the bounded busy-only snapshot
  at its existing WSL location. Verification did not directly inspect the file,
  output its contents, clear/delete it, or copy it to Windows. No real calendar
  DOM or school documents were inspected. No workflow deployment or target,
  date-window, disclosure, connector or permission change occurred.

### Reusable checks and remaining scope

[Native activation](../scripts/activate-windows-owner.js) requires explicit local
restart approval and coordinates the existing task handoff, with no calendar
query. [One-shot parent refresh verification](../scripts/verify-parent-refresh.js)
requires separate explicit live-query approval (`--approved-live-once`), makes
one exact October refresh, reports only safe booleans/counts and independently
checks native metadata. It never calls Clear or saved-only. Do not reuse the
historical September verifier for this repair: its default projection and final
Clear differ from the current window/retention contract.

Baseline full suite **629/629**; final full offline suite **642/642**. Eleven new
refresh-verifier tests cover exact bounds, redacted output, wrong-page gating,
busy state, cache/synthetic/cleanup/malformed failures, no retry/no Clear and
independent readback. Two activation tests cover locked preflight, fail-before-stop
and native-only handoff. Editor diagnostics and whitespace checks passed.
Fresh browser/mobile/keyboard and parent usability are not newly established.
Kimi still requires the native child operation boundary, verified connector
capability, guarded deployment/readback and user-only selection/confirmation.
The existing one-workflow child approval is unused; no new OAuth is currently
needed for parent access. No commit or push was performed.

## Approved October demo

The owner explicitly approved one existing Azure workflow **v4 → v5** migration
for Mike and Debby's **9–15 October 2026** demo, together with existing official
basketball discovery. It does not authorize agent-run calendar validation,
arbitrary live dates, new resources, production readiness, or commit/push.
The protected identities, caller authorization, connector and busy-only scope
are unchanged. Secure target values are supplied only through operator stdin;
ARM redacts them, so exact secret-value readback cannot be claimed.

**Current checkpoint: cloud migration and local activation completed on
16 September 2026.** The migration CLI accepts `october` only as a
one-way v4-to-v5 mode: cooperative local lock, fresh matching-origin CSRF idle
check, exact Disabled v4 preflight, unchanged owner/connector/invitation resources,
immediate full pre-write re-read, one fixed-resource PUT, exact Disabled v5
readback and preservation checks. Unexpected state stops without adapting/retrying.
SAS stays Disabled; no enable, callback or invocation is allowed in this mode.
This is **not atomic Azure compare-and-swap**.

The v5 delta is only the contract tag and bounded request/response timestamps:
Taipei 9 October 00:00 to 16 October 00:00 exclusive, UTC
`2026-10-08T16:00:00Z` to `2026-10-15T16:00:00Z`; 336 slots per person.
Historical v1–v4 factories and September fixtures remain frozen. Dynamic date
selection is still only UI/synthetic/public-search capability; live requests must
match the full October window, not a subset. The empty-tab default changes to
October without rewriting remembered dates or granting consent.

The single-file October store validates a September envelope with its original
v4 binding/context/window before treating it as an October miss. It never shifts
statuses or timestamps, writes on miss, or implicitly fetches from a provider.
This is not multiweek storage: the retained September file is not viewable through
the October live UI. A successful projected October result atomically replaces it.
Before querying, RAM is cleared and pending writes fenced without deleting a
completed disk file. Generic unavailable/empty/wrong-window responses return no
old data and leave prior bytes intact. A later explicit saved-only request may
reuse a matching prior October snapshot with its original timestamp after a
generic Update failure; it is not fallback within that failed request. Explicit
Clear and known revoked/expired/blocked/contract/cleanup failure still delete;
corrupt/foreign/private-extra envelopes block without implicit deletion/query.
Startup/status and out-of-range requests do not inspect snapshot existence.

The new `owner-contract: bounded-availability-v5` marker gates all new UI APIs.
The operator activation script verifies actual cloud v5/Disabled first, then exact
port-8002 PID/command/cwd/start time and fresh safe-idle status under the local lock.
It sends SIGTERM only to that process, requires graceful exit and a released port,
then waits for the existing persistent owner task and verifies a fresh live/disk
v5 status. There is no force kill, automatic retry or calendar/cache validation.

### Actual October deployment and activation, 16 September 2026

- **Migration attempted: yes, exactly one PUT.** Initial locked metadata-only
  inspection confirmed exact Disabled v4, not v5, SAS Disabled, preserved guards
  and local safe-idle. A fresh prior-contract check and the existing `updateOctober`
  helper ran under the existing operation lock. No prior successful v5 migration
  was present; older undocumented attempts cannot be established from that check.
- The sole PUT targeted **familycopilot-availability-dev** in subscription
  **609bbde3-d152-4d7d-a12b-005e38ac4f27**, group **vdi-prebuilt-dte**. Attempt logged
  at **09:11:55.079 UTC**; result **october_updated_disabled** at **09:11:59.242 UTC**,
  with `sasDisabled: true`, `preservedResourcesUnchanged: true`, `queryMade: false`.
  Independent metadata readback at **09:12:02.579 UTC** verified the exact Disabled
  v5 contract, SAS Disabled and preserved guards. No second PUT, enable, callback,
  invocation, new resource or mutation of owner-list/invitation/connector occurred.
  Approved targets were constructed only in stdin/process memory, not a target
  file or target-bearing CLI arguments. ARM redaction prevents secret-value
  readback claims; only the exact redacted contract was independently verified.
- The existing activation helper verified cloud v5 and exact safe-idle owner
  identity, then reported `activation_unconfirmed` during its stop phase. A separate
  read-only check established **no listener on 8002** and fresh GET refused, with
  the operation lock released. Its generic output did not establish the precise
  stop-wait failure. No force kill or second process-stop attempt was made.
- A stopped-port startup coordinator rechecked Disabled v5 and refused GET under
  the existing lock. The initial synchronous coordination timed out before task
  startup; it made no mutation. The subsequent interactive lock holder allowed
  the **existing FamilyCopilot: local owner calendars task** to start. At
  **09:15:46.688 UTC**, identity and fresh matching-origin CSRF/status verified
  PID **87538**, `idle / not_requested`, live/disk, `safeIdle: true`, date and saved
  protocols true, and **owner-contract: bounded-availability-v5**. The task remains
  active at **http://127.0.0.1:8002/**. No further server stop/restart is required.
- A current full offline `node --test` run passed **473/473**, zero failures.
  Post-activation HTTP checks passed for the owner page and **7/7 required scripts**;
  served metadata/default dates match v5 and 9–15 October. Status-only recheck
  remained safe-idle. No real availability, saved-only, Clear, provider call,
  snapshot read/write/delete/existence probe, or school-file load was performed.
- **Fresh live-browser verification remains blocked, not passed.** Integrated
  page creation failed with an undefined browser context; isolated installed Edge
  failed CDP connection with `websocket_error`. Thus actual live-browser enabled
  Confirm, rendered empty state, zero startup API requests and mobile layout are
  not newly verified. Earlier synthetic checks below are separate. The failed
  isolated attempt did not confirm browser exit/profile removal; a scoped cleanup
  lookup found no matching disposable October profile/endpoint, so cleanup cannot
  be claimed. No existing user browser or other server was forcibly stopped.
- Existing basketball page/results were untouched and not re-fetched; concurrent
  school work was preserved. No implementation was redone, dependency installed,
  commit or push performed. Deliberately reload an older owner tab when ready;
  remembered dates are retained, so select **9–15 October** before the owner's
  separate **Confirm**. Real schedules and snapshot existence remain unknown.

### October verification evidence

- Baseline: **460/460**. Final `node --test`: **473/473**, repeated under
  `TZ=America/Los_Angeles`: **473/473**. Syntax, editor diagnostics and whitespace
  checks passed before deployment. Tests use disposable synthetic files only.
- Regressions cover one PUT and drift/conflict rejection, exact windows/336 slots,
  consent/old-marker gating, no startup/status disk access, saved-only misses,
  byte preservation, partial/unknown data, atomic replacement, explicit deletion,
  revocation and late-write fencing, date handoff and public-only request bodies.
- Integrated browser creation was unavailable. Isolated headless Edge passed
  zero-startup-API October defaults, separate saved miss/Confirm, 672 synthetic
  slots, saved grid/original timestamp, keyboard result focus and grid exit,
  accessible date labels, no page overflow at 1280/320 CSS px, and hiding old data
  on invalid/date edits without an automatic availability query. The subsequent
  Activities label assertion and repeat-run harness setup failed; full browser
  handoff is **not** counted as passed. Offline handoff regressions passed.
- The existing user basketball page and its seven user-reported listings remain
  untouched; listings were not re-fetched here. No tickets, end times, travel,
  suitability or real schedule compatibility were established.
- No real calendar query, real snapshot read/clear/existence check, school-file
  access, new resource, dependency, commit or push is part of this verification.

Official Microsoft references were rechecked for this increment:
[Graph getSchedule](https://learn.microsoft.com/en-us/graph/api/calendar-getschedule?view=graph-rest-1.0)
documents dateTime/timeZone boundaries and work/school delegated support (not
personal delegated accounts); status zero may include working elsewhere.
[Logic Apps create/update](https://learn.microsoft.com/en-us/rest/api/logic/workflows/create-or-update?view=rest-logic-2019-05-01)
documents standard workflow PUT, state/access control and SecureObject parameters,
not a mandatory atomic If-Match contract. No provider implementation scope changed.

## Saved-week recovery (16 September 2026)

The reported generic failure cannot be attributed to a historical provider error:
the safe local status returned `idle / not_requested`, and this process retains no
last-request error code. No real browser calendar contents, cache contents or Azure
resources were inspected. Static code confirmed a separate destructive defect:
after a request, date edits sent `/api/clear` with `reason: "range"`, which deleted
the completed snapshot before a later unsupported-date request. A RAM range miss
also cleared its entry. The availability endpoint itself already rejected other
live ranges before cache/provider effects; it was not the destructive gate.

The repair retains completed snapshots across date changes and cache-only range
misses, but never serves them under other dates. Range changes still fence writers,
cancel pending operations and wait for cleanup; they cannot clear a sticky failure
or revive revoked sessions. Explicit Clear still removes the file. Known auth,
contract and cleanup failures still invalidate data. A genuine Update still
invalidates its old snapshot before provider work, without fallback; data already
deleted by an earlier update or date edit cannot be recovered by this fix.

**View saved only** uses the same Host/Origin, CSRF/session and explicit scope gates,
with exact dates and `cacheOnly: true, refresh: false`. Misses return `cache_missing`
without provider work or deletion. The UI explains missing data and leaves Confirm
as a separate explicit action. No startup/status read of a snapshot was added.
Live/disk ranges remain exactly **20–26 September 2026**, not arbitrary subranges.
Unsupported selections show a plain explanation and **Choose 20–26 September 2026**;
this button changes dates and focuses View saved only, but loads nothing. Basketball
dates and search remain independent. There is no implicit synthetic fallback.

The additional `owner-saved: preserve-v1` marker prevents new static UI from making
requests against an older destructive backend. Baseline **398/398**, initial repair
suite **404/404**, and the final executed full suite **405/405** passed (0 failed,
cancelled or skipped). Editor diagnostics for the repair files and documentation,
and `git diff --check`, were clean. Synthetic fixtures cover disk
bytes unchanged across original → unsupported October → original saved-only views,
fresh-server reuse, misses, stale/partial context, no provider calls, invalid bodies,
origin/session rejection, explicit deletion, sticky failures and late-write fencing.

**Final browser evidence, 16 September:** integrated browser creation failed, so
validation used isolated headless Edge with a disposable profile, never a user
profile or an existing loaded calendar page. The temporary synthetic disk store
was seeded only from the partial fixture with `checkedAt: 2026-09-15T01:00:00Z`.
At 1280 CSS px, startup made zero API requests. Keyboard View saved only loaded
20–26 September; selecting 9–15 October hid all prior results, disabled calendar
actions and explained the fixed range without an availability request. Keyboard
Choose 20–26 September changed dates/focused View saved only without loading.
A second explicit saved-only action restored identical grid, original timestamp
and freshness text. Both successful requests used `cacheOnly: true, refresh: false`
and the exact September dates. The disposable file remained byte-for-byte unchanged;
instrumentation recorded zero provider-adapter calls and no store write/clear during
review. Debby (sample) remained explicitly unavailable, not free. Layout had no
page overflow at 1280/320 CSS px; result focus and keyboard exit from the grid passed.

Browser-injected responses separately verified **“Couldn’t load the week. Try
Update once.”** for `unavailable / not_requested`, with old results hidden, and
**“No saved view exists for these dates. No calendars queried.”** for `cache_missing`.
There was no automatic retry or provider fallback; Confirm remained a separate
choice after a miss. These are synthetic error checks, not a diagnosis of the
historical generic failure. The temporary server was gracefully stopped, its
temporary data directory removed, and the isolated browser/profile closed/removed.

**Actual activation completed:** under the existing loopback operation lock,
port 8002 had no listener and a fresh page GET returned `ECONNREFUSED`. It was
already stopped; there was no PID to stop, and no force-kill or other-server change.
The existing **FamilyCopilot: local owner calendars** persistent task started the
current script (verified workspace/command, PID **71082**). While still holding
the lock, a fresh GET and CSRF-bound status-only POST used matching
`http://127.0.0.1:8002` Host/Origin. Both returned 200; status was
`idle / not_requested`, live/disk, with `safeIdle: true`, both protocol markers
including `owner-saved: preserve-v1`, and no sticky cache/cleanup failure. This is
local process status, not a fresh cloud-state check. No availability/cache request,
Azure/provider call, real-file read/write/delete or existence check occurred.

A new empty browser page at **http://127.0.0.1:8002/** showed the saved marker,
enabled Confirm/View saved only, disabled Update, and only Unknown placeholders,
with zero startup API requests and no JavaScript exceptions. The task remains
running. Existing user pages were not inspected or refreshed; deliberate reload
is needed for an older page session. Real snapshot existence is **unknown** and
was not probed. This completion changed documentation only, not the implemented
repair. Concurrent school-calendar work was preserved: its unfinished core asset
returned 400 on the new server (404 on the older synthetic review server); owner
saved-view controls worked. School-file loading was not attempted or validated.

This repair supersedes the original date-edit deletion statements in the historical
record below. Dynamic live workflow approval and parent usability remain separate.

## Date selection and Confirm (16 September 2026)

**Later dates-only basketball handoff:** selected dates now restore before any
Confirm/query and survive same-tab, same-origin navigation in one bounded
`sessionStorage` key. Activities uses these dates only, never busy-status data,
names, ages or consent. Invalid dates block public search; Reset dates on Activities
forgets only the date key, while calendar Clear retains its existing snapshot
semantics. [Exact contract](basketball-search.md#dates-only-handoff).
After combined **397/397** offline passes in both tested time zones, the verified
idle disk-backed 8002 process was gracefully restarted under the local lock.
No real calendar/cache access or Azure operation occurred. Reload an older owner
tab deliberately for a fresh local session; nothing loads automatically. This
does not authorize other live calendar ranges. The original validation record
below describes the preceding date-UX-only increment.

The owner approved **select start and end dates → Confirm → load calendars**, with
no separate owner checkbox. The visible scope remains Mike + Debby, default
calendars, Busy-only. Confirm is the explicit access action; it is not identity
verification, visitor sign-in or a change to the authorized account/audience.

- Strict ISO dates, years 2000–2100, **1–7 inclusive days**. Both dates are full
  days in Asia/Taipei (UTC+8), with the API end at the following midnight exclusive.
  A one-day view has 48 half-hour slots per person; seven days has 336.
- No requests on startup or initial date edits. Confirm sends the exact reviewed
  dates with the existing explicit acknowledgement signal and CSRF/session gates.
  Update is disabled until confirmed, then deliberately bypasses the matching cache.
- Any date edit immediately removes old rows, freshness and context and advances
  the UI generation. After a request it sends a bounded range-clear, removes the
  shared snapshot and cancels/fences pending work before enabling Confirm again.
  The HTTP response connection stays open until server cancellation is confirmed,
  avoiding the existing disconnect guard's deliberate session revocation. Date
  changes during cleanup cannot re-enable queries early or revive old responses.
- `/api/clear` with `reason: "range"` does not revoke the page; explicit Clear and
  leaving keep their existing revocation behavior. Storage/cleanup failures remain
  sticky, hide results and block reuse. Range-clear cannot revive a revoked session.
- Requested-window validation occurs before cache/provider work. Wrong-window
  responses cannot render or cache. RAM cache keys include the exact window;
  disk remains restricted to the existing fixed live contract, before any I/O.
- The live workflow remains **v4, 20–26 September 2026**. Other live ranges return
  `range_unavailable` without reading/clearing cache or querying the provider in
  that availability request. A preceding deliberate range-clear still removes a
  previously loaded snapshot, as disclosed above. No old-week substitution.
- The `owner-range: configurable-v1` page marker gates the new protocol. Older
  running servers cannot silently accept new UI assumptions: the new page blocks
  API actions and gives a safe-restart/reload notice. No automatic restart/retry.
- Activity search retains its own configured dates. No selected dates or calendar
  data cross to Activities in this increment.

**Validation performed:** full offline suite **380 passed, 0 failed**, including
concurrent basketball tests. Focused tests cover invalid/extra/missing dates,
leap-day/year boundaries, exact 48/96/336-slot ranges, partial/unknown projection,
cache isolation, live out-of-contract no-side-effects, same/cross-session clearing,
sticky failures, revoked sessions, late responses, old-server gates and explicit
Confirm semantics. Synthetic RAM browser review on isolated port 8015 confirmed
zero startup API requests, keyboard Confirm, changing dates without automatic
availability queries, exact year-boundary labels, invalid input blocking,
slow-request cancellation followed by successful reconfirmation, partial data and
layout at measured 319 CSS px without horizontal page overflow. Status is textual,
inputs are labelled, and Confirm/result focus remains keyboard reachable.

**Not performed/approved:** existing live-server restart, cloud read/write or
workflow deployment, real schedule/cache access, wider disk retention, parent
usability approval, commit or push. A safe coordinated local restart and separate
approval for the dynamic workflow/storage contract remain the next activation
steps. The historical checkbox and fixed-week UX records below are superseded
only by this local date-selection/Confirm implementation, not by a live deployment.

## Approved scope and current outcome

The owner's explicit implementation/deployment approval supersedes the earlier
[metadata-only feasibility milestone](availability-feasibility.md), **only** for
this bounded local experiment. No repeat architecture or actor approval is needed.
The two exact owner-supplied adult SMTP addresses are secure operator parameters,
not repository fixtures, command-line arguments, browser inputs or directory probes.
Person 0 is Mike Lee; person 1 is the owner's confirmed Debby target. Labels are
not a claim of directory lookup or secure-value readback. The audience
is the local owner for the explicitly authorized scheduling purpose, not an assistant.

- Fixed range: **2026-09-20 00:00 through 2026-09-27 00:00 exclusive,
  Asia/Taipei (UTC+8)**, equivalently **2026-09-19 16:00Z through
  2026-09-26 16:00Z exclusive**. Seven full days, 30-minute intervals,
  **336 slots per person, 672 total**. Absolute dates never roll on reload.
- Existing authorized Outlook connector/current Azure CLI operator only.
- New **familycopilot-availability-dev** workflow in the existing approved
  subscription/resource group/region. Existing owner-list, invitation and connector
  resources are reference-only and guarded against changes.
- No mail, meeting/event/calendar writes, registration, scope expansion, connector
  reset, booking, background refresh, browser persistence or automatic retries.
  Only the local redacted snapshot persistence described next is approved.

### Parent-friendly calendar presentation

16 September 2026. This presentation-only increment supersedes the earlier compact
confirmation copy. The page leads with **Our week**, the fixed **20–26 September
2026**, **Taipei (UTC+8)** and **Mike / Debby**. **Default calendars · Busy-only**
and one unchecked **I’m the owner, authorized to view this week** confirmation
precede **View week**, **Update**, and secondary **Clear**. No provider/cache access
occurs on startup or when the checkbox is checked alone.

The single routine status reads **Updated [original date, time] · Saved view**.
It uses the original `checkedAt`, including on reuse, and adds **May be out of date**
after five minutes or when freshness is unknown. It does not claim continuous
updates. The visible source is **Outlook · Default calendars only**, or **Sample
data · Not real calendars** with the always-visible **SAMPLE DATA** badge and sample
aliases. Missing people are named, for example **Debby’s schedule isn’t available**;
Unknown keeps text and hatching. No missing time becomes free.

**Details** remains closed below the calendar and contains exact source, access,
retention, permission limits, successful cleanup diagnostics and keyboard guidance.
Routine success is neutral, not an orange warning. Urgent failures remain above the
grid with status announcements and focus: expired pages offer **Reload page**, safe
load failures suggest one deliberate **Update**, and cleanup blocks say **Updating
paused. Get help in Details.** Failed deletion stays visible and offers Clear/help.
Unknown or pending cleanup is never hidden as routine success. No automatic retry,
automatic acknowledgement, authentication, backend, storage or provider change.

The existing warm theme, seven-day geometry, protected targets, static asset routes,
DOM IDs and shared cancellation handlers are retained. No dependencies, server
restart, actual calendar/cache access or Azure operations are needed. Reload the
owner page when ready; this does not query calendars. Other uncommitted UX work is
outside this increment.

### Compact access confirmation

16 September 2026. Presentation-only simplification removes the dedicated
**A quick access check** card and repeated facts. The visible summary is
**Mike + Debby · Default calendars · Busy-only**, with one unchecked confirmation
beside **Check week**: **I’m authorized to view these calendars for this week.**
The existing heading supplies the week and Taipei timezone. Exact local/UTC bounds,
336 slots each, display-target mapping, owner-only audience and dynamic storage/
retention text remain in the initially closed **Access, privacy & cleanup details**.
A short cache/query and permission-recheck note stays visible. Synthetic summaries
retain **(sample)** names, prominent sample notices and fictional source mapping.

The same acknowledgement ID and handlers gate Check week and Refresh; checking
alone loads nothing, and withdrawal retains the existing cleanup behavior. No
authorization, fixed targets/dates, core logic, provider, backend or snapshot change.
No real page, API, cloud operation or owner cache was accessed; no server restart.
Reload when ready to see the presentation; reload itself never loads availability.

Baseline: **270 passed**. Four focused presentation assertions failed before the
change; updated focused tests: **30 passed**. Final **`node --test`: 272 passed,
0 failed**; editor diagnostics, `git diff --check` and seven-file whitespace,
conflict-marker, secret-pattern and stale-selector/DOM checks passed.
Synthetic browser review at actual
**1280px / 319px** confirmed one visible checkbox, compact wrapping without page
overflow, keyboard/focus outlines, disabled actions before acknowledgement and
enabled actions afterward, accessible descriptions, closed/keyboard-expandable
details and unchanged Unknown/sample labels. **Zero startup or review API requests**;
neither Check week nor Refresh was activated. Screen-reader speech and parent
usability approval remain separate from these checks.

### Our week presentation copy

16 September 2026. The owner approved only **Our week** as the weekly heading and
**Mike / Debby** as display labels. These supersede the historical Dad/Mom labels
below, without changing **Mike → Mike Lee; Debby → Debby**, protected indices/targets,
authorization, the Sunday **20–26 September 2026 Taipei** window, 336 slots each,
CSS geometry, legend, unknown/freshness semantics or snapshot retention/binding.
Aliases are not cache keys; no cache migration or invalidation is needed.

The access summary, daily tracks, per-person loading/missing context, grid region,
block descriptions and tooltips use the same display mapping. Synthetic pages
use **Mike (sample) / Debby (sample)** and explicitly map them to the existing
fictional Alex/Sam examples, with **SAMPLE DATA**, **SAMPLE calendars** and the
adjacent source notice unchanged. The adversarial fixture statuses are unchanged.
No family-role/relationship assertion, nickname settings, Add someone or multiuser
flow is implemented. No dependency, backend, auth, cloud or cache-binding change.

Baseline: **269 passed, 0 failed**. Updated copy regressions failed before the
presentation change (6 expected failures). Final **`node --test`: 270 passed,
0 failed**; focused owner/UI/presentation tests: **33 passed**. Editor diagnostics,
tracked diff whitespace and seven-file whitespace/conflict/secret-pattern checks
passed. The other **76 repository files** have identical before/after fingerprints,
including CSS, fixtures, backend, cloud/auth/cache helpers and other-chat UX work.

Isolated RAM-only synthetic review on port **8004**, at actual CSS widths
**1279px / 319px**, confirmed the title, visible/accessibility sample names,
Alex/Sam source mapping, unclipped track labels, unchanged seven-day/672-slot
geometry, keyboard acknowledgement/load, status focus, mobile internal arrow-key
scrolling and Tab exit. Partial context keeps Debby (sample) Unknown; Clear resets
both samples. Startup made **zero API requests**, with no external resources.
Only synthetic screenshots were inspected. The created tab was closed and its
temporary server gracefully stopped with no cleanup failure. No real page/API,
provider/Azure operation or owner snapshot read/deletion occurred. No commit/push.
Screen-reader speech and parent usability approval remain unverified.

The static-asset handler reads HTML/JS/CSS on each request: reload when ready,
without restarting the backend or discarding its snapshot. Reload itself never
loads availability. Existing loaded real pages and running servers stay untouched.

### Synthetic review source-label repair

The reported Sun 20–Sat 26 diagram showed four alternating half-hour statuses
for Dad and all-week Unknown for Mom. Metadata-only inspection established that
port **8011** ran `createServer` with `synthetic: true`, in this workspace, with
memory-only cache. Its HTML advertised synthetic mode but had the same browser
title as the real owner page. Port **8002** advertised live mode and disk cache.
No real cache content, loaded owner page, provider request or Azure operation was
used to diagnose this. The original shared browser page ID was no longer available;
its selected scenario was not read back.

The [built-in availability fixture](../browser-fixtures/availability.js) cycles
`free_or_elsewhere`, `tentative`, `busy`, `oof` every half hour; its `partial`
scenario makes the second person unavailable. Reproducing that fixture on an
isolated synthetic server matches the reported pattern. This is evidence of a
misleading synthetic handoff, not evidence that the family's calendars contain
those blocks or that Mom's real calendar is inaccessible. The adversarial fixture
remains unchanged for regression coverage, rather than substituting invented
plausible commitments to hide the symptom.

- Preserve the concurrent warm UX, seven-day CSS geometry, real Dad/Mom alias
  mapping, approved range and consent/cache protections.
- Add **SAMPLE calendars** tab title, **SAMPLE DATA** header, explicit
  **Sample data—not real calendars** notice, and **Dad (sample) / Mom (sample)**
  in every day and accessible block description. A source notice beside the grid
  survives a screenshot that omits the page header. The owner entry point is stated.
- New server-rendered title/header labels work without JavaScript. Current static
  UI scripts also label older running synthetic backends after reload, without
  restarting them or loading data. Unrendered markup says source not verified.
- Fix the synthetic constructor's calendar-list default to use its fixture too;
  explicitly injecting either built-in fixture into live mode now fails before
  any request. General test dependency injection is not a security boundary.
  Browser mode matching still rejects synthetic responses, including cached ones,
  on a live page. Temporary-file tests verify both directions of cache-mode binding
  rejection without deleting the mismatched file. No actual owner file was opened.
- Offline regression checks reconstruct all **672** day/person/half-hour positions
  and verify exact Sunday/Taipei UTC bounds. Synthetic browser review at actual CSS
  **1279px / 319px** verifies seven correct headings, 00:00–24:00 placement, labels
  without clipping, no page overflow, mobile internal keyboard scrolling, focus,
  partial Unknown and Clear. The shared time axis and 48-row tracks differ by less
  than 0.4px from browser rounding, not a half-hour/day shift.

Baseline **263** offline tests passed; final suite **269 passed, 0 failed**.
Focused UI/server/cache tests: **58 passed** each under UTC and America/Los_Angeles.
Editor diagnostics and `git diff --check` passed. The dedicated synthetic 8004
page was closed and its server gracefully stopped. Existing 8002/8003/8011
processes were not restarted or stopped. A fresh 8011 page also verified the
updated sample labels on its older backend with zero startup API requests.
That fresh tab was then handed off to **http://localhost:8002/**, confirmed
live/disk mode, unchecked consent, empty Unknown grid and **zero API attempts**.
No existing loaded owner tab or owner snapshot was read, cleared or changed.
The page's current static UX needs no backend restart; new constructor safeguards
apply to future server instances. No commit or push. Parent usability and
screen-reader review remain separate; no true calendar accuracy claim or provider
verification is made by this repair.

### Local-file snapshot, no TTL

The owner explicitly approved retaining this existing POC's snapshot while the
backend is stopped. This supersedes the historical RAM-only policy below, not the
fixed **Sun 20–Sat 26 September 2026 Taipei**, Dad/Mom protected targets, 336 slots
each or existing cloud/authentication checks. No cloud operations or true queries
are part of this implementation/validation increment.

- Live CLI startup explicitly injects [the disk store](../scripts/availability-disk-cache.js)
  into [the single snapshot cache](../scripts/availability-cache.js) and
  [owner proxy](../scripts/serve-owner.js). Constructors/imports do not touch home
  storage, and synthetic startup stays RAM-only. Tests inject temporary directories.
- Path: `$XDG_CACHE_HOME/familycopilot/owner-availability.json` for an absolute
  XDG_CACHE_HOME, otherwise `~/.cache/familycopilot/owner-availability.json`.
  The resolved directory must be outside this repository, owner-only **0700**;
  file **0600**, regular, current-UID-owned, not a symlink/hardlink. No DB, cloud
  storage, dependencies, credentials, browser persistence or encryption service.
- Single JSON envelope: format version, exact contract, SHA-256 local binding,
  original verified cloud context digest, and strictly projected `data` containing
  only `window`, original `checkedAt`, and indices 0/1 with fixed statuses and
  336 allowlisted enums each. No addresses, names, raw/provider events or IDs.
  Projection precedes writing; reads reject extra/malformed fields, mismatched
  contract/window/index/binding/version, invalid or wholly failed snapshots.
  Both stat and actual reading are bounded to **24 KiB**, never an unbounded read.
- Local binding includes OS UID/home, workspace, live/synthetic mode, configured
  workflow, contract, fixed week and index mapping. The existing verified
  caller/configuration-version fingerprint is carried from the original query,
  not replaced with an alias or freshly authenticated at restart. No Azure/CLI
  access is needed to retrieve it. **Same local owner/configuration is assumed**;
  neither changing CLI accounts nor remote config/permission changes are detected
  offline. Clear before owner/target/configuration changes; Refresh performs the
  unchanged live checks. This is not multitenant authorization or tamper-proof
  storage against someone with the same OS account/root access.
- No file read/display/query on startup, page GET or status check. Exact
  Host/Origin, CSRF, 30-minute session, explicit acknowledgement and existing
  cleanup block apply before cache load. **Check week** reads RAM/file then queries
  only on a genuine miss. Invalid storage/I/O errors do not trigger a cloud query.
- **Refresh** removes the previous snapshot before the explicit query. A successful
  result is projected and saved through a 0600 temporary file, flush and same-directory
  atomic rename before display. No old-snapshot fallback on write/query/auth failure.
  Temporary files are never loaded as snapshots; a force-kill during a write can
  leave a private temporary artifact. No secure-erase or power-loss durability claim.
- **Clear / cancel**, withdrawal, known access/contract failure or sticky cleanup
  failure invalidates RAM and advances the generation synchronously before I/O.
  File validity is destroyed before unlink, so an unlink failure after truncation
  cannot restore a valid snapshot at restart. Cross-session Clear aborts pending
  availability and waits for independent disable cleanup; late writes cannot save.
  Deletion failure reports **not confirmed** and keeps reuse blocked. Retry Clear;
  if filesystem permissions/device failure prevents both invalidation and removal,
  data may remain: operator storage recovery is required, never restart to bypass
  the block. Clear does not erase backups, other displayed copies or Azure history.
- **Page exit/reload** cancels that page's work without deleting a completed
  snapshot. Graceful backend shutdown retains the file. No TTL or refresh timer:
  original `checkedAt` remains unchanged; older than five minutes is stale.
  **Provider permissions are not rechecked for cached snapshots.** The backend
  must run to serve the page/cache; retaining a file is not browsing while closed.

Activation: existing real port 8002 was not stopped, queried or migrated in this
increment. Its RAM may contain user results; the old process offers no cache-only
export and Check week might query on a miss, so no migration probe is safe here.
The next safe launch of the existing live task enables local-file storage. Only
subsequent deliberate successful Check week/Refresh saves results. Reload alone
cannot upgrade a running backend. UI mode comes from the server; old/memory-only
processes are not described as persistent. No new live query is claimed.

Validation baseline: **241 passed**. Dedicated temporary-directory tests cover
cross-process reuse without provider access, HTTP restart with new CSRF/acknowledgement,
31-minute-old retention/staleness, Refresh replacement, Clear/restart miss,
permissions/atomic rename, strict redaction/bounds, corrupt files, I/O failures,
failed deletion, and Clear during asynchronous cleanup with late-response fencing.
Final full offline suite: **263 passed, 0 failed** (includes five concurrently
added presentation tests, not attributed to this cache increment). Dedicated
cache/HTTP/UI tests in **America/Los_Angeles: 52 passed, 0 failed**. Editor
diagnostics, `git diff --check`, and the 12-file scoped whitespace/private-address/
secret-pattern/relative-link checks passed. No new dependencies or cloud changes.

An isolated temporary-disk synthetic server on port 8004 verified zero startup
API calls, keyboard acknowledgement/load and status focus, cached reload with the
unchanged original timestamp/stale/permissions-not-rechecked disclosure, explicit
Refresh, Clear followed by a miss, and an injected deletion-failure message with
reuse blocked and explicit Clear recovery. Actual CSS widths **1279px / 319px**
fit without page overflow; mobile has internal calendar scrolling and keyboard
exit. Backend restart persistence itself was tested with a new HTTP server/session
and a separate Node process, not claimed as a browser-only observation.

The synthetic review server was gracefully stopped and its own temporary cache
removed. Socket metadata confirms the existing real 8002 and synthetic 8003
listeners were preserved; no real page/cache content or provider was accessed.
The live RAM-to-disk activation remains pending a safe restart as described above.
Unrelated/concurrent work is preserved; no commit or push. Parent usability,
screen-reader validation and production readiness remain separate.

### Sunday week and RAM cache

Historical RAM-only increment; local-file retention above now supersedes restart
deletion. Its cloud migration evidence remains historical, not a new query approval.

Current approved POC: **Sun 20 → Sat 26 September 2026**. Headings derive from
actual dates, not rotated labels; status offsets stay bound to the matching day.
Same exact Dad/Mom targets, busy-only contract and 336 slots each. The only cloud
change authorized here is a guarded **Disabled v3 → v4** configuration PUT; no
workflow enabling, true schedule query, connector/list/invitation change or retry.
Historical records below retain their original dates and one-query evidence;
they do **not** verify live availability for this new week.

- **Check week**: explicit owner acknowledgement required on every fresh page.
  Reuse the single verified snapshot if present, otherwise query once through the
  existing authorization/cleanup checks. Cache hits make no Azure request.
- **Refresh**: explicit new query, no overlap/automatic retry. Invalid or failed
  refresh discards the previous cache; no stale fallback. Repeated manual Refresh
  is allowed without reloading; calendar-list remains one-shot.
- **Clear / cancel** and acknowledgement withdrawal: erase shared RAM immediately,
  advance its generation, abort an in-flight availability operation even from a
  different page, and await existing disable cleanup. Cannot erase another page's
  already displayed copy or protected Azure history. Known auth/context/contract
  failures and cleanup failure also discard RAM. Sticky cleanup failure survives
  new sessions and must not be bypassed by restarting merely to drop the flag.
- Reload/page exit: clear tab state and cancel only that page's pending operation;
  preserve a completed verified RAM snapshot. Nothing is automatically displayed
  or requested on the new page. Existing 30-minute page-session expiry/CSRF still
  apply **before** cache access and are not snapshot-retention limits.
- Retention: **no TTL, no 30-minute cache limit**. One entry in the owner server
  process, emptied by Clear or restart, no disk/database/browser storage/timer.
  Only projected window, original `checkedAt`, two fixed indices/statuses and at
  most 2 × 336 allowed enums. No labels, addresses, subjects, tokens or raw payload.
  Internal key binds fixed contract/week and verified caller/configuration-version
  fingerprint, never a display alias or browser-configurable target.
- **Last updated** stays original on cache hits; freshness older than five minutes
  is stale, not current availability. **Provider permissions are not rechecked for
  a cached snapshot**. There is no offline revocation detector or claim of one.
  Partial/missing context stays Unknown, not free; wholly failed/invalid snapshots
  are not cached. No new login, settings, dependencies or general storage layer.

The `sunday` operator path in [scripts/availability.js](../scripts/availability.js)
uses the existing lock, full exact old-contract/preserved-resource/adjacent snapshot
checks, one standard PUT and exact Disabled/SAS-disabled readback. It accepts only
the frozen v3 contract; already-v4 is not updated again. Historical `week` remains
v2 → v3 and `repair` v1 → v2. Secure target values remain protected operator
stdin/in-memory parameters; ARM secure-value equality is not claimed. Microsoft
[Create Or Update](https://learn.microsoft.com/en-us/rest/api/logic/workflows/create-or-update?view=rest-logic-2019-05-01)
was rechecked: standard PUT and SecureObject, not a new ETag/CAS requirement.

Validation baseline: **228 passed**. Full offline suite: **241 passed, 0 failed**.
Direct availability/UI/server runs in UTC and America/Los_Angeles: **72 passed,
0 failed each**. Editor diagnostics, diff/whitespace, relative documentation links
and scoped private-address/secret-pattern checks passed across 15 changed files. Synthetic browser
review at actual CSS widths **1279px / 319px** verified Sunday-first dates, two
336-slot rows, zero startup API calls, consent/keyboard/status focus, stale original
timestamp, cached reload (`cached: true`), explicit Refresh (`cached: false`),
Clear followed by reload/miss, acknowledgement withdrawal, independent partial
Unknown row, slow cancellation and no page overflow. Mobile scroll is internal
and keyboard-exitable. Only synthetic screenshots/results were inspected.

Cloud execution: **`sunday_updated_disabled`**, one exact v3 → v4 PUT;
`queryMade: false`, `sasDisabled: true`, `preservedResourcesUnchanged: true`.
Independent current-contract readback: **Disabled / SAS Disabled**, owner/list and
invitation Disabled, connector Connected, delivery off. No workflow was enabled
and no calendar query occurred. Exact secure parameter values are not claimed
readable from ARM. Existing metadata-only guards and standard PUT were retained.

After independent cloud readback plus an immediate local idle/cleanup/process
identity check under the shared operation lock, only the old **8002** owner
process was gracefully stopped and restarted using its existing task. It is idle
on current backend code with empty RAM. **Reload an old tab** for a fresh session;
no data loads until acknowledgement and an explicit button. The isolated **8004**
fixture process was gracefully stopped (cleanup verified, no failure), its review
tab closed; other existing servers and the loaded real user page were untouched.
No new live-week verification is authorized or claimed. Parent usability,
screen-reader and production readiness remain separate; no commit or push.

### Historical presentation-only weekly diagram (15 September 2026)

Approved presentation mapping: **Dad (previously Mike Lee)** and
**Mom (previously Debby)**. These are user-chosen display aliases only. No new
identity, parent relationship, guardian authority, secure target, access policy
or cloud contract is established or changed. The mapping is visible in the
pre-query access summary without addresses. Synthetic review instead maps Dad
to fictional Alex and Mom to fictional Sam and identifies its source as fixtures.

- One primary calendar diagram: **Tue 15 → Mon 21 September 2026**, seven day
  columns, each with Dad/Mom tracks and a shared Taipei time axis. Pure
  `weekLayout` merges only contiguous equal enum statuses within each person's
  day. Every track covers 00:00–24:00, preserving all 48 half-hours per day,
  336 per person; no segment crosses midnight. It never reads event details.
- Desktop fits all seven days. Mobile keeps legible day widths in one labelled,
  keyboard-focusable horizontal scroll region; time labels stay at the left.
  Blocks have short text plus exact date/person/time/status accessible names
  and hover descriptions, without hundreds of Tab stops. Unknown is hatched
  and explicitly labelled. “No busy*” still means no busy block reported,
  possibly working elsewhere, **not guaranteed free time**.
- The visible access summary and unchecked acknowledgement still gate one
  exact two-default-calendar/week request. The diagram starts entirely Unknown,
  with separate Not checked context for each person. Source, retrieval/freshness,
  missing/partial/unavailable and stale warnings remain visible. Clear,
  acknowledgement withdrawal, cancellation, session expiry and sticky cleanup
  failure preserve the existing fail-closed lifecycle and shared-session fences.
- Main controls are **Check week** and **Clear / cancel**. Long processing,
  authorization, retention and cleanup explanations are in a closed disclosure,
  including the explicit local-cleanup check. Historical deployment notices
  remain in this record, not the primary page. Choose calendars stays hidden and
  inert with its DOM and shared cleanup handlers retained. Fictional activity
  discovery remains a separate experience with no real-result ingestion.

Validation: offline baseline **223/223**; full suite **228/228**; focused
availability/UI **47/47**. UI tests also pass **13/13** in UTC and America/Los_Angeles;
the UI test clock is fixed. Editor diagnostics and diff/whitespace checks pass.
Synthetic browser checks used a newly created fixture-only server on **8004**,
never the real loaded page or the existing 8002/8003 services. Actual CSS widths
were **1279px / 319px** (embedded-browser rounding near 1280/320): seven visible
desktop columns, mobile internal scrolling, sticky axis, fitting block text,
keyboard acknowledgement/load/scroll/Tab exit, status focus, zero startup API
calls, two successful 336-slot fixture rows, merged mixed/partial Unknown runs,
unavailable second person, clear, slow cancellation, and injected session-expiry
reload with zero automatic queries passed. Only synthetic screenshots were used.
Screen-reader announcements and parent usability approval are not established.

The temporary 8004 server was gracefully stopped with fixture cleanup verified
and no cleanup failure; its synthetic review tab was closed. The existing 8002
listener was confirmed using socket metadata only, not an HTTP/API/cloud request.

No cloud request, live query, target/auth/infra change, commit or push is made by
this increment. The owner server reads static assets from disk per request;
**no restart is necessary**. Reload an old tab when ready to adopt the new UI;
this discards its existing local results, does not query availability, and does
not establish Azure cleanup. The loaded real page was not inspected or changed.

### Approved fixed-week milestone (15 September 2026)

The owner explicitly approved replacing the first protected target with Mike Lee,
retaining Debby, this exact full-week range, and **one true live verification**.
This is not permission for another list/query, mail, writes or background sync.
The protected parameter value is supplied through the existing bounded operator
stdin/in-memory path; no real addresses belong in files, fixtures, argv or logs.

Implementation and the exact v2 → v3 deployment are complete. The single approved
true live verification **succeeded**; no retry or second provider query was made.
Baseline: **212 passed**. Current offline suite: **223 passed, 0 failed**.
Final dedicated availability/UI runs also passed **42/42 each** under `TZ=UTC`,
`TZ=Asia/Taipei` and `TZ=America/Los_Angeles`. Editor diagnostics, tracked diff
whitespace, trailing whitespace in all 15 changed files, relative documentation
links and real-target-address/token-pattern scans passed. Final metadata-only
inspection again confirmed exact v3 **Disabled / SAS Disabled**, owner-list and
invitation Disabled, connector Connected and invitation delivery off.

| Safe fixed-week verification evidence | Actual outcome |
| --- | --- |
| Guarded update | `week_updated_disabled`, one PUT, no schedule query |
| Anonymous/invalid-token denial gates | Passed before the authenticated request |
| Mike Lee success / projected slot count | **true / 336** |
| Debby success / projected slot count | **true / 336** |
| Independent Disabled / exact SAS-disabled contract | **true / true** |
| Preserved resources | Checks passed; no configuration writes |
| Verification session cleared / proxy idle | **true / true** |

Only privacy-safe booleans/counts/lifecycle status were reported. ARM redacts secure
parameter values, so **no cloud readback of exact target addresses is claimed**.
The helper bound the submitted targets in memory; successful projected rows are
runtime evidence of matching the configured indices, not an independent directory
identity audit. No raw schedule pattern, email, provider ID, token or callback URL
was emitted. Cloud-side detail projection and protected service history remain.

Independent exact-v3 Disabled/SAS-disabled and preserved-resource checks plus
immediate local idle/cleanup and process-identity guards preceded graceful restart
of **only the old port-8002 owner process**. The current owner task runs the new code.
Existing user tabs were not reloaded or inspected; **reload an old tab** to get the
current code and fresh session. Reload itself makes no availability request.

- The initial **Choose available calendars** section is `hidden` and `inert`.
  Its DOM and shared clear/revocation handlers remain, along with backend capability;
  hidden acknowledgement/load/check handlers cannot initiate a list request.
  Synthetic scenario controls sit outside that hidden section.
- The access summary names both people, default-calendar sources, owner-only
  busy-status disclosure, exact timezone and exclusive window, and all 336 slots
  before explicit checkbox confirmation can enable Load. No startup request.
- Each person has seven date-labelled expandable daily groups of 48 slots, with
  explicit next-date midnight boundaries. First day opens initially. Textual
  statuses, independent unknown/error rows and freshness remain intact. The
  separate fictional activity demo is not integrated with these real results.
- `week` accepts **only exact Disabled repaired v2**, including the old fixed window
  and redacted secure-parameter envelope. It uses the existing exclusive local lock,
  repeated preserved-resource checks, immediate full snapshot re-read, one standard
  PUT and exact v3 readback. It is not Azure atomic CAS. Secure values remain hidden
  on ARM readback; identity-value equality is not inferred from that envelope.
  Historical `repair` still means v1 → v2 only; `deploy` remains absence-only.
- v3 changes only the contract tag, request/response window and exact slot bounds.
  Two protected Select projections, not a per-slot connector or ForEach loop,
  produce 336 slots each. Every fallback produces the same exact length. Target
  bound two, no-pattern syntax gate, caller/SAS protections and no retry remain.
- The old shared callback default was **8 KiB**, too small for the expanded enum
  payload. Only the bound availability callback now permits **24 KiB**, with the
  same bounded byte allowance in the UI. Owner-list/invitation transport is unchanged.
  Worst-case pretty-printed synthetic grids and oversized streams/declared lengths
  are covered offline. Missing array entries also become Unknown, never checked free.

Official documentation rechecked for this milestone:
[getSchedule API](https://learn.microsoft.com/en-us/graph/api/calendar-getschedule?view=graph-rest-1.0)
allows 30-minute slots (5–1440 supported); the
[concept/limits page](https://learn.microsoft.com/en-us/graph/outlook-get-free-busy-schedule)
allows fewer than 62 days and up to 20 entities. This fixed seven-day/two-target
contract stays narrower. Default calendars only, `0` can mean working elsewhere,
and subject/location may be fetched into protected cloud processing. The concept
page's older `Calendars.Read` wording differs from the API's least-privilege
`Calendars.ReadBasic`; no permission change is made here.
[Logic Apps limits](https://learn.microsoft.com/en-us/azure/logic-apps/logic-apps-limits-and-config)
document 100,000 Consumption ForEach items and 8,192-character expressions;
this workflow retains bounded Select actions and does not introduce ForEach.
[Create Or Update](https://learn.microsoft.com/en-us/rest/api/logic/workflows/create-or-update?view=rest-logic-2019-05-01)
documents the standard workflow PUT and SecureObject parameter type.

Synthetic browser review used an isolated current-code server on port 8004, leaving
the existing 8003 service untouched. Actual CSS viewports **1279px and 319px**:
seven headings/person, 672 slots, daily keyboard expansion, acknowledgement/load,
status focus, partial second row with 336 Unknown slots, clear/withdrawal,
slow cancellation and hidden-control Tab exclusion passed. Mobile intrinsic-width
overflow in the relocated fixture selector was found and fixed. Injected expired
session recovery reloads with zero startup API calls. No real result page was read.
Parent usability and screen-reader review remain separate, not established here.

Final browser handoff used only a **new empty live page** on port 8002, never the
owner's previously loaded tab: at **320px / 1280px**, no horizontal overflow,
no grids, acknowledgement off, Load disabled, hidden calendar/scenario controls,
and **zero startup API requests**. The created port-8004 synthetic server was
gracefully stopped; the active review page is the empty live page. The pre-existing
port-8003 service and activity-preview/PKCE work were not changed or stopped.

### Historical failed owner Load: local session expiry, not cleanup failure

The later user click failed. The existing browser's resource timing reports
**HTTP 403 at page age 130 minutes** for the availability request. Only fixed
HTTP status/page-age metadata was read; no real result grid was inspected.
The proxy expires a page session after **30 minutes** and rejects a missing or
expired session before body processing, credential acquisition or Azure work.
The old availability UI discarded the rejection reason and displayed generic
unavailable and unconfirmed-cleanup messages. An expired page also cannot use its
old session to check local cleanup. This is the verified local defect, not a
new provider failure, contract mismatch or `cleanup_failed` diagnosis.

Independent safe inspection found the live proxy **idle**, its last cleanup
**workflow_disabled**, and the exact availability workflow **Disabled / SAS
Disabled** with the current contract accepted. Latest existing run/action metadata
remained Succeeded. Preserved resources were unchanged across the diagnostic
reads. CLI expiry metadata showed at least ten minutes remaining at inspection;
that is not historical token-expiry evidence, and credential acquisition was not
reached by the rejected browser request. No protected payload URL was followed.

The local-only repair:

- Retains HTTP 403 and `status: blocked`, adding only fixed
  `reason: session_unavailable` after the existing Host/Origin checks. No global
  cleanup state is disclosed to an invalid session. Expiry/restart/missing-session
  responses deliberately do not claim to distinguish causes the server no longer
  retains. TTL, CSRF, single-use and authorization checks are not relaxed.
- Shows the local rejection and explicit reload guidance. Credential, contract,
  busy and actual cleanup errors use fixed distinct messages; arbitrary provider
  text is never rendered. Rows remain unknown, and the rejected page cannot load
  again, including after a rejected cleanup check.
- Distinguishes no enable cleanup recorded, pending cleanup, confirmed disable
  and unknown cleanup. A later shared-proxy status cannot hide a sticky UI cleanup
  failure. A fresh page cannot clear the process-wide backend failure block.
- Adds a keyboard-accessible reload link. Reload clears local results and gives
  a new page session; it does **not** confirm Azure cleanup, renew authority,
  retry availability, or erase protected Azure history. Check local cleanup
  explicitly after reload. Any genuine unresolved cleanup still needs operator
  recovery, never a restart merely to discard the failure flag.

The original **205/205** baseline passed. Two new HTTP regressions first failed
because the rejection reason was absent; after the repair, the full suite passed
**212/212**. Five additional UI regressions execute the actual handlers offline,
covering zero startup calls, reload guidance, unknown rows, fixed safe errors and
sticky cleanup. Synthetic browser review at **1280px / 320px** verified keyboard
acknowledgement/load, injected session-rejection display, status focus, reload-link
keyboard activation, horizontal fit, and **zero startup API calls after reload**.
Normal two-row and partial-row fixtures, clear and acknowledgement withdrawal
also passed. Browser rejection was injected only on port 8003; the HTTP tests use
the real server and synthetic clock expiry. No live retry was performed.

Only the exact idle live owner process on port 8002 was gracefully restarted,
after another independent Disabled check and a local idle/confirmed-cleanup
guard. Other servers were preserved. The existing failed tab **needs reload**.
The restarted service starts idle with `not_requested`; that is not a fresh
cloud-state check. Parent review and production readiness remain unestablished.

### Earlier guarded cloud repair and successful live verification (historical)

The exact v1 → v2 repair deployed
as **Disabled / SAS Disabled**, with full prior-contract, caller/authentication,
adjacent snapshot, post-readback and preserved-resource checks. The first actual
authorized mailbox query was then performed once through the local proxy:

| Safe verification evidence | Actual outcome |
| --- | --- |
| Local load attempted | Yes, once |
| Anonymous and invalid-token denial gates | Passed before authenticated workflow invocation; each must return 401 or 403 |
| Checked slot count | 16 per person |
| Person 0 / person 1 complete success | **true / true** |
| Returned status | `checked` |
| Independent workflow Disabled check | true |
| Preserved resources unchanged | true |
| Session cleared / proxy idle | true / true |

Independent metadata-only follow-up confirms `Validate_targets`,
`Validate_target_syntax`, `Get_schedule`, `Validate_response` and `Availability`
all **Succeeded**, with the exact repaired workflow still Disabled and preserved
resources unchanged. No second mailbox query was made for this follow-up.

### Exact cause and repair evidence

The earlier failed workflow execution had `Validate_request` Succeeded,
`Validate_targets` Failed and `Get_schedule` Skipped, so it made **no mailbox
query**. Read-only action error metadata was subsequently processed privately.
Only fixed classifications were emitted: `patternUnsupported: true`,
`patternPropertyNamed: true`, `propertyNotSupported: true`; pattern mismatch,
parameter/type failure and invalid-regex classifications were false. The service
message names the JSON schema **`pattern` property as unsupported**. This is an
action-schema runtime incompatibility, not evidence of malformed actors, lost
SecureObject values or provider permission denial. Unrecognized service codes
remain `OtherOrAbsent`, never arbitrary echoed strings. No protected input/output
link was followed and no service message, address, token or schedule was logged.

The repair retains `Validate_targets` array/type/length/uniqueness checks. A new
protected `Validate_target_syntax` action requires two **true** booleans using
the equivalent lowercase syntax: exactly one at-sign, alphanumeric local start,
the same local/domain character sets, nonempty domain before the final dot and
at least two alphabetic suffix characters. It additionally rejects terminal
newlines that JavaScript's `$` anchor can accept. Unsupported `pattern` is replaced,
not bypassed. `Get_schedule` runs only after both gates succeed; syntax failure
has its own protected all-unknown fallback. SecureObject definition/value shape,
empty trigger body, caller claims, SAS denial, connection route, fixed range,
projection, secure histories and retry policy are unchanged.

The separate `repair` entry point accepts **only the exact Disabled legacy
contract**, not arbitrary updates, absent resources or already-repaired resources.
The original `deploy` entry remains absence-only. One standard supported PUT uses
the shared local operation lock and complete immediate pre-/post-read checks;
no undocumented If-Match/CAS promise. Real targets are supplied privately in memory,
not stored in source, files, argv or output. Secure values cannot be read back, so
secure-value equality is not claimed. The successful identity-bound projected
response is separate runtime evidence. No connector, invitation or owner-workflow
write, permission expansion, automatic retry, commit or push occurred.

Official references checked for this repair: [Workflow Create Or Update and
WorkflowParameter types](https://learn.microsoft.com/en-us/rest/api/logic/workflows/create-or-update?view=rest-logic-2019-05-01),
[workflow parameters and expressions](https://learn.microsoft.com/en-us/azure/logic-apps/workflow-definition-language-schema),
and [Parse JSON action schema](https://learn.microsoft.com/en-us/azure/logic-apps/logic-apps-workflow-actions-triggers#parse-json-action).
The unsupported-keyword evidence is from the actual runtime, not a claim that the
general documentation lists every unsupported schema feature.

## Local experience

Use the existing **FamilyCopilot: local owner calendars** task and open
**http://localhost:8002/** externally. Only this owner service was restarted;
other existing picker/demo services were not touched. Reload an older page.
The availability section uses the presentation-only weekly diagram above,
keeps session-expiry and current local-retention guidance in its details, and starts with two
**Not checked** contexts and fourteen Unknown day tracks without contacting Azure on startup. No live results are
embedded in the page. A prior verified snapshot may remain in RAM/local file under
the current policy above, but is not displayed until acknowledgement and Check
week. Live loaded results must not be shared with an
agent. The verification's authorization has been consumed; any subsequent load
must be a new deliberate owner request, not an automated recovery retry.

The implemented path is review acknowledgement → Check week or explicit Refresh → two
independent tracks per day in one diagram after verified disable. Default calendars only; picker
selection does not select these sources or authorize event-derived access. Source,
fixed range, retrieval timestamp, freshness and missing context are explicit.
`0` means **No busy block reported (may be working elsewhere)**, not guaranteed free
time. Tentative, Busy, Out of office and Unknown are distinct textual states.
Unknown digits, missing/duplicate actors, malformed grids and per-person errors
fail closed independently. A successful first row never marks the second checked.

Clear/cancel and acknowledgement withdrawal clear both shared-session surfaces
and shared RAM/local file and fence late responses. Page exit clears tab state and cancels
its pending work, not a completed snapshot. Shared-proxy cleanup status refers to the last
operation, not a fresh cloud-state inspection. Cleanup uncertainty blocks loading.
Freshness is evaluated locally (five-minute snapshot limit); no refresh is scheduled.
The fictional activity demo remains independent and usable without calendar access.

## Privacy and lifecycle contract

[infra/availability.js](../infra/availability.js) generates the fixed request and
protected response pipeline. Exact actors are a `SecureObject` workflow parameter.
ARM hides secure values on readback, so exact secure-value readback is **not**
claimed. The definition, caller claims, connection reference, protections and
redacted parameter envelope are checked; per-load resource snapshots fence drift.
Create uses documented standard PUT with an adjacent absence read; repair uses
the exact legacy contract and adjacent full snapshot equality check. Both use
cooperative local operation exclusion and exact post-read. This is **not atomic
Azure CAS**; an external-writer race remains possible.

Graph `getSchedule` can fetch subjects, locations and private flags into protected
Azure/connector processing. The cloud pipeline is designed to bind `scheduleId` to
fixed person indices and emit only the window, timestamp, fixed status and 336 enum
slots. Addresses, provider IDs, schedule items, working hours and raw errors never
belong in its response. Offline tests exercise the actual projection expressions
with synthetic malicious/provider-detail fixtures. The successful fixed-week live run verifies
the current bounded connector route and both 336-slot projected grids; raw connector
payloads were not inspected. It is not proof of every possible binary/provider shape.

Request secure outputs, Parse JSON/Compose/Response secure inputs (implicit output
hiding), and Query/Select/connector secure inputs+outputs protect every data path.
No tracked properties or workflow outputs exist; content access remains denied.
Azure retains service-managed protected history. Busy-only disclosure is **not** a
claim that details cannot be fetched, that cloud retention is zero, or that Clear
erases cloud history. Production retention review remains separate.

[scripts/availability.js](../scripts/availability.js) restricts management paths,
method/body, callback and invocation count. It requires the exact current caller,
an adequately fresh existing CLI credential (same ten-minute margin as owner-list),
preserved Connected connector and Disabled owner/invitation resources. It temporarily
enables only availability, requires both denial probes, invokes at most once, then
independently disables/verifies in `finally` before releasing any response. Aborted
requests do not cancel cleanup. Ambiguous enable/cleanup blocks further work.
No token reaches the browser or logs; no interactive credential input is requested.

The [localhost proxy](../scripts/serve-owner.js) keeps exact loopback Host/Origin,
CSRF, body bounds, no-store/CSP, one global flight through cleanup, one calendar-list
load per page, explicit repeat availability Refresh, session revocation, shutdown
fencing and sticky cleanup failure. The bounded local cache is separate from session expiry.
No URL/actor/range may be supplied by a browser. Clear cannot undo cloud processing.

## Repeatable checks and validation actually performed

- [scripts/verify-availability.js](../scripts/verify-availability.js), `--live-once`:
  one explicit live verification, no automatic retry. Uses in-memory responses,
  emits only success booleans, fixed slot count and safe lifecycle state, clears
  the local session and independently checks Disabled before/after. A future agent
  execution needs an explicitly reviewed verification, not a routine test run.
- [scripts/diagnose-availability.js](../scripts/diagnose-availability.js),
  `--metadata-only`: fixed latest-run/action GETs with bound run path; output only
  allowlisted step/status/error-code metadata and fixed error-template classifications.
  No writes, invocation, payload links, provider messages, timestamps, run IDs or
  addresses in reports. Both exact known Disabled contracts can be diagnosed.
- Earlier cloud-repair baseline **197 passed**, including pre-existing activity-preview work;
  that increment finished at **205 passed**. Latest local session-expiry repair:
  full `node --test` **212 passed, 0 failed**. `git diff --check` passed.
  No new dependencies/framework/build.
- [test/availability.test.js](../test/availability.test.js): fixed contract,
  secure history/config, actual WDL expression projection, redaction/injection,
  malformed/missing/partial grids, disclosure/freshness, cancellation, ambiguous
  sends, denial gates, cleanup, no-retry verification, guarded deployment, real
  local HTTP security, metadata diagnostics and token-lifetime regressions. Repair
  regressions cover unsupported-pattern classification, 500+ deterministic syntax
  cases against the former regex, exact legacy/current definition delta, one-way
  PUT, preserved/caller/auth/version drift, ambiguous failures, no retries, and the
  explicit rule that two all-unknown fallback rows are **not success**.
  The small offline WDL evaluator is **not** an Azure runtime or Parse JSON schema
  emulator; offline success alone does not establish runtime target validation.
  The actual successful first mailbox query supplies separate live evidence.
- Synthetic-only browser review at **http://localhost:8003/**, desktop 1280px and
  mobile 320px: keyboard review/load, two 16-slot grids, textual status/freshness,
  independent unavailable row, clear/ack withdrawal, slow cancellation/late fences,
  focus and horizontal fit. A cross-surface clear-focus defect was found and fixed.
  No real loaded browser result was inspected or captured.
- Repair-session browser recheck: synthetic keyboard load, two 16-slot grids,
  status focus, 320px horizontal fit, independent unavailable second row,
  clear and acknowledgement withdrawal passed. A new live **empty** page at
  **http://localhost:8002/** displayed the corrected notice at 1280px and 320px,
  with no grids loaded, Load disabled, no overflow and **zero startup API requests**.
  Only the idle live owner process was restarted; other services were untouched.

Parent usability/screen-reader review, other provider response shapes,
child-account feasibility and production readiness remain open.
All unrelated uncommitted work was preserved. Nothing was committed or pushed.