# Chat-first integration contract v1

Published: 18 September 2026. Contract identifier: `familycopilot.chat.v1`.

## Status, inputs and authorization

**Publication redaction, 22 September 2026:** Parent A, Parent B and Child are
neutral display aliases, including explicit invitation wording and synthetic
scenarios. Age 7, parent indices, ownership, disclosure, dates and consent gates
are unchanged. Legacy identity/protocol bindings remain intact and block complete
name removal. Historical prose is editorially anonymized, not a verbatim approval
transcript; old hashes identify old evidence, not this tree. No private media or
authorization record was changed. See [the publication review](publication-redaction.md).

**Whole-month calendar, invitation preparation and first-typing repair, 21 September 2026:**
the owner approved the product changes and a separate Producer video update,
offline synthetic only, with existing narration and original images. October is
now a five-week whole-month view, with optional day details initially closed.
Parent markers identify Busy periods, not entire unavailable days. Child's four
loaded events retain names/times through the existing private DOM renderer;
redaction, cancelled/unknown/all-day states and source/freshness remain intact.
No fixture, source metadata, data scope, consent or provider contract changed.

Exact invitation consent now enters an abortable 700ms page-local UI preparation
before synchronous Calendar revalidation and pending response. The existing
polite status/spinner says `Preparing Parent A's invitation...`; reduced-motion stops
animation. `coordinateMeeting` remains synchronous. Reset, disposal, changed
context/preferences, failure and timeout fence late results. No real send or IO.
Idle scroll padding is corrected after reproducing a 22px desktop first-focus
scroll; the same typing run now stays at 0px with stable composer height.

The [current handoff](chat-builder-handoff.md) pins **25 assets** at SHA-256
`c626ab54ed20895f53ea6b458e6e6a2fde904fdca0e9f7b37fe2830c939c0a61`,
including the four unchanged original images restored by Producer. The earlier
missing-image statement below is historical. Final snapshot-bound focused checks
passed **228/228**, bounded `npm run test:chat` **214/214**. Disposable offline
1440x900 and 390x844 checks passed with zero unexpected requests, forbidden IO or
page errors; browser and test hashes identify the same snapshot. No shared service,
private data, provider, install, TTS, media edits, extra agents, commit or push.
Producer's authorized re-record/edit is still separate work; old footage does not
verify this revision. Parent and provider review remain separate gates.

**Equal-interest preferences integrated, 21 September 2026:** the owner reviewed
the static draft SHA-256 `c73b3096a59362228590b22378fc7c1d0d85aafe15c1535926bff91171debe74`
and explicitly approved applying it to the actual chat, including isolated
network-blocked desktop/mobile product checks. Prominent family initials/names
and parent/child roles, seven equally weighted default interests, state-derived
team typography, secondary location and the functioning composer are implemented.
All original preference controls and edit/save/cancel/custom/reset/update/focus
behavior remain; no readonly draft controls or automatic collapse were introduced.
Unknown interests use a neutral icon, custom teams remain literal text. No domain,
Calendar, fixture, consent, source, date, network or persistence change.

The [current handoff](chat-builder-handoff.md) pins 21 read-only assets at SHA-256
`a74147a68b7db1cba4725c29ba30c69859e4823582a22626775e5fdec39df8f7`.
Four accepted missing third-party images are intentionally not packaged or replaced;
existing fallback was tested. Baseline 38/38; focused final 39/39; bounded chat
and final snapshot-bound checks **211/211**. In-memory browser validation passed
1440x900 and 390x844, source hashes matched before/after, final screenshots inspected.
No shared-service interaction, listener, private/live data, recording, media edit,
TTS, installs, other agents or commit/push. This supersedes earlier statements that
preference integration or these product browser checks were unapproved. Parent
usability, provider verification and any media work remain separate gates.

**Natural source labels without repetitive Demo wording, 21 September 2026:**
the latest owner decision supersedes the standalone disclosure and DEMO badge
requirements below. Calendar now retains `Sample calendars` in its header.
Chat's existing closed `Details` contains saved-public provenance, exact sample
date bounds, fixed submission-order dialogue, no live search/AI and material
uncertainties, without invitation instructions. Calendar's existing closed
`Details` contains `#calendar-invitation-source`: `Invitations use sample data
and stay on this page. No invitation is delivered and no calendars are changed.`
That static qualification survives cancellation, stale context and return to
activities; it is not conditional on a pending invitation. It remains accessible
when the Calendar body is collapsed. Reset clears conversation history, not the
source facts. Disposal retains the existing hide/fail-closed behavior.

`#meeting-disclosure` is removed, not renamed to Invitation preview. Progress says
`Preparing Parent A's invitation...`; Calendar says `Parent A pending response` and its
result is awaiting his response, without claiming sent/delivered/accepted.
The timeline note retains `Current user: Parent B. Parent A's response and travel are
not confirmed.` Real names are no longer described as fictional identities;
the schedules remain explicitly sample data, not real authorization.
Exact explicit consent, Child ownership, Parent B's 30-minute overlap, busy-only
parents, expiry/revalidation, unknown availability and saved-source/showtime
qualifications are unchanged. No fixtures, preferences or domain algorithms
were changed; no real delivery, provider access or calendar writes were added.

The [current handoff](chat-builder-handoff.md) pins 25 read-only assets at SHA-256
`e7aaece55caf8a85555a8fdeff5383db45fa122af0252cf1ce7ce748e55fb33b`.
Six copy/presentation assets differ from `d3c76cf2...`; the other 19 are identical.
Baseline: 134/134. Final focused: Calendar 64/64, conversation/integration 70/70,
then integration 38/38 including one new capture-flow regression. Final pinned
seven-suite check: **210/210**, zero failures/skips/cancellations, source/test
hashes matched before and after. Scenario schema, browser-runner syntax and
editor diagnostics passed. Offline DOM checks are not visual/browser evidence.
Producer's read-only 11-scene scenario needs assertions changed in scenes 7, 9
and 11, plus limitation 4. Exact replacements and ownership are in the handoff.
No Producer scripts/docs/media, fixtures, service/browser/shared tabs, network,
private data, TTS, installs, agents, commit or push were touched. New visual review,
rehearsal/capture and parent usability remain separate gates, not authorized here.

**Member calendars first (B), 21 September 2026:** the latest owner decision
supersedes earlier candidate-first presentation and the long simulation sentence.
This is a small presentation-only change: `Family calendars` opens the existing
month overview and member day tracks before the shared candidate-times summary.
It does not add three independent full-calendar screens or new data access.
Parent A and Parent B remain busy-only; Child retains permitted sample event names/times.
Candidate windows remain conditional on loaded, fresh, complete context, not
guaranteed availability. Data, algorithms, consent and source coverage are unchanged.

Progress now says `Loading family members' calendars...`, then
`Finding activities that match your family's interests...`; upcoming actions use
matching wording. The underlying `search_saved_activities` operation still reads
saved public facts, with provenance visible elsewhere. No provider search,
production delay, new parser or fixture change was introduced.

`#meeting-disclosure` now says only `Demo invitation`, separately above the
composer alongside the invitation exchange, including Parent A's pending response.
It retains its visibility through No, Undo, stale context and return to activities,
and clears on reset/disposal. Calendar DEMO context and its existing no-delivery
details remain. Exact explicit consent, Child ownership (October 16, 2026,
15:30-16:30 Taipei), Parent B's 30-minute overlap and fail-closed checks are unchanged.
No real invitation has been implemented or authorized. Producer must retain
visible simulation context, not crop it away.

The [current handoff](chat-builder-handoff.md) pins 25 static assets at SHA-256
`d3c76cf2d2a43487a30c6c6a8a5ceae896ea8baaffb6a7c724b056dc0b5cad5d`.
Only three UI assets differ from `4275e114...`; 22 assets are unchanged.
Executed: Calendar 64/64, conversation/integration 70/70, then **209/209**
snapshot-bound tests with product/test hashes matched before and after.
Scenario schema, browser-runner syntax and editor diagnostics passed.
No browser, services, private/live data, network, recording, TTS, install,
other agents, commit or push. Visual review and Producer rehearsal/capture need
separate approval; the five-scene Builder scenario is not a rehearsed B video.

**School-event ownership correction, 21 September 2026:** the owner clarified
that the school meeting belongs to a shared or Child calendar, not Parent B's. The
implemented sample uses Child's calendar as its single source: October 16, 2026,
15:30-16:30 Asia/Taipei. It appears in Child's month marker and day details;
the comparison reads that event and fails closed when child context is missing.
`Child / School meeting` remains the timeline owner before/after a proposal or
invitation. Parent B and Parent A retain separate unchanged busy-only rows, with Parent B's
30-minute overlap preserved. Inviting Parent A does not transfer event ownership or
add a parent calendar event. Natural dialogue, explicit consent, pending response
and the independent simulation disclosure below are unchanged.

The [current Builder handoff](chat-builder-handoff.md) pins 25 assets at SHA-256
`4275e114a1b9c32e772e9d3a6bef5bf5d9cd8274b168a78ea99254c46d205056`.
Executed: 101/101 final Calendar/integration tests and **209/209** snapshot-bound
tests, with source/test hashes matched before and after. Scenario schema,
browser-runner syntax and editor diagnostics passed. No new browser/visual
review, media, private data, provider queries, service operations or calendar
writes; previous browser evidence does not verify this revision.

**Natural invitation dialogue and separate simulation status, 21 September 2026:**
the owner approved removing technical demo/no-real-send wording from invitation
messages. The natural question retains Parent A, Child's school meeting, Friday,
October 16, 2026, 3:30-4:30 PM (Asia/Taipei), the one-parent attendance condition
and the existing exact explicit-consent replies. The result says Parent A's invitation
is awaiting his response, never accepted or attendance confirmed. Cancellation,
clarification and stale messages also omit technical simulation disclosures.
The underlying page-local operation, Calendar revalidation, deduplication and
fail-closed consent rules are unchanged; bare Yes still cannot send.

`#meeting-disclosure`, a separate polite status above the composer, says
`Simulation only. No real invitation has been sent and no calendars have changed.`
It remains visible while meeting state or meeting messages remain, including
No/Undo/expired context/return to activities, and hides after reset/disposal clears
the conversation. Calendar's existing synthetic/pending status remains intact.
Producer must keep this separate disclosure in frame, not crop away simulation
facts or reinsert them into dialogue. No real delivery or calendar write exists.

The historical natural-invitation section of the [Builder handoff](chat-builder-handoff.md) pins a 25-asset read-only
snapshot with SHA-256
`d4edafb7e596be589986d6d0c44c4a962645e90f98c00343c16eec3bdc3e5fb1`.
Executed: baseline 69/69 and final 70/70 conversation/integration checks;
**208/208** snapshot-bound seven-suite tests, no failures/skips/cancellations,
with product/test hashes matched before and after. Browser-runner syntax and
updated five-scene schema passed; no browser, rehearsal, recording, services,
real data, provider, TTS, delegation, commit or push. Prior browser/media evidence
does not verify this disclosure layout. Exact owned paths, tests, immutable
manifest and remaining Producer framing/approval gates are in the handoff.

**Action-specific processing without Stop, 21 September 2026:** the owner requested
removing Stop and showing the other background actions instead of a generic
activity-search label. The composer no longer contains a Stop button, listener or
focus target. It shows the current action and a compact list of upcoming actions
from the actual orchestrator: load family calendars, search saved activities,
compare activity times, check both parents for the school meeting, or create Parent A's
page-local demo invitation. Only actions belonging to that request appear; upcoming
steps are labelled `Next`, never presented as running or completed. Settlement,
failure, reset and disposal clear the queue. Calendar failure still permits
independent activities without asserting that Calendar loaded successfully.

Status remains a polite live region. Keyboard submission focuses that region
while busy and returns to the composer on completion. Existing request timeouts,
Retry, New conversation cancellation, generation fencing and the core pause API
remain intact. Synchronous meeting checks and demo-invitation creation publish
their own state but may complete before a browser paint; no artificial latency,
real invitation, provider query or calendar write was introduced.

Owned paths: `chat/index.html`, `chat/chat.css`, `chat/ui.js`,
`chat/conversation-core.js`, `test/chat-conversation.test.js`,
`test/chat-integration.test.js`, `scripts/test-chat-browser.js`,
`docs/designer/README.md` and this record. Baseline: 67/67 conversation/integration
tests. Final: **207/207** across the seven explicit suites in
`node scripts/test-workflow.js chat`, zero failures/skips/cancellations.
The separately approved isolated 1440x900 `--calendar-review` browser check passed
with zero forbidden I/O. Calendar/search screenshots were visually inspected;
current and upcoming actions fit above the composer, and Stop is absent.
`browser-artifacts/chat-preferences-e1jyNx/report.json` records source and runner
hashes for that check. No shared service, real data, mobile check, recording or
existing media was touched. The earlier pinned handoff and video retain their
original assets and are not evidence for this revision. Browser approval is
consumed; human usability review and any later recording remain separate.

**Neutral composer and earlier suggestions, 21 September 2026:** the approved
offline copy correction replaces invalidated activity history's `Previous search
retired.` with `Earlier suggestions`. Stale result/card removal, generation fencing
and abort behavior are unchanged. Initial HTML and every runtime composer state
now use `Message Family Copilot...`, including reset, processing, meeting replies,
cancellation and completion. Inputs remain empty on initial load/reset; actual
typed October/weekend/school/invitation queries remain supported. No prefill,
automatic submission, NLP expansion, date/data change or other-field copy change.
Explicit demo-invitation consent, pending Parent A, source/coverage and uncertainty
labels remain intact. This supersedes earlier composer-copy descriptions only.

Baseline and post-edit conversation/integration checks passed 67/67. Final
snapshot-bound seven-suite verification passed **205/205**, zero failures/skips/
cancellations, with product/test hashes matched before and after. The
[current Builder handoff](chat-builder-handoff.md) pins the fresh 25-asset snapshot,
exact old/new hashes, owned paths and evidence. Only three copy-bearing assets
differ from the previous `257e1b1f...` handoff; 22 assets and the Builder scenario
are unchanged. The existing nine-scene media snapshot was verified read-only to
share those prior product hashes. No Producer file, browser, service, live/private
data, network, recording, TTS, install, delegation, commit or push was touched.
The main agent retains the requested recording step after this offline handoff;
old browser/media evidence does not verify the new product snapshot.

**Explicit demo invitation and product copy, 20 September 2026:** the owner's
new approval supersedes the three-turn proposal workflow below. On
`Can you check my schedule for the school meeting?`, Calendar checks Parent B's
conflict and Parent A's schedule together with the existing checked, fresh, complete
coverage and revision guards. The reply is:

> Your work meeting overlaps by half an hour. Parent A's calendar looks clear then.
> If only one parent needs to attend, shall I invite him? Demo invitation to Parent A:
> Child's school meeting, Friday, October 16, 2026, 3:30-4:30 PM (Asia/Taipei).

Only `Yes, one parent is enough. Please send Parent A an invitation.` or
`Yes, please send Parent A an invitation.` in that established context creates the
page-local synthetic invitation. The shorter reply accepts the preceding
one-parent condition. Bare Yes, old proposal replies and ambiguous or conditional
messages do not send. This is bounded exact dialogue, not general NLP.

Calendar revalidates both parents on send. One stable page-local event/recipient
ID prevents duplicate records; Parent A remains pending, never accepted or assumed
to attend. The result visibly says `Demo invitation sent to Parent A for Child's school
meeting, Friday, October 16, 2026, 3:30-4:30 PM (Asia/Taipei). Waiting for his
response. No real invitation or calendar change.` The supporting timeline retains
Parent B's overlap, since an invitation does not resolve attendance. No contact,
provider call, calendar write, reminder, storage or real delivery was added.

No/Cancel/Undo, pause, reset, preferences, changed revision and expired or
incomplete calendar context retire the demo state. Cancellation says the demo
invitation is cleared, not that a real sent invitation was withdrawn. Meeting
replies do not search activities, move their dates or consume their request budget.
Continue activities explicitly closes the school demo without searching.

Copy changes are on the **main chat and activity surfaces**, not standalone
availability helpers: `Here are some ideas for your family.` and `Explore this
movie for the weekend.` replace repetitive generic warnings. Movie cards show
`Showtimes unverified` and the navigation link `Choose a showtime`. The visible
Calendar summary says `Calendar not checked for this weekend` outside coverage,
or `Calendar fit not confirmed` for unknown timing. Exact scope, freshness and
per-item reasons stay under Calendar check details; evidence and source detail
surfaces retain their facts. `Saved public sources / Incomplete shortlist /
Availability unverified` remains visible. No coverage, event dates or verification
facts were widened or fabricated.

Validation: final pinned handoff **203/203 passed**, zero failed/skipped/cancelled,
with source/test hashes matched before and after execution. Baseline 121/121 Calendar/conversation/integration tests;
focused invitation 1 passed (56 intentionally skipped); conversation 30/30;
copy/conversation/integration 75/75 after updating two obsolete copy assertions;
Calendar/integration 98/98; final scenario integration 35/35. Browser runner syntax
and five-scene schema passed, **no browser execution**. The updated
[Builder handoff](chat-builder-handoff.md) records the final frozen snapshot,
source/test hashes and full bounded unit evidence. Old browser/media evidence is
not evidence for this revision.

Only approved product paths and Builder handoff files were edited. Producer
files, production scripts/tests/media, standalone availability/auth/live code
and the shared localhost:34393 page were untouched. No service operation,
browser, capture, TTS, install, delegation, commit or push was performed or
authorized for follow-up. Parent review and separately approved desktop/Producer
rehearsal remain pending.

**Parent B-initiated school meeting, 20 September 2026:** newer explicit approval
supersedes the Parent A-conflict/Parent B-proposal direction and activity-first requirement
in historical entries below. The exact opening
`Can you check my schedule for the school meeting?` now loads only synthetic
Calendar fixtures in a fresh conversation. Stable mappings remain Parent A = person 0,
Parent B = person 1; Parent B is the current user. Her October 16 15:00-16:00 busy interval
overlaps the 15:30-16:30 school meeting by a computed 30 minutes. Parent A's afternoon
busy interval is 17:00-18:00, with no overlap. Other dates and live mappings are unchanged.

The reply is `Your work meeting overlaps with the school meeting by 30 minutes.
Does only one parent need to attend?` The bounded response
`Yes. Could Parent A go instead?` establishes one-parent attendance and asks Calendar
to assess Parent A; it does **not** create a proposal. With complete current coverage,
the next reply is `Parent A has no conflicting events in the loaded calendar. Shall I
propose that he attend?` Only a subsequent explicit `Yes` creates a proposal pending
Parent A's confirmation. His willingness and travel remain unknown; no contact,
notification or calendar write exists. This is fixed dialogue, not general NLP.

Calendar checks freshness, coverage, person status, alternate conflict and revision
at both consent boundaries. Undo/Cancel/No, reset, pause, preferences and calendar
context changes withdraw consent. Expiry notifies chat even without an activity
assessment. Missing/partial/revoked/stale/revised context cannot authorize a proposal.
Unknown replies clarify, not consent. Late or failed preparation never starts a
search. Meeting replies preserve activity dates, scene position and request budget;
`Continue activities` closes the meeting without searching, and the following
outing input starts or resumes the existing two-scene activity flow.

Owned paths: `chat/conversation-core.js`, `chat/ui.js`,
`owner/chat-calendar-fixtures.js`, `owner/chat-calendar.js`,
`owner/chat-calendar-template.js`, the three `test/chat-{calendar,conversation,integration}.test.js`
suites, `docs/designer/README.md`, `docs/chat-handoff-scenario.json`,
`docs/chat-builder-handoff.md` and this record. Baseline: 112/112 across the three
bounded suites. Snapshot identity and final hash-bound unit results are in the
[new Builder handoff](chat-builder-handoff.md). This revision is **unit-only**:
no browser, service, network, private data, recording, TTS or Producer edits.
Old browser evidence and the old `37f671...` snapshot do not verify these changes.
Browser scenario selectors, visual framing, rehearsal, capture, parent usability
and provider validation remain pending separate approval.

**Conversational school-meeting review, 20 September 2026:** the owner rejected
the checkbox/proposal-button interaction and approved using the existing composer.
After the first activity scene loads, Review a school meeting (or the message
`Review school meeting`) asks whether only one parent needs to attend. Explicit
`Yes` permits a proposal only; `No` declines, and `Cancel` or `Undo` withdraws.
Unrecognized replies clarify rather than granting consent. `Continue activities`
closes this conversation without searching; a subsequent outing message advances
the existing activity scene. This is bounded scripted dialogue, not general AI.

Calendar retains the busy-only timeline and checks current coverage, freshness and
revision again before proposing Parent B. Missing, partial, revoked or changed context
cannot authorize a proposal; Calendar changes retire earlier meeting messages.
Willingness, travel and Parent B's confirmation remain unknown. Nobody is contacted
and no calendar is written. Only bounded state and overlap minutes reach chat;
meeting replies never reach activity search or change its dates or request budget.
Bounded recent history keeps cancellation available during longer conversations.

Owned paths: `owner/chat-calendar.js`, `owner/chat-calendar-template.js`,
`chat/conversation-core.js`, `chat/ui.js`, `chat/index.html`,
`test/chat-calendar.test.js`, `test/chat-conversation.test.js`,
`test/chat-integration.test.js`, `scripts/test-chat-browser.js`,
`docs/designer/README.md` and this record.
Executed: **112/112** tests across those three bounded suites. Separately approved
isolated `test-chat-browser.js --calendar-review` passed at 1440x900 with keyboard
Yes/Undo/Continue activities, focus, processing, timeline, reset and clear checks,
and zero forbidden I/O. Question/reply screenshots in
`browser-artifacts/chat-preferences-l8F6W9/` were visually inspected; text is visible
above the fixed composer. No server, recording, mobile review, provider operation
or private-data access was performed. Parent usability approval remains separate.
The prior media and pinned Builder snapshot are unchanged and do not include this
revision; no recording or other operational approval remains from this review.

**Explicit custom-team editing, 20 September 2026:** the owner reported that the
two preset checkboxes and comma-separated field did not look configurable.
The editor now has a labelled Team name input, Add team action and individually
removable custom-team items. Enter adds to the draft without submitting preferences;
IME composition is excluded. Existing preset checkboxes remain selectable.
Apply commits the draft, including a typed but not yet added name; Cancel/Escape
discards it. Reopening exposes saved custom teams with named Remove controls.
Case-insensitive deduplication, 16-team/120-character limits, literal text rendering,
page-local state, request fencing and no automatic search remain unchanged.

Owned paths: `chat/index.html`, `chat/chat.css`, `chat/ui.js`,
`test/chat-integration.test.js` and this record. Executed: 58/58 bounded conversation
and integration tests, including add/remove, Enter, Cancel, validation, empty teams,
reset and request isolation. No new browser visual review was run in this increment.
No media, frozen snapshot, provider, shared service or Producer file was changed.
The earlier pinned handoff remains valid for its original assets; it does not
include these new controls and must not be presented as evidence for this revision.

**Meeting and processing-state handoff, 20 September 2026:** Builder completed
the existing school-meeting flow's joint offline acceptance with visible processing
feedback beside the fixed composer. Stop remains reachable while preparing or
searching; live status, busy state and reduced-motion handling do not add artificial
delay. Existing one-parent gating, pending Parent B confirmation, Undo, reset and
fail-closed Calendar behavior remain unchanged. No contact or calendar write exists.

The [fixed Builder handoff](chat-builder-handoff.md) pins 25 static assets and the
full product scenario, with **184/184** bounded tests and two passing 1440px
snapshot-backed browser workflows, both with zero forbidden I/O. It includes source
and test hashes, screenshots, exact owned files and unresolved limits. No shared
service was started/restarted, live query made, media file changed or recording
authorized. Producer must consume the pinned snapshot, not later working assets;
parent review, media rehearsal/capture and public image rights remain separate gates.

**Compact cards and visitor guidance, 20 September 2026:** the owner approved
researching useful admission/price/transport information and reducing repeated
Unknown rows. Public cards now show readable Taipei dates, a concise venue,
one preference reason, admission guidance, price-check navigation and a venue map.
The detailed unknown table, other rationale, retrieval timestamps and evidence
remain under closed details. Coverage-only issues move into Saved source scope,
with a visible incomplete-shortlist warning; actionable source failures stay visible.
Unverified film showtimes, game end time and calendar compatibility remain explicit.

Verified public references (checked 20 September 2026):

- [Forgotten Island](https://www.vscinemas.com.tw/vsweb/film/detail.aspx?id=8956)
  and [Chiikawa](https://www.vscinemas.com.tw/vsweb/film/detail.aspx?id=8786)
  list Protected classification and runtimes of 109 and 100 minutes respectively.
- [Vieshow booking rules](https://www.vscinemas.com.tw/vsTicketing/ticketing/ticket.aspx)
  prohibit under-6 admission to Protected films; ages 6-11 require adult
  accompaniment. Children ages 2-11 use concession tickets. The NT$20 online fee
  is labelled as a fee per ticket, never as the admission price.
- [DEA standard ticket contract](https://tix.ctbcsports.com/DEA/UTK0107_)
  requires tickets at 110 cm and above; below 110 cm, one child without a seat
  may accompany each ticket holder. The card labels this as general club policy,
  subject to game-specific exceptions, not a confirmed rule for this preseason game.
- [Club website](https://ctbcdea.com.tw/) links to the
  [official ticket portal](https://tix.ctbcsports.com/DEA/UTK0101_).
  No game-specific price or seat stock was established from the retrieved pages.

Bounded visitor guidance lives in the existing public snapshot module and matches
source/event/category/URL exactly. It is display guidance, not a change to the v1
numeric eligibility, cost, setting or route fields, nor a fresh event inventory.
Original source retrieval timestamps remain unchanged; visitor references carry
their separate check date. No child's height, exact origin or private event was
collected. Map links contain public venue names only; they do not assert nearest
MRT, travel time or distance. Attempts to retrieve cinema venue/FAQ information
returned ERROR or HTTP 403, so numeric ticket prices and detailed transit data
remain unverified. Official links are navigation only, with no booking action.

Owned paths: `activity-preview/activity-cards.js`, `activity-preview/activity-cards.css`,
`activity-preview/chat-public-snapshot.js`, `test/activity-cards.test.js`,
`scripts/test-chat-browser.js` and this record. Executed: 65/65 card, activity-search
and chat-integration tests. Separately approved 1440px offline browser review passed
with unchanged Calendar and zero forbidden I/O; screenshots in
`browser-artifacts/chat-preferences-m1pyoW/` were inspected. The subsequent wording
correction from end time "not published" to "not verified" is covered by the final
unit run, not those screenshots. No server restart, media/scenario/snapshot change,
provider operation or mobile review occurred. Parent review and the unavailable
prices/transit details remain open; no availability or suitability guarantee is made.

**Two-team matchup preview, 20 September 2026:** the owner requested a basketball
media layout resembling the supplied official-page screenshot. Game 27539 now
shows Dreamers on the left (AWAY), DEA on the right (HOME), both Chinese names,
GAME 5 and the occurrence's date/time. The official game page confirmed home/away
and game number; this check does not refresh schedule retrieval timestamps or
establish ticket availability. A fixed local Dreamers WebP joins the existing DEA
logo, with source attribution and the same unresolved-public-rights disclosure.
Each logo has its own text fallback. No calendar-write button was added.

Owned paths: `activity-preview/chat-assets.js`, `activity-preview/activity-cards.js`,
`activity-preview/activity-cards.css`, `activity-preview/chat-assets/formosa-dreamers.webp`,
the asset notice, `scripts/serve-chat-preview.js`, `scripts/test-chat-browser.js`,
`test/activity-cards.test.js`, `test/chat-integration.test.js`,
`test/chat-preview-server.test.js` and this record. Executed: 43/43 bounded tests
across those three test files, plus isolated 1440px desktop review with no forbidden
I/O and unchanged Calendar. Final screenshots in
`browser-artifacts/chat-preferences-aYq3zi/` were inspected; both logos and Chinese
names render, and 17:00 stays on one line. The browser reused the existing isolated
Noto Sans TC configuration, with no download or system-font change.

The owner separately approved one 34393 preview restart and desktop validation.
The verified old static-preview PID 144741 was stopped; the replacement serves
both logos and updated CSS/JS with HTTP 200 and bytes matching the tested files.
That restart approval is consumed. Film cards, real Calendar services, snapshots
and videos were untouched; mobile and public-release rights remain unverified.

**Preview image delivery repair, 20 September 2026:** the owner reported blank
media and explicitly approved updating/restarting only the isolated port 34393
preview and offline desktop browser validation. All three image GETs reproduced
404: the old one-shot server allowed only synthetic SVG images. Builder added
`scripts/serve-chat-preview.js` with explicit raster paths, PNG/JPEG MIME types,
loopback binding and GET/HEAD-only serving. API/private paths and queries remain
rejected. The verified old preview PID 129816 was stopped and replaced on the
same port; all three images now return 200 with bytes identical to disk.

Browser review also exposed intrinsic image sizing clipping inside the 16:9
media region. `activity-preview/activity-cards.css` now positions images inside
that fixed region with `object-fit: contain`. The updated desktop runner checks
image decoding and geometry. Executed: 12/12 card/preview-handler tests and the
1440px isolated desktop workflow, with unchanged Calendar and zero forbidden I/O.
Final screenshots in `browser-artifacts/chat-preferences-1sHTlV/` were inspected:
the team logo and both posters are complete. The isolated Linux screenshot font
still lacks CJK glyphs; this is not verification of Chinese font rendering.

Owned paths: the preview script, `test/chat-preview-server.test.js`, activity card
CSS, `scripts/test-chat-browser.js`, the asset notice and this record. Existing
calendar services, media helpers, immutable snapshots and videos were untouched.
The preview restart approval has been consumed. No public-release rights were
granted, and no mobile or provider validation was performed.

**Internal demo images and Chinese matchup, 20 September 2026:** the owner
approved integrating references for the three existing cards, not CTBC Brothers.
The fixed local presentation registry now maps game 27539 to the owner-supplied
CTBC DEA logo, and Vieshow films 8956/8786 to their Forgotten Island/Chiikawa
posters. Matching requires the public source, event, category and activity URL.
Activity thumbnails remain null and the v1 contract is unchanged. Card details
link image sources and disclose unconfirmed public reuse permission; this is not
a free-license claim. Source URLs, inspected file hashes and Producer handoff
requirements are in the [asset notice](../activity-preview/chat-assets/README.md).

The matchup now reads 新北中信特攻 vs 福爾摩沙夢想家
(New Taipei CTBC DEA vs Formosa Dreamers). Both language aliases remain matchable.
If the team logo fails to load, its media area shows 新北中信特攻 / TPBL
(New Taipei CTBC DEA / Taiwan Professional Basketball League); the full matchup
title remains visible. TPBL supplements, rather than replaces, team identity.

Owned paths: `activity-preview/chat-assets.js`, `activity-preview/activity-cards.js`,
`activity-preview/chat-public-snapshot.js`, the three raster files and their notice
under `activity-preview/chat-assets/`, `test/activity-cards.test.js`,
`test/chat-activity-search.test.js`, `test/chat-integration.test.js`,
`scripts/test-chat-browser.js` and this record. Executed: 98/98 offline tests with
`node --test test/activity-cards.test.js test/chat-activity-search.test.js test/chat-integration.test.js test/chat-contract.test.js`.
Image signatures/hashes, both scenes, bilingual matching, source mismatch and
load-error fallback are covered. Browser runner syntax and editor diagnostics pass.
All three downloaded images were visually inspected, not browser-rendered.
No browser rerun, snapshot export, recording, service restart, private calendar
access or runtime external retrieval occurred. The existing preview's image
allowlist is unverified and may require separately approved activation. Browser
review, Producer packaging/capture and public-use rights remain separate gates.

**Consolidated recommendation message, 20 September 2026:** the owner approved
combining repetitive uncertainty copy, with no layout changes. When every calendar
assessment is unknown, the assistant now says: "Here are some activity ideas;
timing or calendar details still need checking before I can confirm they fit your
family's schedule." Usable public snapshots with coverage-only gaps hide the
redundant standalone incomplete-search status using the existing Retry suppression
condition. Source failures and other actionable states remain visible; card-level
unknowns, provenance, coverage and calendar assessment details are unchanged.

Owned paths: `chat/conversation-core.js`, `chat/ui.js`,
`test/chat-integration.test.js` and this record. Executed: 56/56 focused conversation
and integration tests, covering both snapshot scenes, the consolidated response and
partial source-failure visibility. No browser rerun, video update, service restart,
external query or calendar operation was performed.

**Demo foreground copy, 20 September 2026:** the owner requested removal of repeated
Sample/Fictional wording from the video-facing interface, specifically the school
meeting title. The chat now shows `Parent-teacher meeting / Fri, Oct 16 /
15:30-16:30 Taipei`, plain calendar-person names and event titles, and natural
loading, coverage and assessment copy. A visible DEMO marker remains on the calendar
heading; synthetic provenance stays in the existing closed source/Demo details.
Missing coverage, unknown availability, travel exclusions and the proposal-only/no
contact/no calendar-change statements remain. These are still synthetic schedules,
not newly verified real events.

Builder owns this bounded copy change in `owner/chat-calendar-template.js`,
`owner/chat-calendar-fixtures.js`, the chat-specific presentation branches in
`owner/availability-ui.js`, `chat/ui.js`, `chat/conversation-core.js`,
`test/chat-calendar.test.js` and this record. Existing unrelated controller changes
were preserved. No media helper, narration, immutable snapshot or existing video
was changed. Producer must use a newly reviewed snapshot for any subsequent capture;
this change does not authorize capture, paid synthesis or a service restart.

Executed verification: 197/197 bounded calendar, chat integration, conversation and
shared availability UI tests. A visible-DOM regression checks loading and expanded
day/meeting views for repeated prefixes, the retained DEMO marker, detailed source
disclosure and no-write warning. No new browser/video capture or frame validation
was run, so this is product-copy evidence, not a verified updated video deliverable.

**Retry visibility correction, 20 September 2026:** usable partial public snapshots
with only `scope_incomplete` or `candidate_limit` issues no longer show an enabled
Retry action. Existing cards, unknowns and partial-coverage disclosures remain.
Source failures, timeouts, calendar rechecks and the Show suggestions action after
preference changes retain their existing behavior. Empty or non-public partial
results are not covered by this suppression.

Scoped paths: `chat/ui.js`, `test/chat-integration.test.js`,
`scripts/test-chat-browser.js` and this record. Executed verification: 56/56 UI and
conversation tests, including both snapshot scenes, partial source failure and
preference-update actions; browser runner syntax checked. Desktop assertions were
added but the separately authorized browser workflow was not rerun in this increment.
No service restart, runtime external query, storage or Calendar change occurred.

**Full team names and editable preferences, 20 September 2026:** the latest owner
request supersedes the abbreviated basketball display below. The official
[team introduction](https://ctbcdea.com.tw/about) identifies 新北中信特攻
(New Taipei CTBC DEA); the [baseball club site](https://www.brothers.tw/) identifies
中信兄弟 (CTBC Brothers). Defaults, basketball title and matching now use the full
basketball name, with the former short Chinese and English names retained as aliases.
Only these public name references were checked; no new event inventory was imported.

Preferred teams now use the existing Interests chip presentation and a labelled plus
button. The same page-local editor offers sport-labelled checkboxes, additional
comma-separated team names, Apply, Cancel and Escape. Users may remove all teams;
the empty state is No preference. Team names are trimmed, deduplicated and validated
against the existing 16-name/120-character contract. Custom names are preferences,
not claims that a team or its schedule has been independently verified. Apply retires
old suggestions and fences in-flight work without automatically searching; the next
explicit request uses the new preferences. Cancel preserves current choices, focus
returns to the opening control, and New conversation restores the demo defaults.

Builder's scoped changes are `chat/index.html`, `chat/chat.css`, `chat/ui.js`,
`chat/conversation-core.js`, `activity-preview/chat-public-snapshot.js`,
`test/fixtures/chat-context.js`, `test/chat-integration.test.js`,
`test/chat-activity-search.test.js`, `test/activity-cards.test.js`,
`scripts/test-chat-browser.js` and this record. No new storage, runtime source calls,
baseball adapter, calendar changes or service restart was introduced.

Executed verification: 87/87 focused conversation, search, cards and UI tests plus
33/33 contract tests. The full isolated 1440px browser workflow passed keyboard
opening/cancellation, team updates, changed recommendation reasons, unchanged
Calendar and zero forbidden I/O. Screenshots in
`browser-artifacts/chat-preferences-Za4jyQ/` were reviewed for chip/editor layout.
The pre-existing missing CJK-font limitation still affects Linux screenshots;
Chinese glyph rendering and parent usability approval are not claimed.

**Chinese team names, 20 September 2026:** the owner requested basketball preference
中信特攻 (CTBC DEA) and baseball preference 中信兄弟 (CTBC Brothers). Builder updated
the existing page-local defaults, family summary, basketball card title and matching
alias in the previously owned integration files. English CTBC DEA aliases still match;
the baseball preference does not match the basketball candidate. No baseball discovery,
new source query, persistence or service operation was added.

Executed verification: 85/85 focused search, cards, conversation and integration tests;
the isolated 1440px desktop workflow passed with zero forbidden I/O. Evidence is in
`browser-artifacts/chat-preferences-FHireC/`. Text assertions confirm both Chinese names,
but screenshot review found missing CJK glyphs in the Linux test environment, which
has no Chinese font available. Chinese glyph rendering is therefore not visually
verified; no font download or installation was performed for this change.

**Public snapshot integration completed offline, 20 September 2026:** the
owner selected verified public snapshots instead of runtime network requests,
Vieshow Taipei Hsin Yi as the cinema scope, and CTBC DEA as the demo preference.
Builder is the sole integration writer for this increment: this document;
`shared/chat-contract.js`; `chat/conversation-core.js`, `chat/ui.js`,
`chat/index.html`; `activity-preview/chat-search.js`, `activity-cards.js`,
new `activity-preview/chat-public-snapshot.js`; `test/chat-contract.test.js`,
`test/chat-conversation.test.js`, `test/chat-integration.test.js`,
`test/chat-activity-search.test.js`, `test/activity-cards.test.js`,
`test/fixtures/chat-context.js`; and `scripts/test-chat-browser.js`.
No Calendar internals, shared server, provider adapter or media-production files
are in scope. No concurrent domain writing or delegated work is requested.

The additive v1 contract retains synthetic execution (offline, no model or runtime
source fetch) while distinguishing `public_snapshot` source/evidence from invented
fixtures. Snapshot facts require HTTPS provenance and a retrieval instant.
Movie catalogue entries may have null start/end times and must remain needs-checking,
with incomplete date coverage; missing showtimes never become synthetic occurrences.
TPBL end times remain unknown. Calendar fit stays unknown for incomplete timing;
parent/child synthetic schedules are not evidence of real family availability.
Partial public results must still allow the next explicit date-refinement scene.
Preference matching/ranking and reasons use page-local age, interests, team and
coarse area, never calendar identities; nothing is forwarded to external sources.
No ticketing, purchase, notifications, retained family profiles or live activation.
Unlicensed posters/logos are not copied; real items must not reuse fictional artwork.

Public facts were reviewed at **2026-09-20T09:29:03Z**, then saved as a bounded
three-record catalogue in `activity-preview/chat-public-snapshot.js`:

- [TPBL official feed](https://api.tpbl.basketball/api/seasons/3/games): game 27539,
  CTBC DEA vs Formosa Dreamers, October 10 at 17:00, Taipei Heping Basketball
  Gymnasium. [Official game link](https://tpbl.basketball/schedule/27539).
  The feed provides a local start, interpreted as Taiwan venue time, but no end.
- [Forgotten Island](https://www.vscinemas.com.tw/vsweb/film/detail.aspx?id=8956):
  release September 25, 2026; runtime 109 minutes. The official film page lists
  Taipei Hsin Yi for its Mandarin digital version. Selected as the October film idea.
- [Chiikawa The Movie: The Secret of the Mermaid Island](https://www.vscinemas.com.tw/vsweb/film/detail.aspx?id=8786):
  release July 31, 2026; runtime 100 minutes; Taipei Hsin Yi is listed for the Mandarin
  digital version. Selected as the nearer film idea. It is not a confirmed September
  20 screening. Both film pages display Taiwan Protected classification; numeric
  admission conditions and individual age-7 suitability were not independently verified.

The [official showtime page](https://www.vscinemas.com.tw/ShowTimes/) describes
weekly publication and exceptions. The retrieval exposed no dated screening for
either requested window. Runtime is retained as catalogue evidence, never converted
into a made-up start/end. The source subset is partial in both scenes and does not
claim complete basketball/movie discovery or cinema availability. In the nearer
scene the saved October game is filtered out, not relabelled. The later public
snapshot is explicitly replayed against the scripted September 18 reference; this
is not a claim about what was available or known on that reference date.

The chat HTML now injects the public catalogue through the existing search-pool
interface. Original synthetic pool tests remain intact. Source and evidence kinds
are `public_snapshot` with HTTPS links and matching retrieval times; runtime mode
remains `synthetic` because no live adapter or AI was enabled. Untimed movies require
unknown timing evidence, partial coverage and needs-checking status. Public records
reject fictional thumbnails/evidence. Both missing showtimes and the September
calendar coverage gap are visible. Assessments with expiry now retire even when all
time-fit statuses are unknown; Clear/expiry do not initiate another search.

The approved CTBC DEA preference appears in the family summary. First-submit copy
names the page-local interests, team, age and area before presenting results. Public
ranking uses category-interest score 2 plus preferred-team score 1, so choosing only
movies ranks movies ahead of an otherwise preferred team's game. Reasons are computed
from actual matches and change with preferences; unknown age suitability/travel remain
explicit. Matching a listed cinema's district is not a routing estimate. The existing
game details disclose official identity/source under **Explore basketball - game and
source**. A separate multi-game/team editing explorer was not added in this increment.

Executed verification: **357/357** across the eleven bounded chat, Activities,
Calendar and shared-date suites; full isolated Chromium workflow passed at **1440px**,
including first-response preference reasons, official source links, real title changes,
no fictional media, keyboard controls, Clear/expiry regressions and zero forbidden I/O.
Evidence: `browser-artifacts/chat-preferences-1icI1v/`; public-picks screenshot reviewed.
No mobile rerun, real-calendar proof, numeric age-admission verification, licensed
posters, ticket availability, travel estimate or parent usability approval is claimed.
Public pages were read during preparation only. No runtime live search, paid service,
private snapshot access, deployment, shared-service restart, commit or push occurred.

The existing isolated preview initially returned 404 for the new snapshot module
because its static allowlist was captured at startup. The owner separately approved
updating and restarting only `localhost:34393`. That preview was restarted with the
current HTML-derived allowlist on the same loopback port. All 19 HTML/script/style
assets matched disk; API paths, unlisted files, query strings and POST remained
rejected. The real-calendar service on port 8002 was not touched. This single-use
preview restart approval has been consumed and grants no further service operations.

**Message-box Enter support, 20 September 2026:** the owner requested Enter to send.
Builder updated `chat/ui.js` so plain Enter shares the existing form-submission
handler. Shift+Enter retains native multiline editing. Composition events and
keyCode 229 do not send; modified Enter and already-handled events are left alone,
and repeated Enter keydowns are suppressed. Blank/busy safeguards, search budgets,
button submission and listener disposal remain unchanged.

`test/chat-integration.test.js` passed **28/28** with keyboard regressions. The full
`scripts/test-chat-browser.js` run passed at **1440 CSS px**, including native
textarea Enter/Shift+Enter, simulated IME key events and the existing desktop
workflow; zero forbidden I/O. Evidence: `browser-artifacts/chat-preferences-pvVGP6/`.
Actual OS input-method candidate selection and mobile keyboards were not tested.
Only those three code/test files and this record changed for this increment.

**Composer spacing repair, 20 September 2026:** the owner waived official-holiday
verification for this increment and requested focus on the failed full desktop
browser workflow. Weekend-only candidate behavior remains unchanged; no holiday
verification or entitlement is implied.

The full runner reproduced the failure: the fixed composer was 134px tall but
workspace bottom padding was only 134px instead of the expected 166px. Its measured
height still reflected the initial in-flow composer, before 32px of padding was
added. `ResizeObserver` used its default content-box observation, which misses this
padding-only change. Builder changed `chat/ui.js` to observe `border-box`, added
an observer-options regression assertion in `test/chat-integration.test.js`, and
retained the original geometry thresholds with diagnostic dimensions in
`scripts/test-chat-browser.js`. No delay or relaxed assertion was introduced.

The focused integration suite passed **26/26**. The **full** browser runner passed
at **1440 CSS px**, including preference editing, resized-composer clearance,
activity refinement, Calendar preservation, reset/reload and keyboard interaction,
with zero forbidden I/O. Evidence: `browser-artifacts/chat-preferences-eaZveb/`;
editor and results screenshots were visually reviewed. This resolves and supersedes
the broader-browser failure recorded below. Mobile was not rerun. Only the three
listed code/test files and this record were changed for this repair; no services,
private calendars, holiday sources, dependencies, commits or pushes were involved.

**Varied weekend times and conflict coordination, 20 September 2026:** the owner
approved the proposed synthetic-calendar refinement and independent third demo
scenario. Builder continues as sole writer for this local synthetic increment:
`owner/chat-calendar-fixtures.js`, `owner/chat-calendar-template.js`,
`owner/chat-calendar.js`, `owner/chat-calendar.css`, `owner/availability-ui.js`,
`chat/index.html`, `chat/ui.js`, `test/chat-calendar.test.js`,
`test/chat-integration.test.js`, `scripts/test-chat-browser.js` and this record.
Other uncommitted work, provider adapters and running services are preserved.

The month fixture now contains different weekday and weekend commitments for each
parent and two additional fictional child activities. Candidate calculation still
uses the loaded snapshots and existing half-open conflict assessment. Only Saturdays
and Sundays qualify, within 09:00-20:00 Taipei and at least two hours long. Select
one date per Monday-based week first, then additional dates up to six; duplicate
clock intervals are omitted, never changed to manufacture variety. Data can yield
fewer than six and is never padded. Default complete October fixtures yield:

| Date | Taipei interval |
| --- | --- |
| Saturday October 3 | 09:00-12:00 |
| Sunday October 4 | 14:00-17:00 |
| Saturday October 10 | 13:00-19:00 |
| Saturday October 17 | 10:00-12:00 |
| Saturday October 24 | 15:00-18:00 |
| Saturday October 31 | 10:30-14:30 |

These cover five October weeks. Weekend eligibility is not evidence of time off,
travel fit or guaranteed availability. The official DGPA calendar listing at
`https://www.dgpa.gov.tw/informationlist?uid=30` was read, but the retrieval did not
expose verifiable 2026 dates. National/substitute holidays are therefore **not**
asserted or added; Details states that limitation. Default seven-day real-calendar
contracts and direct week fixtures remain unchanged.

**Review a school meeting** is an independent, explicitly clicked Calendar-local
scenario after loading, not a third input-order activity query or model inference.
The fictional October 16 meeting is 15:30-16:30 Taipei. Parent busy slots compute
Parent A's 15:00-16:00 conflict as 30 overlapping minutes; Parent B's 17:00-18:00 busy
interval does not overlap. A shared 14:00-18:00 time axis shows the meeting, busy-only
parent intervals and hatched overlap. No work titles or child details leave Calendar.

The parent must confirm **Only one parent needs to attend** before **Propose Parent B
attending** is enabled. Selection changes only the proposed attendee and shows the
before/after comparison, with Parent B's confirmation, willingness and travel unresolved.
Undo and removing the one-parent condition withdraw the proposal. No notification,
contact, booking or calendar write is implemented. Freshness is rechecked on selection;
Clear, date invalidation, partial/missing/stale/revoked data and session expiry fail
closed. Both parents busy prevents the alternative. The additive presentation-only
`resetProposal()` method clears the local condition/choice on New conversation without
reloading or changing Calendar snapshots. No new chat assessment payload is introduced.
The two activity scenes, query budget, privacy projection and September unknown state
remain unchanged. Candidate selection still does not filter the separate activity search.

Validation: the eleven bounded offline suites passed **352/352**; after the final
proposal-status wording change, Calendar tests passed **51/51** again. The existing
browser runner now supports `--calendar-review`, run at **1440 CSS px** with its
existing isolated Playwright/runtime installation. Final evidence:
`browser-artifacts/chat-preferences-nOHoES/`. It verified six distinct windows over
five weeks, keyboard day selection and coordination, actual 30-minute overlap,
condition gating, before/after screenshots, undo, reset, September unknown, Clear,
no horizontal overflow and zero forbidden I/O. Screenshots were visually reviewed.

The broader browser runner reached the new checks successfully but later failed its
existing preference-editor `content reserves composer height` assertion. Its cause
is not established and no unrelated product-layout repair was made; do not report
the full browser workflow as passing. The shared embedded browser also timed out
clicking its non-visible tab, so keyboard evidence comes from isolated Chromium.
No mobile, real-calendar, national-holiday completeness, real AI or parent-usability
approval is claimed. No services were started/restarted, packages installed, private
snapshots read, calendars modified, or changes committed/pushed.

**Candidate time shortlist, 19 September 2026:** the owner approved replacing the
expanded, repetitive month grid as the primary chat Calendar view with computed
candidate times and click-through day timelines. This is a continuation of the
sole-writer local synthetic Calendar work, not real-calendar or model approval.
Builder's exact files for this increment are `owner/chat-calendar-fixtures.js`,
`owner/chat-calendar-template.js`, `owner/chat-calendar.js`,
`owner/availability-ui.js`, `owner/chat-calendar.css`,
`test/chat-calendar.test.js`, `scripts/test-chat-browser.js` and this document.
Existing uncommitted work is preserved; no parallel writer is assigned.

Calendar computes candidates from its actual loaded parent/child fixtures using
the existing half-open occurrence assessment, not DOM scraping or hardcoded result
dates. It scans October in half-hour intervals, 09:00-20:00 Taipei, merges adjacent
`no_conflict` intervals, and chooses each day's longest interval of at least two
hours (earlier wins ties). The first three qualifying dates are displayed. These
explicit demo display rules appear in Details and do not become family preferences.
The current fixture repeats daily 09:00-11:00 parent busy time, so October 1, 2
and 3 all yield 11:00-20:00; no artificial variety is inserted into the results.

The primary heading is **Time together in October**, followed by date/time choices
and an explicit sample/no-guarantee/travel caveat. **View day** opens and focuses the
existing three-person timeline, with a dashed candidate-range outline and textual
range. **Browse October** starts closed. Selecting, expanding and collapsing are
presentation-only: no query, consent, date-storage mutation or activity search.
The first/second activity scenes retain October 9-11 and September 19-20. Candidate
selection is not a new activity-search filter; no unsupported Find button is added.

The calculator stays inside Calendar. No new public chat seam, raw calendar fields,
candidate windows or profile storage are sent to Activities or a model. Missing,
partial, expired, revoked and cleanup-failed contexts cannot produce candidates.
Known all-busy coverage has a distinct empty state. Clear/date changes withdraw the
shortlist and highlight; freshness timers redraw without queries, and clicks check
freshness again before showing a candidate. Real Calendar defaults are unchanged.

Validation: **340/340** across the eleven bounded chat/activity-card/availability/
child/date-selection suites. New tests cover interval merging and daytime clipping,
unknown/all-busy states, Clear, invalid dates, timed expiry, stale clicks, same-day
timeline highlighting and presentation-only selection. Desktop browser review at
1788 CSS px confirmed the three choices, initially closed month, keyboard selection
and timeline focus, 31-date browsing, October 31 details, September unknown fit,
Clear removal and no horizontal overflow. Screenshots verified the shortlist and
three-track highlighted day view. Fetch/XHR calls remained zero. The existing
browser runner's new assertions pass syntax checking; its full historical
desktop/mobile workflow was not rerun. No mobile, live-provider or parent-usability
approval is claimed. The shared preview was left on the first-scene shortlist.

**Full October synthetic calendar and demo copy, 19 September 2026:** the owner
approved simulating all October dates and moving scripted-flow explanations out
of the main conversation. The chat Calendar now loads October 1-31 inclusive,
Taipei, from September 30 16:00 UTC to October 31 16:00 UTC exclusive: 1,488
half-hour slots per parent. All 31 days are viewable and assessed from the same
loaded fixtures. September is not added; scene two still reports unknown fit.

`owner/availability-core.js` and `owner/child-calendar-core.js` expose isolated
`forSyntheticOctober()` instances, selected only by `owner/chat-calendar.js` and
the explicit synthetic fixture option. Default core objects, seven-day validation,
October 9-15 live scope, real source authorization and shared date storage remain
unchanged. The child controller's coverage text derives from its validated result
instead of a hardcoded week. No real-calendar request, service restart or storage
change occurs. The assessment accepts the previously reviewed week or this exact
month, not arbitrary expanded ranges. Fail-closed stale/Clear behavior is retained.

The main reply now says "Let me check your October calendar and find some ideas."
The October proposal and sooner reply keep their explicit dates but omit technical
scene instructions. A closed, keyboard-accessible **Demo details** disclosure
states fictional data, fixed input-order scenes, no AI interpretation, bounded
October 9-11 activity discovery, the fixed September reference and travel limits.
Whole-month calendar coverage is not a claim of whole-month activity discovery.

This follow-up touches the two core factories, synthetic fixture/mount, child
coverage copy, chat core/UI/HTML/CSS, existing Calendar/chat tests and their shared
test harness. No Activities source or shared date-contract changes are included.
Validation: **336/336** across eleven bounded offline suites, including original
availability, child and date-selection regressions. Desktop browser checks verified
31 enabled October dates, October 31 details, two assessed October cards, natural
main copy, closed disclosure with keyboard open/close, unchanged September unknown,
no horizontal overflow and zero fetch/XHR calls. Preview was reloaded to idle.
No new mobile or live-provider verification is claimed.

**Local synthetic calendar-fit approval and implementation, 19 September 2026:**
the owner requested completion using local fake calendars and explicitly assigned
Builder sole writing of `owner/chat-calendar.js`, `owner/chat-calendar-fixtures.js`
and their offline tests for this increment, confirming no concurrent Calendar writer.
Builder also owns `chat/index.html`, `chat/ui.js`, `chat/chat.css`,
`chat/conversation-core.js`, `test/chat-conversation.test.js`,
`test/chat-integration.test.js`, `test/chat-calendar.test.js`, the fixture wrapper
in `test/fixtures/our-week-harness.js`, and this contract record for the integration.
Activities files, its v1 request/result schema, and real owner controllers remain
unchanged. This supersedes the older synthetic calendar-assessment deferral below
only for this local fixture implementation; it authorizes no real calendar reading,
model transfer, public-provider disclosure, service operation or persistent storage.

The existing Calendar fixture transport now compares occurrence intervals against
the same in-memory snapshots that populate its view. The mounted demo chooses the
complete `loaded` fixture; partial, missing, stale, unavailable, revoked and cleanup
failure scenarios remain available to tests. No second calendar/controller or DOM
scraping is introduced. Parent busy/tentative/out-of-office slots and scheduled
child intervals exclude overlaps, using half-open bounds; cancelled events do not
block time. Incomplete, unavailable, stale, missing-time and out-of-coverage results
remain unknown. No reported busy block does not imply guaranteed availability or
account for travel/working elsewhere. Coverage stays October 9-15; September does
not trigger another calendar load.

The additive synthetic Calendar seam is:
- `assessOccurrences([{ id, startAt, endAt, timeZone }])`, at most 40 records,
  synchronous local execution only. IDs identify public activity occurrences.
- Output version `familycopilot.calendar-fit.v1`, local revision, comparison
  `checkedAt`, fixed `coverage`, earliest snapshot `expiresAt` or null, and matching
  `{ id, status, reason }` records. Status is `no_conflict`, `conflict` or `unknown`.
  No names, titles, busy intervals, consent, source handles or raw events leave
  Calendar. Chat strictly checks and projects this response; malformed/error
  responses become unknown, not a successful check.
- `subscribeAssessment(listener)` returns an unsubscribe callback. Clear, source
  changes and new fixture requests invalidate previous checks; collapse does not.
  Initial preparation changes are awaited by the existing gate. Later changes and
  expiry cancel/fence work and retire old cards, with no automatic search or retry.

Chat compares after discovery and before showing recommendations, excluding only
known conflicts. All-conflict results are explicitly empty; unknown options remain
visible with their reasons. A separate page-local assessment accompanies the
unchanged Activities cards. Its context replaces the generic provider-side
not-checked paragraphs through the scoped `calendar-assessed` class; Activities
itself still receives no Calendar information. Date/reference, two-scene behavior,
preference preservation, query budget and generation fencing remain unchanged.

Validation: six bounded suites passed **147/147**, covering actual conflict
filtering, all-conflict empty results, boundary times, partial/failure/stale states,
Clear, expiry, malformed assessment, no raw data transfer and late-response fencing.
Desktop browser review at 1788 CSS px verified two October cards with no overlap
reported, keyboard refinement to the distinct September movie with unknown fit,
unchanged October coverage, loaded movie image, no horizontal overflow and zero
fetch/XHR requests. The initial browser automation attempt was blocked by a hidden
tab; bringing the same shared page to the foreground resolved it. No mobile,
real-provider, parent-usability or standalone browser-runner success is claimed.

**Calendar-first reply repair, 19 September 2026:** the opening parent message
now precedes Copilot's calendar reply and the existing Calendar mount; subsequent
messages and activity cards follow it. While preparation is pending, the reply says
"Loading your family calendar...". Afterwards the opening intent remains visible:
"Let me load your family calendars first, then look for October ideas."
The global completed-search status no longer says "Calendar availability not
checked" above the conversation; that limitation remains beside suggestions.
No Calendar DOM/data is read, moved or remounted. The existing preparation gate
still precedes the activity search, and Calendar failures still permit independent
discovery without claiming a successful load or verified calendar fit.
This fixes presentation, not calendar-derived filtering: the requested actual
assessment remains a separate privacy/contract milestone. Focused core/UI tests
passed 47/47, including delayed Calendar preparation, chronological shell order
and reset. Desktop browser review confirmed parent/reply/calendar/suggestions
order, two October cards, no horizontal overflow and zero fetch/XHR calls.
No live access, service operation or mobile review occurred.

**Latest demo-flow approval, 19 September 2026:** the owner approved a two-scene,
input-order-driven scripted demo, replacing text matching for composer submissions.
The first valid submitted message proposes the preset activity range
2026-10-09 through 2026-10-11 (Asia/Taipei); it does not claim to search all October.
After a completed result, the next distinct valid submission searches 2026-09-19
through 2026-09-20 using the fixed demo reference 2026-09-18T04:00:00Z. Wording
does not select dates, infer preferences or invoke a model. Contextual responses
explicitly identify this as a fixed scripted scenario, not language understanding.
Further submissions show completion without another search. Duplicate or in-flight
submissions do not advance; invalid/blank inputs do not consume a scene. Failures,
partial/unavailable results, cancellation and preference changes require an explicit
retry before advancing. Retry stays in the same scene and keeps the existing
three-dispatch budget. Reset/reload returns to scene one. Cancellation and generation
fences remain; no raw chat or Calendar data enters activity requests.

This is desktop-only synthetic execution. Calendar coverage remains October 9-15;
scene two neither queries September calendars nor implies calendar fit. The current
default preferences are age 7 and seven interests, as recorded in the
[design decisions](designer/README.md#current-owner-confirmed-chat-decisions).
No service activation, live query, profile persistence or provider permission is added.
Focused core/UI tests passed 47/47; desktop browser keyboard submissions with
non-keyword wording produced two October cards then one distinct September movie
occurrence, unchanged Calendar coverage, no overflow and zero fetch/XHR calls.

**Latest presentation refinement, 19 September 2026:** the owner explicitly
requested age beside Child, removal of the numbered Children block, interest bars,
openness sentence, redundant headings/idle text and masthead Demo / Scripted badge.
The single age preference now renders beside Child; this association is presentation
only and adds no identity field to requests or Calendar coupling. The interest plus
control opens the existing editor, with comma-separated custom interests under the
unchanged 12-item/40-character contract. Apply/Cancel, generation fencing and no
automatic search remain unchanged. Source evidence and scripted limitations remain
in contextual responses/details. This supersedes older badge/Children copy below.
See the [updated design decisions](designer/README.md#current-owner-confirmed-chat-decisions).

**Design reference and interests, 19 September 2026:** all nine supplied designer
PNGs have a [detailed textual reading](designer/README.md), separating depicted
intent from later owner decisions and unimplemented capabilities. Read it before
changing chat UX. The owner-selected demo defaults are now Basketball, Baseball
and Movie, with other suggestions welcome. Runtime seed and shared fixture both
use `["basketball", "baseball", "movies"]`; interests remain ranking preferences,
not hard filters. Ping-pong remains available but unselected. No new source,
Calendar access, retained profile or action permission is introduced. The old
empty-interest defaults in historical records are superseded.

**Presentation wording amendment, 19 September 2026:** the owner requested removal
of Synthetic from the UI. Chat, embedded Calendar and reusable activity cards now
omit repetitive labels or use brief Demo/source wording. The header retains
Demo / Scripted; evidence, not-checked/unknown states and no-live-retrieval facts
remain. Internal `mode: "synthetic"`, validators, transport, permissions and
asset identifiers are unchanged. Historical literal UI-copy assertions below
are superseded, not the synthetic-only execution boundary. The standalone owner
page is not relabeled. No service activation or real-data query was performed.

**Editable preferences amendment, 18 September 2026:** the owner approved real
page-local age, origin-area and interest editing plus a bottom-docked composer.
This supersedes the earlier fixed age/origin restriction, not the Calendar interface:
`familycopilot.chat.v1` retains its exact envelope and seven validator exports;
`ages` is exactly one integer 0..120, initially `[8]`; `origin.area` is trimmed,
nonblank plain text of at most 120 characters, initially Xinyi. Precision stays
`district`, landmark stays null, and no geocoding or travel evidence is inferred.
Interests remain bounded ranking preferences, not hard filters. Only changed,
applied interests become `parent_confirmed`; age/origin-only changes preserve basis.

Activities acknowledged this amendment and sole ownership of
`activity-preview/chat-search.js` and `test/chat-activity-search.test.js` for
dynamic-age disclosure and changed-preference regressions. Builder owns the
shared validator/tests, conversation/UI/integration tests and this record;
Calendar and other Activities files remain read-only. Existing uncommitted work
is preserved. No live provider, Calendar data, storage, Sync or date mutation.

Draft/Cancel have no query or committed-state effects. Apply validates the whole
candidate context, advances context revision and retires/aborts old requests and
cards without dispatch, resetting budget or resuming a paused session. Updating
suggestions is a separate explicit action using the existing retry trigger and
current preferences. Invalid Apply leaves committed state/work untouched. Reset
conversation restores the original fixture defaults; reload also starts fresh.

**Isolated browser review completed, 18 September 2026:** after separate owner
approval, the integrated synthetic shell passed desktop/mobile and browser-keyboard
checks on an allowlisted static-only loopback service. Both original images loaded
under the page CSP; the reviewed flow made zero API/external requests. The review
service was stopped. No product-code repair was needed; see
[browser evidence and limits](#13-isolated-synthetic-browser-review).

**Integrated offline, 18 September 2026:** the owner delivered Activities synthetic
v1 and then Calendar stage 1, requesting Builder-owned wiring and actual-component
integration tests. Both delivered seams now load in the shared static chat shell;
see [integration evidence](#12-delivered-domain-integration). Six focused offline
suites pass **99/99 in each of UTC, Asia/Taipei and America/Los_Angeles**. This is
synthetic Node/DOM-harness evidence, not browser or real-provider acceptance.
No service operation or live access was authorized or performed.

**Latest approval, 18 September 2026:** the owner relayed Activities' confirmation
that **T1 and offline M1 are approved**, and requested the shared validators,
contract tests and synthetic context fixture before Activities continues its
synthetic search/reusable-card implementation. Those prerequisites are delivered
and revalidated; see [Activities handoff](#10-t1m1-approval-and-activities-handoff).
This turn delivers code/file coordination only, not a new implementation writer,
service operation, live query or runtime activation.

**Earlier scoped handoff, 18 September 2026:** the owner approved synthetic
Calendar stage 1 and requested delivery of the shared v1 contract first. The
[pure contract module](../shared/chat-contract.js),
[contract tests](../test/chat-contract.test.js) and
[non-calendar context factory](../test/fixtures/chat-context.js) are now delivered.
The owner confirmed that the existing Calendar chat is waiting for this handoff
and other writers have stopped editing its section 5 paths. That existing chat,
not a second Calendar implementation agent, receives the sole-writer handoff.
See [delivery and ownership record](#9-shared-contract-delivery-and-calendar-handoff).
That earlier stage-1-only approval did not approve T1 or stages 2/3; the later
T1/M1 approval above supersedes that coding gate only. Services remain unapproved.

The initial publication was a **documentation-only integration specification**,
not implementation or operational permission. It consolidated both owner-supplied
reviews; the following design boundaries remain:

- Calendar stage 1: single-instance mount, expand/collapse and dispose; Calendar
  renders its own source, coverage, freshness and errors. Builder receives only
  presentation controls, not calendar state or even the existing title-free bridge.
- Activities stage 2: `searchActivities(request, { signal })`, correlated bounded
  requests/results, reusable illustrated cards, no raw chat/private calendars,
  and `calendarFit: "not_checked"`. The explicit once-started scripted synthetic
  session is now approved as T1; no startup or live query is implied.
- Calendar reports that the **second Child Sync remains stuck/unresolved**. This
  blocks an actual calendar demonstration. It was not reproduced or diagnosed in
  this contract work; the approved synthetic work does not establish live success.

Design baseline: [current chat-first handoff](activity-search-component.md),
[04 calendar](../activity-preview/poc-storyboard/04-calendar-chat.svg),
[05 suggestions](../activity-preview/poc-storyboard/05-ai-picks.svg), and
[06 refinement](../activity-preview/poc-storyboard/06-sooner.svg).
The [product/privacy plan](parent-schedule-activity-discovery.md) and current
[Our week records](our-week.md) retain their narrow authorization boundaries.
The SVGs contain fictional schedules/listings/artwork, not evidence of capability.

### Additional visual guidance: designer concepts

On 18 September 2026 the owner supplied the nine PNG concepts under
`docs/designer` as implementation guidance. Use the
[chat composition](designer/Designer%20%288%29.png),
[navigation and status cards](designer/Designer%20%283%29.png),
[illustrated activity card](designer/Designer%20%286%29.png) and
[brand overview](designer/Designer%20%2810%29.png) as visual references:
warm off-white surfaces, dark teal navigation, green accents, softly rounded
cards, generous spacing, clear typography, friendly illustration and restrained
pastel status backgrounds. Retain text/icons alongside color, accessible contrast,
keyboard focus and responsive layouts. Use the chat-first stages above rather
than replacing them with the dashboard or adding nonfunctional navigation.

These concepts guide presentation, not authorization or source evidence. Do not
copy claims of conflict-free schedules, known ticket availability or suitability
without evidence. Reminder scheduling, registration, calendar writes/rescheduling,
background monitoring, inbox access and cross-calendar reasoning remain outside
M1. `calendarFit` stays `not_checked`. Depicted names, schools, interests and dates
are not user profile inputs. Artwork remains subject to the thumbnail provenance
rules; do not crop concept screens into purported live results. The T1/M1 approval
gates were unchanged by the visual-guidance request; the later stage-1-only
approval is recorded separately above.

This v1 replaces the integration proposals in
[Builder's v0 checklist](../.github/agents/familycopilot-builder.agent.md)
for the proposed offline milestone only. It does not override stronger active
agent instructions, provider contracts or existing live APIs. Calendar and
Activities must acknowledge this exact v1 and their file sets before coding;
their supplied reviews are not yet acknowledgement of these final details.

## 1. Roles and dependency order

| Stage | Sole responsible role | Deliverable |
| --- | --- | --- |
| 1 | [Calendar](../.github/agents/familycopilot-auth.agent.md) | Existing Our week presentation in an expandable panel, independent source lifecycle and truthful status |
| 2 | [Activities](../.github/agents/familycopilot-activities.agent.md) | Bounded search seam and evidence-based reusable illustrated cards; basketball/movie are alternatives |
| 3 and shell | [Builder](../.github/agents/familycopilot-builder.agent.md) | Shared conversation, date interpretation, request lifecycle, context retention, assembly and integration tests |

Order: owner confirms milestone/trigger → both domain owners acknowledge v1 →
Builder supplies pure contract validators and synthetic non-calendar context →
domain seams and Builder shell proceed within separate owned files → Builder
integrates → parent reviews. No agent changes another owner's files to unblock
itself. An incompatible seam is returned as a contract amendment first.
The earlier stage-1-only approval permitted the independent Calendar seam and
shared-validator prerequisite. The subsequent T1/M1 approval permits the offline
flow within these ownership boundaries; current work is the Activities handoff.

## 2. Host and Calendar presentation boundary

### Host decision

The proposed offline entry is the Builder-owned new `chat/index.html`, containing
one persistent `#calendar-host`, one conversation region and one composer. This
is a dedicated offline review surface, **not a replacement of `/` or `/activities`
and not an iframe/proxy to port 8002**. No server route, port or startup operation
is assigned by this document. Browser review uses an explicitly approved isolated
offline harness; until assigned, a review server must not be started.

Builder owns the host element's placement, not its contents. Calendar mounts its
own markup/styles/controller once inside it and reuses the existing Our week
rendering/consent/lifecycle, rather than drawing the SVG grid or pasting a screenshot.
Required legacy IDs occur only once per document. Standalone auto-bootstrap must
not also run on the chat host. Calendar owns any necessary explicit initialization
and injected synthetic-transport refactor; a `synthetic` label/meta tag alone is
not a network-isolation mechanism.

Calendar injects both a fail-closed in-memory transport and an in-memory date
store (using the existing store's injection seam). No native-fetch or real-browser-
storage fallback is allowed for mount, Sync, saved viewing, date edits, Clear or
dispose. Shared date utilities remain read-only. Contract validators cover public
interface records only; Calendar's private-shaped synthetic payloads remain in
Calendar's own validation/fixtures, not the shared chat schema.

This DOM ownership is an application data-flow restriction, not a security sandbox
against same-origin JavaScript. Builder must not read descendants, observe their
mutations, serialize them, subscribe to internal Calendar events or pass them into
messages, telemetry, Activities or a model. No external model executes in v1.

### Interface

`mountCalendar(host, { version, mode })` is synchronous and returns a frozen
controller exposing **only** `expand()`, `collapse()`, `dispose()` and
`loadSynthetic()`. The last method is the owner-approved conversation-trigger
amendment in section 17; it grants no live operation or Calendar data access.

| Operation | v1 semantics |
| --- | --- |
| `mountCalendar` | `version` is `familycopilot.chat.v1`; only `mode: "synthetic"` is accepted. First mount creates one instance; the same active host/options returns that controller. A second host, changed options or disposed document is rejected with a fixed local error code. No startup query, saved-file read or storage mutation. Calendar selects its own synthetic fixture/transport internally, not from Builder-supplied event data. |
| `loadSynthetic()` | Only an explicitly submitted, supported opening message invokes this command through session preparation. Returns one memoized `Promise<void>` per mounted Calendar, runs the existing child-then-parent fixture load at most once and suppresses focus stealing. Resolution indicates settlement, not success, coverage or calendar fit. Failures remain Calendar-owned; unexpected failures reject with fixed `calendar_load_unavailable`, disposed calls reject with `calendar_disposed`. New conversations never refresh Calendar. No DOM click simulation or event payload is exposed. |
| `expand()` | Returns no data. Shows the existing body, updates accessible expansion state and preserves local data, scroll, selection and original freshness. Idempotent; no Sync, remount, lifecycle reset or new query. |
| `collapse()` | Returns no data. Hides only the body; Calendar-owned summary/status remains. If focus is inside the body, moves it to Calendar's expand control. Does not cancel an already parent-started operation, clear consent, discard data or suppress freshness/error updates. |
| `dispose()` | Returns an idempotent `Promise<void>`. Immediately fences in-flight work, hides private page state and disables controls; invokes Calendar's existing **non-destructive leave** lifecycle once, then releases listeners/timers. Never maps to explicit Clear/deletion, never retries cleanup automatically. Failure uses a fixed `calendar_cleanup_unconfirmed` code, not state/payload. Required safety status stays visible until the document leaves. |

All repeated/racing dispose calls share the same promise and terminal outcome,
including rejection. After disposal begins, expand/collapse are no-ops and cannot
remount or restore data. Cleanup must explicitly establish success; a legacy
routine resolving after catching an internal failure is not success. Failed or
unknown cleanup rejects with `calendar_cleanup_unconfirmed`. Deliberate SPA exit
keeps the Calendar safety strip on failure; pagehide cannot guarantee awaited
completion and must never be reported as confirmed cleanup without evidence.

No `getState`, result callbacks, payload-bearing lifecycle events, source IDs,
identities, source metadata, busy slots, titles, consent or raw Calendar data are
returned. The existing title-free bridge remains **internal to Calendar**. Builder
can await synthetic load settlement or disposal, not infer what was loaded.

### Collapsed status and disposal timing

Calendar owns a persistent heading/expand control/status strip inside the host but
outside the collapsible body. It presents source/covered dates/per-source freshness,
saved-vs-fresh provenance, loading/partial/missing/stale/revoked/cleanup failures and
other important errors with accessible text, not color alone. Details can expand,
but essential uncertainty never disappears when collapsed. Builder must not copy
this strip into conversation state. Synthetic persons remain clearly synthetic;
age 8 is an activity preference, not an identity mapping.

An in-flight parent-started synthetic Sync may complete/fail while collapsed.
Status and freshness/expiry update in the strip without focusing hidden controls,
reopening the body, moving scroll/selection or issuing another query.

Collapse, chat refinement, activity cancellation, pause, result updates and **Reset
conversation** do not dispose Calendar or change its dates. A true route/document
exit or explicit teardown of the whole chat experience disposes it. Calendar owns
`pagehide`/`pageshow` integration; Builder calls `dispose()` before a deliberate SPA
exit, but does not manufacture `pagehide` or duplicate cleanup requests. Race/repeat
calls coalesce. A bfcache-restored disposed page hides old data and requires explicit
re-entry/reload; no automatic remount, saved load or Sync.

The synthetic transport performs only in-memory fixture cleanup. Future live
embedding must preserve reviewed `reason: "leave"` behavior and independent cleanup
without deleting completed snapshots/remembered sources; it needs separate review.
Explicit Clear still means the existing destructive user action, not a synonym
for collapse, reset-chat or dispose. Sticky cleanup errors cannot be bypassed.

## 3. Shared schema and validation

v1 is dependency-free JavaScript with browser/CommonJS-compatible pure validators.
Unknown keys are rejected at every record level, not silently stripped. All IDs
below are generated local/public-activity identifiers, never calendar/provider
credentials or person IDs. Numbers must be finite; date strings must validate as
actual dates/instants. Plain text renders through text nodes, never trusted HTML.

### Validator exports (Builder produces first)

The browser/CommonJS contract exports exactly these pure functions:

- `validateDemoContext(value) -> boolean`.
- `validateActivityRequest(value) -> boolean`, including reference-relative bounds.
- `validateActivityResultShape(value) -> boolean`, structure, unique IDs, evidence
  references and status consistency, without active-request or preference checks.
- `validateActivityResult(value, request) -> boolean`, including all correlation,
  range membership, unique identity, evidence references and status consistency.
- `validateCalendarMountOptions(value) -> boolean`, only version/synthetic mode;
  never Calendar payload validation.
- `activityRequestKey(validRequest) -> string`, canonical dedup key as defined in
  section 4; invalid input throws fixed `invalid_request` with no payload.
- `activityCardId(validOccurrence) -> string`, JSON serialization of the tuple
  `[sourceId, eventId, occurrenceId]`, at most 400 characters after JSON escaping;
  invalid input throws `invalid_result`. Individual identifier bounds do not
  override this conversation-reference bound.

Validators are total for JSON-like values, return false rather than echoing input,
have no I/O, clocks, mutation or storage, and never repair invalid records.
Activities uses `invalid_request`/`invalid_result` fixed errors at its API boundary.
Builder validates requests before dispatch and results before rendering. Renderer
accepts only already request-correlated results, then independently calls
`validateActivityResultShape`; it is not the authority
for active-request acceptance. All modules use the single shared implementation.

### 3.1 DemoContext and conversation records (Builder only)

| Field | Exact v1 type / constraint |
| --- | --- |
| `version` | Literal `familycopilot.chat.v1` |
| `sessionId` | Locally generated opaque ID, ASCII `[A-Za-z0-9_-]`, 1–64 characters; no persistence |
| `contextRevision` | Positive safe integer; increment when dates or preferences change |
| `referenceNow` | RFC3339 UTC instant with `Z`; fixed `2026-09-18T04:00:00Z` for this scripted fixture, not a claim of current live time |
| `timeZone` | Literal `Asia/Taipei` |
| `mode` | Literal `synthetic`; `live` is reserved and rejected by this milestone |
| `preferences` | The exact Preferences record below |
| `activityRange` | DateRange below, independent from Calendar's selection |
| `triggerState` | `not_started`, `active`, `paused` or `ended`; not a permission token |

Conversation message: `{ id, role, kind, text, requestId, cardIds }`. `id` uses the
same local-ID bounds; `role` is `parent` or `assistant`; `kind` is `text`,
`interpretation`, `activity_results` or `status`; `text` is plain text ≤1,000
characters, `requestId` is null or a request ID, `cardIds` is ≤6 unique
`activityCardId` strings, each ≤400 characters. Parent composer input is ≤500
characters. Retain at most 20 messages
in page memory; at the limit offer explicit Reset conversation, never silently
persist. No raw input is logged, sent to Activities/model, or used as authorization.

Stage 1 adds only fixed conversation wording and mounts the panel; its event data,
status, coverage and freshness never become a message or DemoContext field.

### 3.2 DateRange and Preferences

`DateRange` has exactly `{ startDate, endDate, timeZone }`: inclusive ISO civil
dates `YYYY-MM-DD`, 1–7 days, years 2000–2100, `Asia/Taipei`. Local comparison uses
start midnight inclusive to midnight after end exclusive. Source occurrences use
explicit instants/zone; never parse an unzoned timestamp using machine timezone.

`Preferences` has exactly:

| Field | Type / meaning |
| --- | --- |
| `ages` | Exactly one integer 0..120; initial `[8]`, editable; not attached to a person or calendar |
| `interests` | ≤12 unique plain-text tokens, each ≤40 characters; empty means no restriction, not all example interests confirmed |
| `interestBasis` | `demo_fixture` or `parent_confirmed`; changed, explicitly applied interests become parent-confirmed; age/origin-only Apply preserves basis |
| `preferredTeams` | ≤16 names, each ≤120 characters; empty by default; preserve existing validated source-name semantics rather than inventing IDs |
| `origin` | Exactly `{ area, precision: "district", landmark: null }`; area is trimmed nonblank plain text of at most 120 characters, initially `Xinyi District, Taipei City`. Coarse area only, no exact address, geocoding, inferred MRT or proximity ranking |
| `travelMode` | null, `walk`, `transit`, `drive` or `cycle`; default null; non-null requires explicit parent choice, never fixture inference |
| `constraints` | Exactly `{ maxTravelMinutes, budget, setting, excludedCategories }`; defaults null, null, `any`, [] |

`maxTravelMinutes`: null or integer 1–180. `budget`: null or `{ amount, currency,
basis }`, where amount is 0–100000, currency three uppercase ASCII letters and
basis `per_child` or `per_person`; not a family-total claim. `setting`: `any`,
`indoor` or `outdoor`. `excludedCategories`: ≤12 unique tokens ≤40 characters.
Constraints are requirements; interests/team names are preferences. Known required
violations are excluded before ranking/limits; unknown required evidence is flagged
`needs_checking`, never a confirmed match. No silent relaxation or inferred currency.

### 3.3 Activity request

`searchActivities(request, { signal }) -> Promise<ActivityResult>` accepts exactly:

| Field | Type / meaning |
| --- | --- |
| `version` | `familycopilot.chat.v1` |
| `requestId` | New local ID, 1–64 ASCII characters as above |
| `generation` | Positive safe integer, monotonically increasing across requests/cancellation/reset within this document |
| `contextRevision` | Current positive context revision |
| `referenceNow` | The context's explicit reference instant |
| `range` | Validated DateRange |
| `mode` | `synthetic` only; reject reserved `live` with `mode_not_enabled` before any work |
| `trigger` | Exactly `{ kind, actionId }`; kind `demo_start`, `refinement_submit` or `explicit_retry`; actionId is a local ID for one explicit user action |
| `preferences` | Validated Preferences, not the full conversation/context |

`signal` is a required AbortSignal supplied out of band, not serialized. Already
aborted calls reject with `AbortError` before work. Invalid schema rejects with a
fixed `invalid_request` error; no echo of unsafe input. No raw chat, Calendar data,
tokens, consent, source handles, busy intervals or arbitrary extension objects.
Request `trigger` text is not authority: Builder must enforce the approved session
state and Activities must validate the mode/trigger values. v1 invokes only local
fixture providers. It does not alter the existing public TPBL HTTP contract or
send age/origin/preferences to public sources.

### 3.4 ActivityResult and evidence/card records

The result has exactly the following fields:

| Field | Type / meaning |
| --- | --- |
| `version`, `requestId`, `generation`, `contextRevision`, `mode`, `range` | Exact request echoes, including range/zone; Builder rejects any mismatch |
| `status` | `results`, `unknown`, `empty`, `partial` or `unavailable` |
| `sources` | ≤8 Source records; actual searched scope, not supposed category coverage |
| `items` | ≤6 validated ActivityOccurrence records, after hard filtering and ranking |
| `issues` | ≤12 `{ code, sourceId, message }`; sourceId nullable, plain message ≤240 chars |
| `calendarFit` | Literal `not_checked` for every result, regardless of loaded calendars |

Issue code is one of `source_unavailable`, `source_unsupported`, `scope_incomplete`,
`candidate_limit`, `evidence_unknown`, `no_matches`. Source IDs in issues/items/
evidence must resolve to a unique Source record; nullable references mean global
issues, not an invented source. Sources is nonempty for dispatched fixture work.
Public identifier strings exclude control characters; local request IDs retain
the stricter ASCII rule. Duplicate `activityCardId` tuples are rejected.

`Source`: `{ sourceId, name, url, kind, coverage, retrievedAt, freshness }`.
Public/synthetic sourceId ≤80 chars; name ≤120; URL null for fixtures or validated
HTTPS ≤2048. kind `synthetic` in this milestone. coverage is exactly `{ range,
categories, completeness }`, with DateRange, ≤12 category tokens and completeness
`complete`, `partial` or `unknown`. retrievedAt is nullable RFC3339 UTC; null for
unretrieved fixtures. freshness `synthetic`, `fresh`, `stale` or `unknown`; only
`synthetic` is valid for v1 fixtures. Source update time is not invented from now.

`ActivityOccurrence` has exactly:

| Fields | Type / meaning |
| --- | --- |
| `sourceId`, `eventId`, `occurrenceId` | Source reference and public/fictional identities (each ≤120 chars); the same event on different dates has different occurrenceId |
| `category`, `title`, `sourceUrl` | Category token ≤40, plain title ≤160, null or validated HTTPS source link; fixture links never imply a real listing |
| `startAt`, `endAt`, `timeZone` | Explicit RFC3339 instants with offset, end > start when known; end may be null and then time fit is unknown; IANA zone `Asia/Taipei` for v1 |
| `venue` | `{ name, area }`, nullable plain name ≤160, area ≤120 |
| `ageGuidance` | `{ minAge, maxAge, rule, evidenceId }`; nullable integer bounds 0–120, ordered when both known; rule `recommended`, `required` or `unknown`; evidenceId nullable |
| `travel` | `{ durationMinutes, distanceKm, mode, originArea, evidenceId }`; nullable finite duration 0–1440 minutes/distance 0–1000 km; mode null or Preferences travelMode enum; originArea null or text ≤120; evidenceId null or ID ≤80; no absent value becomes zero |
| `cost` | `{ amount, currency, basis, evidenceId }`; amount null or finite 0–100000, currency null or three uppercase ASCII letters, basis null or Preferences basis enum, evidenceId null or ID ≤80; unknown cost never free |
| `setting` | `indoor`, `outdoor` or `unknown` |
| `thumbnail` | null or Thumbnail record below |
| `rationale` | ≤3 `{ text, evidenceIds }`; plain text ≤240, references ≤8 existing Evidence IDs; subjective preference reasons may have an empty reference list but must not assert facts |
| `evidence` | ≤16 Evidence records |
| `unknowns` | ≤12 plain-text uncertainty statements ≤160 chars, including travel/age/availability gaps as applicable |
| `assessment` | `candidate` or `needs_checking`; neither means calendar-compatible |
| `calendarFit` | Literal `not_checked` |

`Evidence`: `{ id, field, kind, sourceId, sourceUrl, retrievedAt, sourceUpdatedAt,
text }`. id ≤80 chars, unique within item; field is `identity`, `timing`, `venue`, `age`,
`travel`, `cost`, `setting` or `thumbnail`; kind is `synthetic`, `published`,
`derived`, `estimated`, `conflicting` or `unknown`; text ≤240; URLs/timestamps
nullable as above. All evidence in this milestone is synthetic or unknown, never
published/live-verified. Every non-null evidenceId and rationale evidence reference
must resolve within the same item. age/travel/cost/thumbnail references must match
their field. Identity evidence supports title/category and public occurrence IDs;
timing/venue/setting values require matching field evidence. Non-null factual
values require synthetic evidence in M1; null/unknown values cannot support
eligibility, price-total or travel claims. Retrieved/source-updated timestamps
remain null for fixtures, rather than implying a live lookup.

`Thumbnail`: `{ assetKey, alt, sourceUrl, attribution, permission, evidenceId }`.
assetKey ≤80 references a compile-time local allowlist, not an arbitrary URL/path;
alt ≤160, attribution ≤240, sourceUrl nullable, permission `original_synthetic`,
`licensed` or `unavailable`. Only `original_synthetic` local assets are enabled in
v1; unavailable/missing/load-error renders a neutral fallback. No remote logo/poster
request, arbitrary SVG/HTML injection or unlicensed provider artwork. Local media
keys and attribution are resolved by Activities, not a calendar/model payload.

Status precedence is normative, evaluated in this order:

1. `unavailable`: all requested source work failed/unsupported, zero items,
  source-unavailable/unsupported issue and no complete-source claim.
2. `partial`: any remaining incomplete/unknown source scope or candidate cap,
  0–6 items and a corresponding issue; takes precedence over `unknown`/`empty`.
3. With all stated scopes complete: zero items is `empty` with `no_matches`;
  1–6 all-needs-checking items is `unknown`; ≥1 candidate is `results` (may also
  include separately grouped needs-checking items).

Never convert failure into empty or claim no events exist. Loading/validation/
clarification/cancelled/paused are Builder lifecycle states, not source results.
Fixture occurrence IDs and facts stay stable; no date-shifting an old event.

Assessment rules: required age exclusions are hard failures; recommended guidance
is not an admission rule and a mismatch/unknown is `needs_checking`, not silently
age-suitable. Missing age guidance, missing end time or any unknown required
constraint makes `needs_checking` mandatory. Budget comparison requires matching
currency and unit basis with evidence; no conversion or assumed adult count.
Unknown age, cost, travel, ticket availability and end time must each appear in
visible uncertainty text when applicable, even without a corresponding constraint.
Ticket availability is always unknown in M1. Travel is usable for filtering **or**
ranking only with comparable evidenced origin/mode/metric; district-only v1 origin
does not support routing or quantitative travel-limit compliance. Leave travel
numbers null rather than using the SVG's illustrative estimates to pass a limit.

### 3.5 Card renderer

`renderActivityCards(host, result) -> { dispose() }` validates the result and owns
only that result's DOM/listeners. Disposal is local listener/DOM cleanup; repeated
calls are safe. Builder disposes the old renderer before replacing that result
region. Stages 2 and 3 use this exact function and `searchActivities`, not copies.

The renderer never searches, mutates preferences, accesses Calendar, calls Sync,
writes storage or invokes adapters. Only explicit user source-link navigation is
allowed, with validated HTTPS and `noopener noreferrer`; fixture links can be
absent/disabled. Secondary evidence is expandable, but synthetic labels, essential
unknowns, exact timing and `Calendar availability not checked` remain visible.
Travel ordering is allowed only for comparable origin/mode/metric/evidence; null
or district-only/unrouted travel does not rank as fastest. Missing matching movie
or game is not filled by invention. Detail toggles cause no network request.

## 4. Dates, trigger and request lifecycle

### Date decisions

- Demo reference is Friday 18 September 2026, 12:00 Taipei (explicitly shown as
  a scripted reference). Initial activity dates are 9–11 October 2026, from the
  demo fixture, **not read from Calendar or sessionStorage**.
- Each activity search is 1–7 inclusive days within the internal 28-civil-day
  window starting at the Taipei reference date: 18 September–15 October inclusive
  for this fixture. This is a local synthetic budget, not a live backend change
  or a mandatory parent-facing Look ahead setting.
- Membership is whole-occurrence containment in the selected midnight-to-midnight
  interval when end is known; boundary equality is permitted. Start must be in
  range and not earlier than referenceNow. Unknown end may appear only as
  `needs_checking` with duration/time-fit unknown. Supplied offsets must match
  `Asia/Taipei` at those instants; reject contradictory zone/offsets. Shared
  validators enforce both the 1–7-day range and the reference-relative window.
- `this weekend` resolves to the upcoming Saturday/Sunday; on Saturday or Sunday
  it means that current weekend, with already-started occurrences excluded against
  referenceNow. On Sunday, clip the query's already-past Saturday to the reference
  day and display that interpretation, preserving the forward-only bound.
  In this fixture it resolves to **19–20 September 2026**. The nearer
  fixture screening is a distinct 20 September occurrence, not a relabeled October
  event. Show the interpreted range before/with the result.

- The scripted interpreter recognizes the reviewed demo sentence and this bounded
  weekend intent only, ignoring case/outer whitespace. Conflicting dates, other
  relative-date intents or unsupported input require clarification without a
  search. This is not a general natural-language model. No age/origin inference.
- Calendar owns its independent selected display dates and authorized full query
  scope. Chat date changes never call its date actions, write
  `familycopilot.dates.v1`, dispatch its input events or trigger Sync. Calendar
  date edits likewise do not silently re-search or rewrite active outing context.
- Builder receives no Calendar coverage. It always says fit is not checked; exact
  coverage is displayed only by Calendar. It must not infer or compute September
  mismatch by reading the grid/bridge. A future comparison is separately gated.

### Trigger T1: approved for offline M1

Approved action: **Send the first message**, explicitly labeled synthetic/scripted,
opens a page-local demo session, reveals the Calendar inside the conversation and
awaits its single `loadSynthetic()` settlement before the initial `demo_start`
search. The message-first amendment below supersedes section 17's Start button.
Recommendation triggering does not depend on Calendar success or inspect its
state; unavailable Calendar does not prevent independent activity suggestions.
No Find form or required category/date setup.

The initial publication did not approve execution. The later owner-relayed
T1/M1 approval in section 10 now permits this offline action and execution model.
There are no live providers, background monitors, timers that search, notifications,
startup/reload/typing searches or automatic live fallback. Real proactive AI
requires another scoped decision. The UI must not describe fixtures as live AI.

The exact approved session rules are:

1. One initial search per explicit start. Double-click, render, focus, collapse,
   rerender or repeated Start while active does not issue another request.
2. Only explicit composer submission of a resolved changed range/constraint
   creates `refinement_submit`; typing does not. Invalid/ambiguous input gives
   clarification without dispatch. Age/origin/interests otherwise stay unchanged.
3. Builder maintains one active request, an AbortController, monotonically
   increasing generation and current context revision. Before dispatch of a new
   request, hide/invalidate prior active cards, increment generation and abort
   old work. Abort is advisory; accept responses only if version, requestId,
   generation, contextRevision, mode and entire range match the active record and
   session remains active. Test A → B → A races and providers ignoring abort.
4. Deduplication key is stable canonical serialization of version/mode/referenceNow/
  range/preferences, excluding IDs, generation, contextRevision and trigger.
  Object keys are lexically sorted; primitive types/null retained. Set-like
  interest/exclusion/team arrays use sorted unique normalized tokens for key
  computation only (trim/NFC/lowercase); occurrence facts and display names are
  never rewritten. Dates are exact validated ISO strings. Same active key in
  the same context revision coalesces. A repeated unchanged submission may reuse
  only the **current** completed, non-retired acceptance record without changing
  its IDs/revision/generation; results/unknown/empty qualify, partial/unavailable
  require explicit retry. No cross-revision cache: A → B → A dispatches a fresh
  A with new correlation, not re-enveloped old facts. Reused requestId with
  differing contents is rejected. Retain at most three page-local request keys;
  no global/persistent cache.
5. Retry is explicit after error/partial/unknown/empty if requested; allocate a new
   ID/generation/actionId and bypass completed-key reuse only for `explicit_retry`.
   Do not automatically retry aborts/timeouts. v1 permits **at most three dispatched
   searches per session** (initial + refinement + one additional refinement/retry),
   at most 40 synthetic candidates and 6 returned items per search, 10-second local
   timeout, and zero provider calls. Budget counts dispatches even if cancelled.
  Examine fixture candidates in stable sourceId/eventId/occurrenceId order,
  bounded to 40; filter known hard violations before ranking and the six-item
  output limit. If the input pool exceeds 40, mark affected coverage partial and
  emit `candidate_limit`; never call the examined subset exhaustive or empty.
  Search rejects in-flight cancellation with `AbortError`. Builder owns the
  10-second timer and fixed `request_timeout` UI outcome: retire the acceptance
  record and increment generation **before** aborting/presenting timeout.
  Late success, rejection and finally handlers may not change cards, status,
  loading or dedup entries. Normal rejection likewise retires that request;
  no automatic retry. Error messages never include raw inputs/provider bodies.
6. Pause increments generation, aborts and invalidates active results; Resume does
   not search automatically and offers an explicit retry/refinement. End or Reset
   increments generation, aborts, clears messages/cards/dedup/preferences to the
   fixture, and leaves state `not_started` for a new explicit Start. Calendar is
   untouched. Do not reset the document generation counter or reuse session IDs.
7. Effects are issued by the orchestrator only, never renderer/state derivation.
   Untrusted request/result/event text cannot trigger further requests or consent.

## 5. Exact file ownership for the proposed offline milestone

**The initial publication changed only this document.** The shared contract,
its tests and non-calendar context factory are now delivered under the later
scoped approval. Other new paths below remain reservations. Existing changes must
be preserved; each owner checks actual collisions and acknowledges its exact set.
Listed paths are an upper bound, not a requirement to edit all of them. Anything
not listed is read-only for this milestone unless an explicit handoff amends v1.

| Sole writer | New paths reserved for implementation |
| --- | --- |
| Builder | `shared/chat-contract.js`; `chat/index.html`; `chat/chat.css`; `chat/ui.js`; `chat/conversation-core.js`; `test/chat-contract.test.js`; `test/chat-conversation.test.js`; `test/chat-integration.test.js`; `test/fixtures/chat-context.js` |
| Calendar | `owner/chat-calendar.js`; `owner/chat-calendar-template.js`; `owner/chat-calendar.css`; `owner/chat-calendar-fixtures.js`; `test/chat-calendar.test.js` |
| Activities | `activity-preview/chat-search.js`; `activity-preview/activity-cards.js`; `activity-preview/activity-cards.css`; `activity-preview/chat-activity-fixtures.js`; `activity-preview/chat-assets.js`; `activity-preview/chat-assets/basketball-synthetic.svg`; `activity-preview/chat-assets/movie-synthetic.svg`; `test/chat-activity-search.test.js`; `test/activity-cards.test.js` |

Calendar owns template and synthetic transport/fixtures so Builder cannot supply
or receive event objects. Its fixtures are fictional, separate from real saved
data, and never copied into shared chat context. Activities owns activity fixtures,
media manifest and cards. Builder owns shared schema validators and non-calendar
context only. All producers/consumers import the single contract validator rather
than drifting copies. Browser namespace: `FamilyChatContract`; CommonJS exports
the same pure functions. Domain browser namespaces are `FamilyChatCalendar` and
`FamilyChatActivities`; their public functions are the interfaces in this document.
Builder loads each once in dependency order after its host markup exists.

| Sole writer | Existing paths permitted only if required by the approved slice |
| --- | --- |
| Calendar | [owner/index.html](../owner/index.html), [owner/owner.css](../owner/owner.css), [owner/ui.js](../owner/ui.js), [owner/availability-ui.js](../owner/availability-ui.js), [owner/child-calendar-ui.js](../owner/child-calendar-ui.js); isolate mounting, leave lifecycle and injected synthetic transport without changing standalone semantics |
| Calendar | [test/owner-ui.test.js](../test/owner-ui.test.js), [test/availability-ui.test.js](../test/availability-ui.test.js), [test/child-calendar-ui.test.js](../test/child-calendar-ui.test.js), [test/child-sync-ui.test.js](../test/child-sync-ui.test.js), [test/our-week.test.js](../test/our-week.test.js), [test/fixtures/our-week-harness.js](../test/fixtures/our-week-harness.js); focused preservation regressions |
| Activities | [scripts/test-activity-discovery.js](../scripts/test-activity-discovery.js), solely to include its new focused suites; existing providers/forms and fixtures remain read-only for the first slice |
| Builder | This document only; existing [shell.css](../shell.css), root sample, README and product plan remain read-only in the first slice; put chat-specific styles in the new owned stylesheet |

In particular, [shared/date-selection.js](../shared/date-selection.js), pure
Calendar cores, live TPBL adapter/routes, shared server, activation scripts,
infrastructure, VS Code tasks, package/configuration, private storage and agent
definitions are **not implementation write targets** for this milestone.
If extraction needs an additional path, stop that edit and obtain an exact
single-writer amendment instead of copying an entire controller into the new seam.
Do not start services during concurrent edits or edit code belonging to an active
shared service without coordination. If this conflicts with an existing process,
pause the conflicting work and request a handoff; do not stop/restart it. This
document neither inspects process state nor authorizes any service operation.

Potential later shared route/activation changes remain Calendar's single-writer
handoff after separate approval; port 8002 and its cooperative lock are untouched.
No branch/worktree switch, stash/reset/clean, commit or push is implied.

## 6. First offline milestone and acceptance

Milestone M1: one synthetic/scripted 1 → 2 → 3 conversation. **T1 and offline
implementation approved**, as recorded in section 10; each writer still
acknowledges the exact v1 interface and file set before its implementation.
It demonstrates interaction and privacy boundaries, not real AI, live discovery,
actual calendar availability or resolution of the Child Sync blocker.

| Area | Required observable acceptance |
| --- | --- |
| Schema | Pure validators reject unknown keys, malformed/oversized fields, invalid dates/zones, live mode, raw chat/calendar fields, mismatch echoes and unsafe links/assets. All fixtures pass. |
| Stage 1 | Existing grid and controls with synthetic labels; one mount; collapse/reopen preserves local state/scroll/freshness and zero additional calls; independent partial/stale/missing/error states stay visible in Calendar's collapsed strip. |
| Privacy | Controller exposes only three methods, returns no calendar state; chat/request/result/storage contain no calendar fields or internal bridge payload. No DOM scraping or internal event subscription by Builder/Activities. |
| Stage 2 | After approved explicit Start, exactly one synthetic search; basketball/movie illustrated alternatives with timing, evidence, fallback, unknowns and `not_checked`. No setup form; no invented second match when fixtures/constraints do not supply one. |
| Stage 3 | Demo sentence resolves 19–20 September from the fixed reference; a distinct nearer occurrence replaces suggestions using the same search/renderer. Age 8, origin and preferences preserved. No Calendar input/storage/Sync change. Unsupported text asks clarification without searching. |
| Lifecycle | Start double-click dedup, identical query coalescing/reuse, explicit retry budget, timeout, pause/reset, A → B → A and abort-ignoring late responses cannot restore stale cards. |
| Cleanup | Collapse/reset-chat never invoke leave or Clear. True exit invokes non-destructive disposal once despite repeated calls/pagehide race; no late redraw, no snapshot/source deletion. Sticky cleanup failure is not hidden as success. |
| Evidence | Unknown travel never zero; incomparable values not ranked together; known required mismatch filtered before limit; empty/partial/unavailable distinct. Fictional media/copy cannot be described as licensed/live/AI verified. |
| Isolation | Mock providers only; provider/real API/private-file calls all zero, including startup, reload, card rendering, details, collapse and teardown. No model SDK, new dependency, service worker, analytics, profile/transcript persistence or new browser storage key. |
| Accessibility | After a separately agreed isolated browser-review setup: desktop/mobile, actual keyboard, composer labels, focus after collapse/errors/results, live announcements and non-color unknowns; report unavailable checks honestly. |

Validation plan after approval: Builder runs the three new contract/conversation/
integration suites; Calendar runs its new mount suite plus the five existing test
suites listed above (the sixth path is a fixture harness), and relevant saved-view,
date-picker/date-selection and presentation/week regressions read-only if affected;
edits to unlisted tests require an ownership amendment. Activities runs
its two new suites and the inspected bounded discovery runner. Test date logic in
UTC, Asia/Taipei and America/Los_Angeles. Use bounded focused offline runs, not the
known-hanging full suite. Record pre-existing failures separately; do not rewrite
unrelated tests. Parent usability sign-off is separate from automated success.

## 7. Open gates and publication checks

| Gate | State / owner |
| --- | --- |
| Shared contract and synthetic Calendar stage 1 | Owner approved; pure shared module delivered; existing Calendar chat receives sole-writer handoff in section 9 |
| T1 explicit scripted synthetic Start and offline M1 / stages 2–3 | Approved through the owner's Activities handoff message; this turn delivers prerequisites and hands stage 2 to the existing Activities chat, not a service activation |
| Exact v1 interfaces/file reservations | Calendar + Activities acknowledge before coding; report incompatibility rather than editing shared paths |
| Second Child Sync hang | Calendar-reported unresolved real-demo blocker; separate repair authorization/scope, no agent reproduction here |
| Real calendar embedding/Sync | Separate approved path and operational validation after blocker resolution; no recorded-image fake success |
| Real AI/proactive behavior | Provider/runtime/cost/tool/outbound-data/retention/trigger review required; no background or notification permission |
| Movies, wider sources, posters/logos, routing | Source/licensing and bounded live validation decisions required; Xinyi district is not a routing origin |
| Calendar-fit assessment | Separate privacy-reviewed minimum-data contract; absent here, all fit remains `not_checked` |

Existing consent, busy-only redaction, audience restrictions, snapshot preservation,
fail-closed revocation/deletion, late-writer fencing, Host/Origin/CSRF, URL/SSRF,
escaping, bounded requests and separate commit/push approvals are unchanged.
Later saved-child exceptions do not make Calendar's private data available to chat.

Initial publication validation was limited to document consistency, local link targets,
reserved-path collisions and whitespace/editor diagnostics. No product tests,
live browser/calendar reads, service probes/operations, private-file inspection,
network/provider requests, dependency installation or product implementation are
claimed for that documentation-only publication. Later scoped implementation and
offline test evidence are recorded in section 9.

Fresh read-only Calendar and Activities reviewers found no remaining blocking
contract inconsistencies after clarifying in-memory date isolation, collapsed
updates, disposal failure, validator exports, status/evidence semantics,
deduplication and timeout fencing. This is specification review only, not approval
from the owner, runtime verification or a release of another chat's file ownership.

## 8. Copyable implementation handoffs (gated)

These messages do not authorize implementation on their own. Calendar stage 1
has the separate owner approval and handoff in section 9; the owner's subsequent
T1/M1 approval and Activities prerequisite delivery are recorded in section 10.

### Calendar handoff

Read `docs/chat-integration-contract.md` v1 (`familycopilot.chat.v1`), its linked
design/04–06 and your updated agent instructions. Acknowledge the exact stage 1
interface and section 5 single-writer file list; flag any extraction conflict.
The owner has now approved this synthetic stage 1 independently; section 9 records
the prerequisite delivery and sole-writer handoff. Implement only the synthetic
single-instance Calendar seam in
Builder's `#calendar-host`: persistent Calendar-owned collapsed status, expand/
collapse without queries/remount, and idempotent non-destructive dispose on true
exit. Export presentation controls only, not calendar state or the title-free
bridge. Preserve current standalone behavior. Use Calendar-owned synthetic
fixtures/transport and focused lifecycle/privacy tests. Do not touch Builder or
Activities files, real tabs/snapshots, shared services or providers. Treat the
second Child Sync hang as an unresolved real-demo blocker, not part of M1 repair.
Return exact changed files, contract version, executed tests and remaining gates.

### Activities handoff

Read `docs/chat-integration-contract.md` v1 (`familycopilot.chat.v1`), its linked
design/04–06 and your updated agent instructions. Acknowledge sections 3–5's exact
request/result/card seam, bounds and single-writer file list. T1 and offline M1
are now approved, and the shared prerequisites are delivered. The existing
Activities chat may continue with `searchActivities(request, { signal })` and
`renderActivityCards(host, result)` for synthetic mode only, using shared validators
and your own activity fixtures/local original media. Reject raw chat/calendar
payloads and live mode; echo IDs/revision/range, preserve evidence/unknown/partial
states and fixed `calendarFit: "not_checked"`. Stages 2/3 reuse both interfaces;
renderer never searches, calls Calendar or writes storage. Do not change existing
TPBL/forms, shared contract/shell files or operate any service. Add focused schema,
filter/ranking, cancellation and card/fallback tests. Return exact changed files,
contract version, executed tests and remaining gates.

## 9. Shared contract delivery and Calendar handoff

### Scope and consumer entry points, 18 September 2026

The owner requested code/file handoff only, with no service operations, and
confirmed that synthetic Calendar stage 1 is approved. This delivery creates:

- [shared/chat-contract.js](../shared/chat-contract.js): all seven section 3
  functions, frozen API, browser `globalThis.FamilyChatContract` and identical
  CommonJS exports. No imports, DOM, network, storage, timers, current-clock reads,
  logging, Calendar payloads or domain globals. Validation is not authorization.
- [test/chat-contract.test.js](../test/chat-contract.test.js): 32 contract tests,
  including browser/CommonJS loading with forbidden dependencies trapped.
- [test/fixtures/chat-context.js](../test/fixtures/chat-context.js): CommonJS
  `demoContext()` and `activityRequest()` return fresh non-calendar test records.
  This is not a browser runtime, activity search pool or Calendar fixture.

Calendar loads the shared script before its own seam and calls
`FamilyChatContract.validateCalendarMountOptions(options)`. CommonJS consumers
require the shared module; both use exactly `{ version: "familycopilot.chat.v1",
mode: "synthetic" }`. The validator returns a boolean, rejects extra/missing
properties and live mode, and never inspects a DOM host. Calendar retains ownership
of host checks, lifecycle/controller invariants, fixed boundary errors and its
private-shaped synthetic validation. No shell implementation is needed to consume
this module in Calendar's isolated offline test harness.

Implementation clarifications, without enabling another stage:

- `DemoContext.referenceNow` is the fixed scripted reference. Activity requests
  accept explicit valid UTC instants and enforce the forward 28-day window;
  orchestration must keep them tied to its active context. This module supplies
  no clock, active-session authority, trigger execution or deduplication cache.
- Strict uppercase `T`/`Z` timestamps and `+08:00` occurrence offsets are used.
  Invalid Gregorian dates, rollover and leap-second notation are rejected.
  Fractional seconds are compared without rounding away submillisecond boundaries.
- A single known age bound is open-ended; both absent or rule `unknown` means age
  guidance unknown. Producer evidence/prose must not reinterpret a missing bound
  as proof of an admission policy.
- A failed/unsupported source cannot supply cards merely because another source
  completed. Scope issues, source references and status precedence are validated.
- Thumbnail `assetKey` is a 1–80 character ASCII `[A-Za-z0-9_-]` symbolic key.
  Activities still owns compile-time allowlist membership and fallback rendering.
  `unavailable` permits only a neutral fallback and a matching thumbnail evidence
  reference (which may be unknown); `licensed` remains disabled. Only
  `original_synthetic` with synthetic evidence can enable an allowlisted image.
- The serialized card-ID tuple must fit the existing 400-character message
  reference bound after escaping, as well as each identifier's own bound.
- Validators enforce structured references, bounds and uncertainty flags; they
  do not prove source truth, inspect natural-language claims or confirm that every
  required gap is described in prose. Activities tests/rendering must ensure
  those disclosures and text-node rendering. HTTPS validation is link validation,
  not SSRF protection or permission to fetch. No fetcher exists in this module.
- Ordinary JSON-like records/arrays are accepted, including cross-realm values.
  Accessors, hidden/symbol extras, custom prototypes and sparse arrays are rejected
  without calling input getters. Arbitrary JavaScript Proxies are not JSON data;
  reflection can invoke proxy traps, so this is not a hostile-code sandbox.

### Sole-writer release

The owner explicitly confirmed in this Builder conversation that the existing
Calendar chat is waiting for delivery and other writers have stopped editing the
section 5 Calendar paths. **The existing Calendar chat is now the sole writer**
for its five reserved new paths and eleven existing paths already enumerated in
section 5, within the approved synthetic stage 1 scope. No extra path is released.
Builder and its delegates do not write those paths. No new Calendar implementation
agent was started; the delegated Calendar check was read-only interface review.

All eleven existing Calendar code/test/fixture files were SHA256-compared before
and after delivery and remained byte-identical. Existing uncommitted changes in
[owner/child-calendar-ui.js](../owner/child-calendar-ui.js) and
[test/child-sync-ui.test.js](../test/child-sync-ui.test.js) were preserved, not
reverted or treated as new work by Builder. Preserve the other pre-existing
diagnostic/backend changes too; they do not expand this handoff's file list.

This confirms coordination based on the owner's explicit statement and that this
delivery did not change Calendar files. Stable hashes are **not** a filesystem
lock or proof that another independent chat cannot resume. Any later writer or
unexpected file drift requires stopping the conflicting edit and coordinating
again. Shared-service state was not probed. Existing service coordination rules
remain in force; this handoff grants no start/stop/restart, live query or activation.

### Executed validation and remaining scope

- Before edits: `node --test test/app.test.js test/date-selection.test.js`,
  **60 passed, 0 failed**.
- Three new regression cases first reproduced acceptance of failed-source cards,
  rejection of unavailable thumbnail fallback and oversized escaped card IDs:
  **29 passed, 3 failed** before the fixes.
- Final `node --test test/chat-contract.test.js test/date-selection.test.js
  test/app.test.js`: **92 passed, 0 failed in each of UTC, Asia/Taipei and
  America/Los_Angeles**. This includes 32 new contract tests. No full suite run.
- Editor diagnostics and scoped whitespace checks passed. Read-only Calendar
  review found no shared-interface blocker; this is not another chat's lifecycle
  acceptance or evidence of an implemented mount.

Calendar continues its own implementation after this handoff, rereading the
section 5 limits and preserving standalone semantics. No Calendar implementation,
browser/parent usability validation, real Sync, private-data inspection, service
operation, provider call, dependency installation, commit or push occurred here.
At that stage-1 handoff, T1/stages 2/3 and full M1 were still gated; section 10
records their later offline approval. Service/browser-review setup remains
separately gated. The second Child Sync blocker is not repaired by this delivery.

## 10. T1/M1 approval and Activities handoff

### Latest owner-relayed approval, 18 September 2026

The owner relayed Activities' statement that T1/M1 is approved and asked Builder
to deliver the v1 validators, contract tests and synthetic context fixture first,
then hand off synthetic search and reusable cards to Activities. This record
supersedes the earlier T1/M1-pending coding status, not the operational/privacy
boundaries. No service start/stop/restart, live source, private calendar access,
storage, real AI, deployment, commit or push is approved by this handoff.

### Delivered prerequisites

- [shared/chat-contract.js](../shared/chat-contract.js): frozen
  `FamilyChatContract` browser namespace and matching CommonJS exports:
  `validateDemoContext`, `validateActivityRequest`, `validateActivityResultShape`,
  `validateActivityResult`, `validateCalendarMountOptions`, `activityRequestKey`
  and `activityCardId`. Consume this implementation, do not fork its validators.
- [test/chat-contract.test.js](../test/chat-contract.test.js): the shared contract
  regression suite, including mode/correlation/date/evidence/status/URL/identity
  checks, canonical keys and side-effect isolation.
- [test/fixtures/chat-context.js](../test/fixtures/chat-context.js): fresh
  `demoContext()` / `activityRequest()` factories, synthetic only, one age 8,
  Xinyi district, no travel-mode assumption and no Calendar data. Context uses
  fixed `2026-09-18T04:00:00Z` and initial activity dates 9–11 October 2026.
  Tests may derive a fresh 19–20 September request with new correlation values;
  do not shift October occurrences into September. These are CommonJS test
  factories, not a persistent profile or runtime session-ID allocator.

All three prerequisite files are unchanged in this follow-up. Activities owns its
separate occurrence pool and local media; Builder's context is not an activity
provider. Search validates the request and request-correlated result; cards call
`validateActivityResultShape` without reconstructing a request from the result.
The shared module does not implement search, cancellation, dedup caching or any UI.

### File handoff and implementation acceptance

The **existing Activities chat** receives the sole-writer assignment for its
nine reserved paths in section 5, plus only the permitted suite-inclusion edit to
[scripts/test-activity-discovery.js](../scripts/test-activity-discovery.js).
Builder retains the shared module/tests/context and this contract; Calendar keeps
its existing assignment. No additional Activities implementation writer is
launched from this Builder conversation. Preflight found all nine new Activities
paths absent and no Git modification of its existing runner. These observations
are not a lock on independent chats: the receiving writer must acknowledge v1,
the exact file list and any newly discovered collision before editing.

Implement only `searchActivities(request, { signal })` and
`renderActivityCards(host, result)` for synthetic mode, with focused offline tests
and designer-guided cards. Required behavior remains sections 3–4 and 6:

- Required out-of-band abort signal; reject aborted/malformed/live requests before
  work. Return exact correlation echoes; retain distinct unavailable/partial/
  unknown/empty/results, fixed occurrences, field-matched evidence and visible gaps.
- Deterministic bounded search: at most 40 examined candidates / 6 cards; required
  violations filtered before ranking; over-cap scope becomes partial. Never invent
  a second suggestion, a nearer game, travel numbers or calendar compatibility.
- Reuse the same renderer for stages 2/3; local allowlisted original synthetic
  assets only, truthful provenance and fallback, safe text/links, no renderer
  search/storage/Calendar access. All `calendarFit` values stay `not_checked`.
- T1 execution, the composer, generation fencing and the 10-second timeout remain
  Builder orchestration responsibilities, not new Activities triggers or timers.
  No raw chat/Calendar data reaches search. Existing TPBL/forms/services stay intact.

Return exact files changed, executed test results and any contract incompatibility
to Builder. Do not change shared validators or another owner's files to unblock
the implementation. No review port or service startup is assigned here.

### Revalidation in this handoff

`node --test test/chat-contract.test.js` passed **32/32 separately in UTC,
Asia/Taipei and America/Los_Angeles**, with zero failures/cancellations/skips.
All seven exports and both fresh fixture factories were checked; default context
and request pass their validators. Earlier combined 92-per-timezone results in
section 9 are historical, not a newly repeated combined run. Only this contract
document is edited in this follow-up; Activities implementation remains with its
existing chat. There were no service operations or browser/live-data checks.

## 11. Builder offline shell implementation

The subsequent owner request to start implementation authorizes this Builder
slice of the approved T1/M1, not service activation or new domain ownership.

- [chat/conversation-core.js](../chat/conversation-core.js) supplies frozen
  `FamilyChatConversation` / CommonJS exports: `initialContext`,
  `interpretWeekend`, and `createConversation`. Runtime context is page-local;
  a regression compares its defaults to the shared non-calendar test fixture.
  The controller provides `start`, `submit`, bounded `refine`, `retry`, `pause`,
  `resume`, `reset`, `end`, `dispose`, and detached `snapshot` values.
  `end` clears the session like reset; `dispose` is terminal.
- [chat/index.html](../chat/index.html), [chat/chat.css](../chat/chat.css), and
  [chat/ui.js](../chat/ui.js) provide the static conversation surface, separate
  Calendar host, composer and explicit session controls. The frozen
  `FamilyChatUI.mountChat(document, { calendar, activities, lifecycle })` accepts
  the reviewed domain seams; the same document returns the same handle.
  Classic-script startup uses `FamilyChatCalendar` / `FamilyChatActivities` when
  supplied. At that checkpoint HTML loaded only delivered Builder scripts, so absent
  domain components remain visibly disconnected with search disabled.
- [test/chat-conversation.test.js](../test/chat-conversation.test.js) covers
  reference dates, exact scripted intent, Start/Retry coalescing, A-B-A fresh
  requests, cancellation ignored by providers, timeout, bounded budgets/history,
  partial/unavailable explicit retry, immutable snapshots, pause/reset/disposal.
- [test/chat-integration.test.js](../test/chat-integration.test.js) uses synthetic
  interface doubles, not delivered domain implementations, to verify one Calendar
  mount, no Calendar DOM reads, no startup query, reusable rendering, text-node
  escaping, card disposal, fail-closed missing components and cleanup failure.

Acceptance invalidation precedes abort. Late fulfillment/rejection and stale
timeout callbacks cannot mutate the current request. Replaced cards and their
historical card references retire immediately. Each session dispatches at most
three searches; generation never resets. Search receives only the validated
request and AbortSignal, never chat text or Calendar data. Reset/refinement do not
touch Calendar. Calendar retains its pagehide/pageshow ownership; the shell's
pagehide listener disposes chat state only, while explicit host teardown calls the
Calendar handle's idempotent disposal. A bfcache-restored shell stays ended.

Executed validation: shared-contract + conversation + interface-integration suites
passed **50/50 separately in UTC, Asia/Taipei and America/Los_Angeles**, with zero
failures/cancellations/skips. The initial additional partial-result regression
used an invalid test issue code; the fixture was corrected to the existing v1
schema without relaxing validation.

**Not full M1 acceptance:** Calendar mount and the reusable Activities renderer
were not yet delivered when this slice was assembled. Their separate writers
were creating domain files concurrently and remain sole writers. No substitute
calendar, duplicate activity pool, live adapter or fake second suggestion was
added. The next integration step is to consume their acknowledged entry points,
dependency order and styles, add actual-seam tests, then perform separately
coordinated isolated desktop/mobile browser review. No live/private data, service
operation, parent usability approval, commit or push is part of this slice.

## 12. Delivered domain integration

The owner first requested integration of the delivered Activities synthetic v1,
then relayed Calendar's completed stage 1 and exact dependency order. Builder
consumes both under `familycopilot.chat.v1`; domain files remain read-only.
This increment changes only [chat/index.html](../chat/index.html),
[test/chat-integration.test.js](../test/chat-integration.test.js) and this document.
Existing uncommitted domain, runner, harness and shell work is preserved.

### Wiring and isolation

The persistent `#calendar-host` exists before deferred scripts execute. Load order:
shared contract, Calendar availability/child cores, shared dates, availability/child
initializers, Calendar template/fixtures/seam, Activities fixtures/assets/search/cards,
conversation core and shell UI. Only the scoped Calendar and Activities stylesheets
are added alongside chat CSS; no standalone owner page, owner controller/global
stylesheet, live adapter, route, service or package change is needed.

The existing shell calls `FamilyChatCalendar.mountCalendar` once with synthetic v1
options, and uses `FamilyChatActivities.searchActivities` / `renderActivityCards`
unchanged for both initial suggestions and refinement. No duplicated renderer,
activity pool, Calendar controller or date logic was added. Network remains blocked
by `connect-src 'none'`. Images resolve only to Activities' local allowlisted assets.

### Executed integration evidence

Five new tests extend the existing six interface tests in the same integration
file, reusing Calendar's DOM harness read-only for the combined cases:

- Actual Activities classic scripts load from the HTML dependency list: zero
  startup/typing/focus searches; explicit double Start makes one request; two
  October alternatives become one distinct September 20 screening after the
  displayed September 19-20 interpretation. Preferences remain unchanged.
- Real card DOM, original local assets, image-error fallback, details without
  searches, completed-request reuse, old-card listener disposal and Reset cleanup
  execute through the delivered renderer. No nearer game is invented.
- Activities works without Calendar; pause/resume, reset, explicit retry and
  pagehide fence fixture work. Delayed actual results ignoring later cancellation
  cannot replace a newer result or restore cards after exit.
- The complete HTML script list mounts the real Calendar and Activities together.
  Synthetic Calendar Sync does not start discovery. Collapse/refinement/Reset/reopen
  preserve Calendar dates, grid identity, original freshness and scroll. Shell DOM
  lookup is restricted to its own nodes; network/storage traps and Calendar's
  external-event observations verify no private-data handoff in this harness.
- True teardown waits for real Calendar disposal, coalesces a pagehide race into
  one `reason: "leave"`, never sends explicit Clear, retains both Calendar safety
  and shell failure notices on `calendar_cleanup_unconfirmed`, and never retries
  or remounts. Timers are released.

Baseline before Activities wiring: **71/71** across contract, conversation,
integration and both Activities suites. Calendar baseline: **23/23**. Final command:

```sh
node --test test/chat-contract.test.js test/chat-conversation.test.js test/chat-integration.test.js test/chat-activity-search.test.js test/activity-cards.test.js test/chat-calendar.test.js
```

Run separately with `TZ=UTC`, `TZ=Asia/Taipei`, and `TZ=America/Los_Angeles`:
**99 passed, zero failed/cancelled/skipped in each**. Integration alone: **11/11**.
Editor diagnostics and whitespace checks passed. These are newly executed results,
not the domain owners' earlier 21/21 and 285/285 handoff totals.

### Remaining gates

No browser was opened or inspected for this increment. Desktop/mobile rendering,
asset loading under an actual browser CSP, physical keyboard interaction and parent
usability remain unverified pending a separately coordinated isolated review setup.
No services were started, stopped, restarted or probed; no live provider, real
calendar/tab, private snapshot, model, new storage, dependency, commit or push was
used. The bounded discovery runner was not rerun because it includes local-service
tests; its reported pre-existing Calendar shell timezone failure was not changed.
Real Calendar proof and the reported second Child Sync blocker remain separate from
this synthetic integration. Real AI, movies/routing and calendar-fit assessment
retain their existing approval gates; `calendarFit` remains `not_checked`.

## 13. Isolated synthetic browser review

The owner separately approved one isolated synthetic static service, browser
desktop/mobile/keyboard review and repairs limited to Builder-owned files, with
the service stopped afterward. Existing services and live providers remained
excluded. This follow-up changed only this evidence record: no reproducible
Builder defect was found that required a product-code change.

The temporary loopback service used OS-assigned port 37661 and served only the
chat HTML, its explicitly referenced JS/CSS and the two original synthetic images.
Other paths and non-GET/HEAD methods were rejected. There were no API handlers or
live adapters. A new isolated browser page was used; existing calendar tabs and
private snapshots were not inspected.

Observed acceptance:

- Actual measured viewports were 1920, 375 and 321 CSS px, not the dimensions
  requested through the scaled browser tool. No document horizontal overflow was
  observed, including a 500-character unbroken message. Desktop and mobile
  screenshots showed readable cards, wrapped content and no overlapping controls.
- All required scripts/styles loaded under the existing CSP. Both local original
  SVG images decoded at 800px natural width; a dispatched image-error event showed
  the neutral fallback. This was an error-handler check, not a real failed fetch.
- Browser keyboard Enter operated synthetic Sync, Calendar collapse/reopen,
  Start, Details, Pause/Resume, Retry and Reset. ArrowRight scrolled the narrow
  Calendar grid. Tab moved from composer to Send; Enter submitted the scripted
  weekend request and focus returned to the input. Reset returned focus to Start.
- Two October cards became one distinct September 20 screening with the displayed
  September 19-20 search dates. Pause cleared cards; Resume alone left them empty;
  explicit Retry restored results. Unsupported text requested clarification.
- Awaited shell disposal coalesced repeated calls, cleared cards, disabled input
  and completed synthetic Calendar leave without a cleanup-failure notice.
  Browser resource entries showed zero fetch/XHR and zero external requests
  across the reviewed flow, including teardown.

The browser page was released to `about:blank`; the owned service received SIGINT
and the terminal reported process completion. No shared-service operation, live
query, real account access, dependency installation, commit or push occurred.

The 99-per-timezone Node results in section 12 remain the earlier executed
regression evidence, not a newly repeated test run. Browser keyboard events are
not physical-device, assistive-technology or parent usability sign-off. Those
reviews, actual calendar proof, the independently reported Child Sync issue, real
AI/movie/routing sources and calendar-fit approval remain separate gates.

## 14. Editable preferences and docked composer

The owner's next approved increment makes age, starting area and interests actual
page-local settings, not immutable fixture constraints. Section 3.2 contains the
amended schema. The envelope remains `familycopilot.chat.v1`; Calendar mount,
date storage and provider contracts are unchanged. This is a coordinated local
validator/search rollout, not a claim of compatibility with older deployed validators.

### Ownership and behavior

- Builder changed [shared validator](../shared/chat-contract.js),
  [contract tests](../test/chat-contract.test.js), [conversation state](../chat/conversation-core.js),
  [state tests](../test/chat-conversation.test.js), [markup](../chat/index.html),
  [styles](../chat/chat.css), [UI](../chat/ui.js), [integration tests](../test/chat-integration.test.js)
  and this record. The existing context fixture remains the default, not a restriction.
- Activities acknowledged the amendment before its sole-writer handoff, then
  changed only [search](../activity-preview/chat-search.js) and
  [search tests](../test/chat-activity-search.test.js). Age mismatch text now uses
  the requested age; recommended mismatch remains `needs_checking`, required
  violations are excluded. Interests affect ranking, not source eligibility.
  Changing area does not move fictional venues, geographically filter the fixed
  pool, imply nearby suitability or generate routing/travel facts.
- The collapsed summary has an accessible edit icon. The editor uses one native
  number input, a coarse-area text input, checkbox labels and Apply/Cancel.
  Escape/Cancel discard drafts and restore edit-button focus. Drafting never
  mutates committed context, triggers a search or invalidates current cards.
- Apply trims the area at the UI boundary, validates the complete candidate
  context and fails without mutation on invalid input. Unchanged Apply is a
  no-op. Changed Apply retires cards and result references, advances revision
  and generation, aborts work and ignores late responses even if abort is ignored.
  It does not dispatch, reset the three-search budget or resume a paused session.
  `Update suggestions` is an explicit `explicit_retry`; subsequent refinement
  preserves the applied preferences. Reset conversation/reload restores defaults.
- Composer is bottom-docked with safe-area padding. ResizeObserver reserves its
  measured height; visualViewport resize/scroll handles keyboard overlay/panning.
  The viewport requests resize-content behavior where supported. Scroll padding
  and document bottom space keep final messages/cards reachable above the composer;
  input fields use 16px text. Geometry listeners/observer are released on teardown.
- No Calendar internals were edited or copied into conversation state. Existing
  Calendar dates, grid identity, source/freshness display and transport calls stay
  unchanged through Apply and explicit activity refresh. No retained profile,
  transcript, extra storage key, live source or automatic Sync was introduced.

### Executed verification

The six focused suites from section 12 were newly executed in `UTC`,
`Asia/Taipei` and `America/Los_Angeles`: **121/121 in each**, with zero failures,
cancellations or skips. Integration alone is **15/15**. The Activities owner also
ran search/cards **30/30 in each timezone** during its handoff. These totals are
execution evidence, not parent review or live-provider proof. The broad discovery
runner remains excluded because it includes service tests outside this scope.

Builder added the sole-owned [browser acceptance script](../scripts/test-chat-browser.js).
Run `node scripts/test-chat-browser.js` with `FAMILYCOPILOT_DEMO_TOOLS` pointing to
an existing isolated Playwright installation; use its `PLAYWRIGHT_BROWSERS_PATH`
and, if required, `LD_LIBRARY_PATH`/`FONTCONFIG_FILE` as described in
[demo tooling](demo-video.md#reusable-recorder). It installs nothing and starts no
HTTP listener. A fresh headless Chromium context serves only allowlisted local
assets through interception at `familycopilot.invalid`; unexpected requests,
native network APIs and browser storage fail the test. Contexts/browser close
in finally blocks. Screenshots/reports go into ignored `browser-artifacts/`.

The final run passed at **1440, 375 and 320 CSS px**. It checks actual browser
Enter/Tab/Space/Escape/ArrowUp events, native invalid-age handling, draft/Cancel,
Apply without dispatch, changed-preference ranking, unchanged Calendar state,
weekend refinement, image decoding, 120-character summary wrapping, text-area
resize, final message/card clearance, Reset/reload and teardown. No horizontal
overflow, unexpected resource, CSP violation or forbidden I/O was detected.
Final evidence is in `browser-artifacts/chat-preferences-ddOTfA/`; desktop/mobile
screenshots were visually reviewed. Editor diagnostics and whitespace checks passed.

The integrated browser tool initially returned a hidden page and did not dispatch
Enter; that was not counted as keyboard success. The independent headless suite
provided the keyboard evidence above, and the tool's isolated page was released
to `about:blank`. The existing user rehearsal static service/page was not stopped
or replaced. No shared-service operation, private calendar access, installation,
commit or push occurred.

Mobile keyboard resize, overlay and viewport-panning geometry were simulated;
**physical iOS/Android keyboards and assistive technology were not tested**.
Human/device usability review remains distinct. Real AI, source expansion,
routing, real Calendar proof and the separate Child issue retain their existing gates.

## 15. Activities handoff revalidation

On the subsequent Activities synthetic v1 handoff request, Builder inspected the
delivered search, renderer, asset allowlist, scoped CSS and current HTML bootstrap.
The section 12 wiring and section 14 preference integration were already present
in the working tree and were preserved, not rewritten. The shell consumes the
same `FamilyChatActivities.searchActivities` and `renderActivityCards` exports for
initial and refined requests. Fixtures/assets/search/cards load before shell
startup, and the HTML includes the Activities stylesheet and local image paths.

This follow-up changes only this record and
[test/chat-integration.test.js](../test/chat-integration.test.js). The existing
HTML-driven integration harness now supports controlled timers. An additional
regression runs the actual Activities search and renderer through shell timeout:
the completed-but-delayed result is retired, no automatic retry or card rendering
occurs, explicit double Retry coalesces into one fresh request, the old timeout
cannot cancel that request, and only its accepted result renders cards. Calendar
is not disposed by this path; the timer is released.

Newly executed verification: the six focused suites listed in section 12 passed
**122/122 separately in UTC, Asia/Taipei and America/Los_Angeles**, with zero
failures/cancellations/skips. Integration alone passed **16/16**. The initial
search/cards plus integration baseline passed **45/45** (30 domain tests and
15 existing integration tests). Editor diagnostics reported no errors.

No broad discovery runner or browser acceptance script was executed in this
follow-up. The reported pre-existing Calendar shell timezone failure outside
these focused suites was neither repaired nor claimed resolved. No services were
started, stopped, restarted or probed; no live provider, private data or existing
browser page was accessed. Historical browser evidence above is not a new run.
Domain-owned implementation and runner changes remain untouched.

## 16. Family header and pre-Sync presentation

On 19 September the owner approved moving the family names above Calendar and
clarifying the child's age setting. The owner separately confirmed the Calendar
writer had stopped and handed Builder the necessary presentation changes for
this increment. Actual Calendar edits are limited to
[owner/chat-calendar-template.js](../owner/chat-calendar-template.js),
[owner/availability-ui.js](../owner/availability-ui.js) and
[test/chat-calendar.test.js](../test/chat-calendar.test.js). Existing changes in
those files are preserved; this is not a continuing transfer of Calendar ownership.

The upper shell now shows a wrapping `Our family` list of Parent A, Parent B and Child,
explicitly labeled synthetic. These are static demo labels, not extracted from
Calendar or an authenticated family profile. A separate `Children` setting shows
`Child 1` and its age; the editor calls the field `Child's age`. One editable age
remains supported. The list-shaped presentation can accommodate more children
later, but no nonfunctional Add child control or new multi-child schema is added.
No automatic association between the age preference and Child is made.

The earlier requested interest choices are now Basketball, Baseball, Ping-pong
and Movie. The Movie checkbox keeps the existing `movies` request value for
compatibility. Applying choices does not query; these are ranking preferences,
not claims that the synthetic provider has baseball or ping-pong occurrences.

Within chat only, the untouched initial Calendar state displays `Our week` and
Sync, without an empty month, Not loaded roster, snapshot presentation, Clear or
collapse control. Calendar's own render path checks its existing state, never
the shell reading or scraping Calendar DOM. Sync reveals source/loading status;
the month appears only with loaded data. Partial, unavailable, revoked, expiry
and cleanup states retain their existing truthful disclosure. Collapse after
loading still preserves state and freshness. Actual snapshot retention, fixed
provider bounds, consent, transport and standalone Calendar behavior are unchanged.

Builder files changed for this UX increment: [chat/index.html](../chat/index.html),
[chat/chat.css](../chat/chat.css), [chat/ui.js](../chat/ui.js),
[test/chat-integration.test.js](../test/chat-integration.test.js),
[scripts/test-chat-browser.js](../scripts/test-chat-browser.js) and this record.
The browser script includes startup visibility, member ordering, child-age labels,
the four interests and a no-family-names-in-search assertion.

Verification executed:

- Nine bounded offline suites (the six chat/domain suites from section 12 plus
  availability-ui, our-week and child-sync-ui) passed **294/294 in each of UTC,
  Asia/Taipei and America/Los_Angeles**, with no failures/cancellations/skips.
  The new Calendar regression covers zero-query startup, Sync progress and failure
  visibility. Integration asserts family names never enter activity requests.
- A new browser page used the already running isolated synthetic preview on port
  34393. Screenshots were reviewed at measured 1440px desktop and 320px mobile:
  family names appear above Calendar, no document horizontal overflow, and the
  pre-Sync Calendar visible text is only Our week and Sync. Keyboard Enter opens
  preferences and focuses the age field; Escape closes it; Enter on Sync shows
  loaded/partial source states and the month without changing the top roster.
  Resource entries showed zero fetch/XHR. The page was reloaded to its initial
  state for owner review. No service was started, stopped or restarted.
- Editor diagnostics and browser-script syntax checks passed. The complete
  standalone browser script was not rerun because its earlier isolated Playwright
  installation could not be located; the targeted integrated-browser observations
  above are the current UI evidence, not a claim of full-script execution.

No live provider, real Calendar page, private snapshot, new persistence, package
installation, commit or push was used. Multi-child behavior and parent/device
usability acceptance remain separate from this one-child presentation change.

## 17. One conversation start for Calendar and suggestions

The owner approved replacing the separate Our week / Sync step with one explicit
Start conversation, followed by the month Calendar appearing inside the chat and
then activity suggestions. This is a synthetic/scripted workflow amendment only.
The later Go authorizes this continuation of the confirmed Calendar presentation
handoff: Builder extends the existing seam and controller, rather than creating
a second Calendar or dispatching a hidden button click. Calendar remains the
owner after this scoped integration change; Activities files remain untouched.

Changed paths: [chat/index.html](../chat/index.html), [chat/chat.css](../chat/chat.css),
[chat/ui.js](../chat/ui.js), [chat/conversation-core.js](../chat/conversation-core.js),
[owner/chat-calendar-template.js](../owner/chat-calendar-template.js),
[owner/chat-calendar.js](../owner/chat-calendar.js),
[owner/availability-ui.js](../owner/availability-ui.js),
[test/chat-calendar.test.js](../test/chat-calendar.test.js),
[test/chat-conversation.test.js](../test/chat-conversation.test.js),
[test/chat-integration.test.js](../test/chat-integration.test.js),
[scripts/test-chat-browser.js](../scripts/test-chat-browser.js) and this record.

The Calendar host is mounted once but its conversation region is hidden before
Start. There is no standalone Calendar section or visible Sync button. The
Calendar-controlled hidden action node remains an internal controller dependency,
never a Builder integration hook. Start reveals the same host, shows a loading
message and calls `loadSynthetic()` with no arguments. The existing Calendar
controller performs its existing synthetic child-then-parent flow. Its Promise
never exports source data or a success assessment. Calendar still owns source,
freshness, partial/missing state and safety messages. No model reads Calendar.

Conversation `prepareSession` is optional and receives no chat/request arguments.
Its output is discarded. Initial activity work waits for it to settle; rejection
still allows independent activity suggestions without claiming Calendar success.
Preparation has a 10-second timeout; activity search receives its own 10 seconds.
Preparation timeout retires the request without an automatic search or retry.
An explicit Update suggestions may search independently. The initial attempt
reserves one of the three session search slots even if canceled before dispatch.
Double Start coalesces, and pause/reset/dispose/preference changes fence late
settlement. A retired preparation timer cannot time out the subsequent search.

Reset clears conversation state/cards and hides the conversation Calendar region,
but does not clear, remount, refetch or alter Calendar dates, collapse state or
freshness. Another explicit Start reveals the preserved instance and awaits the
same settled/in-flight load Promise. Pause cancels pending suggestions only;
the already explicitly authorized synthetic Calendar load may finish independently.
True page exit retains Calendar's existing fenced non-destructive leave and
visible unconfirmed-cleanup handling. Reload never starts a load automatically.

The original 3-method Calendar controller contract and separate Sync descriptions
in historical sections are superseded only by this amendment. No live provider,
calendar-fit assessment, unsolicited startup behavior, retained transcript/profile,
new dependency or service activation is authorized. `calendarFit` remains
`not_checked`; the UI continues to label the session scripted/synthetic.

Executed verification for this amendment: the nine focused suites from section
16 passed **302/302 separately in UTC, Asia/Taipei and America/Los_Angeles**, with
zero failures/cancellations/skips. Preparation ordering, coalescing, paused/reset/
disposed sessions, preference changes, rejected preparation, stale timers and
real-domain failure paths are covered. Actual-seam tests verify that another Start
after Reset does not repeat Calendar transport and no names enter activity requests.

The shared synthetic preview was reloaded without a service restart. Browser
keyboard Enter on Start revealed the month and two rendered cards; Sync stayed
hidden. At measured 1440px desktop and 320px mobile there was no document overflow,
and local activity images loaded. Keyboard collapse followed by a weekend message
produced one distinct September 20 card while Calendar retained its October scope.
Resource entries showed zero fetch/XHR. Desktop/month and mobile/result screenshots
were inspected. The page was returned to the initial Start conversation view.
Editor diagnostics, script syntax and scoped whitespace/link checks passed.
The standalone browser acceptance script was updated but not fully executed;
the previous isolated Playwright installation remains unavailable. No live data,
service operation, dependency installation, commit or push occurred.

### Conversation controls amendment

The owner subsequently approved removing the permanent four-button control row.
Only Start conversation is visible before a session, and it hides after Start.
Stop appears only during Calendar preparation or activity loading. It uses the
existing pause fence; sending the next message explicitly resumes before normal
scripted interpretation, without an automatic search on resume. Retry appears
only for incomplete/failed work while budget remains. Preference Apply still
does not search; its contextual Show suggestions action preserves explicit consent.
New conversation is inside a closed native options disclosure with an accessible
ellipsis control and Escape support. Reset semantics and Calendar ownership are
unchanged; no Calendar or Activities implementation changed in this amendment.

Executed checks: conversation, integration and Calendar suites passed 71/71;
editor diagnostics and scoped whitespace/script syntax checks passed. The shared
synthetic browser confirmed initial Start only, no completed-session action row,
composer focus after loading, and 320px options/keyboard Escape/New conversation
without overflow or fetch/XHR. The reusable browser script now opens the options
before reset; its complete standalone run was not repeated.

### Message-first amendment

The owner clarified that the composer itself must start the conversation. There
is no Start conversation button or hidden click handler. The composer is enabled
on initial render and after New conversation; the latter returns focus to it.
Mount/reload/typing/empty submission never loads Calendar or searches activities.
Missing Activities still disables the composer; no live fallback was introduced.

An explicit supported opening message is retained as a parent message, resolves
the bounded activity dates first, then starts the existing preparation pipeline.
The internal `demo_start` trigger remains unchanged. A first weekend request
searches September 19-20 only, never an intermediate October request. Simple
Hi/Hello/Hey greetings start the explicitly labeled October demo dates. Other
opening requests receive the existing scripted clarification without provider work;
no general model understanding is claimed. Empty/invalid input cannot start a
session, and duplicate submissions during preparation coalesce without bypassing
Calendar settlement. Raw messages are never added to activity request projections.

Stop, bounded retry, preference Apply, Reset and Calendar's once-per-page loading
retain their previous semantics. This supersedes historical button-based trigger
wording, not source permissions, storage policy, date bounds or calendar-fit rules.
Existing integration tests now submit through the composer instead of simulating
the removed button; new cases cover first-message dates, clarification, typing,
blank submission and actual Calendar/Activities ordering.